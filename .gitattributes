# SSE 夹具与黄金输出做逐字节比对，Windows 上 git 默认的 LF→CRLF 转换会让新克隆一测就红。
*.sse text eol=lf

# 内嵌的官方 Codex `prompt.md` 逐字节副本（Apache-2.0，见 THIRD_PARTY_NOTICES.md）。
# 🔴 `-text` 而不是 `text eol=lf`：这份文件的价值全在「逐字节未改」，而
# `core.autocrlf=true`（本机就是）会让新克隆得到 CRLF = 21178 字节 —— 许可声明里那个
# 可核对的 20903 就当场变成假话，`include_str!` 嵌进二进制的也不再是原文。
# 有 `template_is_the_official_prompt_byte_for_byte` 那条断言配套盯着这个数。
src-tauri/src/tools/codex_base_instructions.md -text
