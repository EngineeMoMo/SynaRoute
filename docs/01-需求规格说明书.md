# SynaRoute 软件需求规格说明书（SRS）

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | SynaRoute 软件需求规格说明书（Software Requirements Specification） |
| 产品名称 | SynaRoute |
| 文档版本 | v0.1 |
| 编制日期 | 2026-07-15 |
| 文档状态 | 待评审（Draft, Pending Review） |
| 适用范围 | MVP 首个版本（对齐 cc-switch 全功能 + 核心路由/聚合能力） |
| 编制角色 | 需求分析师 |

> 版本变更记录

| 版本 | 日期 | 变更说明 | 状态 |
|---|---|---|---|
| v0.1 | 2026-07-15 | 首版需求草案，用于内部评审 | 待评审 |

---

## 2. 引言

### 2.1 目的

本文档定义 SynaRoute 桌面软件的功能需求与非功能需求，作为设计、开发、测试与验收的统一依据。目标读者包括产品经理、架构师、前后端开发、测试工程师以及项目干系人。

本文档中"必须/应/可"遵循如下约定：
- 必须（P0）：MVP 交付必备，缺失即视为不可发布。
- 应（P1）：重要能力，尽量在首版交付，可有降级方案。
- 可（P2）：增强能力，允许延后至后续迭代。

### 2.2 产品范围

SynaRoute 是一款 **Windows 桌面软件**，本质是"智能 API Key 路由代理 + 配置管理界面"。它面向使用 Claude CLI、Claude 桌面端、Codex 桌面端等 AI 编码/对话工具的用户，解决以下核心问题：

1. **多厂商 Key 的统一管理与无感切换**：在一个工具下配置多个厂商 API Key，主 Key 失败/超时时自动故障转移，用户无需手动改配置。
2. **多模型协作提升准确性（大脑聚合）**：多个模型并行解答，聚合后交由最终决策者产出更优答案。
3. **配置的可视化管理**：卡片式 Provider 列表、系统托盘快速切换、深浅主题等，对齐 cc-switch 使用体验。

范围内：
- 三类目标工具（Claude CLI、Claude 桌面端、Codex 桌面端）的 Key/模型/参数/映射配置管理。
- 本地路由代理端点的生成与生命周期管理。
- Key 无感切换（故障转移）与切换前健康检查。
- 大脑聚合（并行解答 + 两种聚合方式 + 最终决策者）。
- 应用内加密文件方式的 Key 存储。
- 系统托盘、深浅主题、卡片式管理界面等 cc-switch 类基础功能。

范围外（本版本明确不做，除非"待确认问题"结论要求纳入）：
- 移动端 / macOS / Linux 客户端（首发仅 Windows）。
- 云端账户体系、多设备配置同步。
- 计费/用量成本核算的完整报表（待确认，见第 7 节）。

### 2.3 术语表

| 术语 | 说明 |
|---|---|
| SynaRoute | 本产品名称 |
| 目标工具 | 使用 SynaRoute 生成的路由代理的下游工具，包括 Claude CLI、Claude 桌面端、Codex 桌面端 |
| Provider（厂商） | 提供 API Key 与模型服务的上游供应商，如 Anthropic 官方、第三方兼容厂商、GLM 厂商等 |
| Key | 某个厂商下的一条 API Key 配置项，含 base_url、密钥、模型列表、参数、模型映射等 |
| 分类（Category） | 三个目标工具对应的三个配置分类：Claude CLI / Claude 桌面端 / Codex 桌面端 |
| 路由代理（Proxy） | SynaRoute 在本机启动的本地 HTTP 服务端点，供目标工具连接，负责转发与故障转移 |
| 故障转移（Failover） | 主 Key 请求失败/超时后，自动切换到下一个可用 Key 的策略 |
| 健康检查（Health Check） | 切换前对候选 Key 做可用性探测，避免切到不可用 Key |
| 模型映射（Model Mapping） | 将厂商真实模型名映射为工具期望的模型名，如 `GLM5.1 → opus-4-7` |
| 大脑聚合（Brain Aggregation） | 多模型并行解答 + 聚合 + 最终决策者产出最终答案的协作机制 |
| 最终决策者（Decider） | 大脑聚合中负责综合各成员结果、产出最终答案的模型 |
| 成员（Member） | 参与大脑聚合并行解答的 Key + 模型组合 |
| 压缩汇总（Compressed） | 聚合方式 A：先压缩汇总各成员结果再交决策者，减少上下文与 token |
| 全量上下文（Full Context） | 聚合方式 B：将各成员完整结果交决策者 |
| 熔断（Circuit Breaker） | 对连续失败的 Key 临时摘除，避免反复重试拖慢整体 |

### 2.4 参考资料

| 编号 | 名称 | 说明 |
|---|---|---|
| REF-1 | cc-switch | UI 风格与配置管理功能对齐基准（Tauri 2 + React/TS + Tailwind + shadcn/ui，卡片式 Provider 列表、系统托盘快切、深浅主题） |
| REF-2 | echobird | 交互/视觉风格参考 |
| REF-3 | Tauri 2 官方文档 | 桌面框架技术基线 |
| REF-4 | Anthropic Claude API / Messages API | Claude 系列上游接口规范 |
| REF-5 | OpenAI 兼容 / Codex 接口 | Codex 目标工具接口规范 |

---

## 3. 整体描述

### 3.1 产品愿景

让开发者"一次配置，稳定可用"：把分散在多个厂商的 API Key 收敛到一个桌面应用中统一管理，通过本地路由代理实现主备无感切换与多模型协作，在不改变现有工具使用习惯的前提下，显著提升 AI 编码/对话链路的可用性与答案质量。

### 3.2 用户画像

| 画像 | 描述 | 核心诉求 |
|---|---|---|
| 重度 AI 编码用户 | 长期使用 Claude CLI / Codex，手上有多个厂商 Key | 主 Key 挂了不中断工作，自动切备用 Key |
| 多厂商羊毛用户 | 同时持有官方与多个第三方兼容厂商 Key | 统一管理、模型名映射、按需启用/停用 |
| 质量敏感用户 | 对答案准确性要求高，愿意多花 token | 大脑聚合，多模型协作出更优答案 |
| 轻度用户 | 只有一个 Key | 直连即可，配置越简单越好 |

### 3.3 运行环境

| 项目 | 约束 |
|---|---|
| 操作系统 | Windows 10 / 11（64 位）。首发仅 Windows |
| 运行形态 | 桌面 GUI 应用 + 常驻后台的本地路由代理服务 |
| 网络 | 需可访问各上游厂商 API（出网）；路由代理仅监听本机回环地址 |
| 依赖工具 | Claude CLI、Claude 桌面端、Codex 桌面端（由用户自行安装） |

### 3.4 设计约束（基线决策，不可更改）

| 编号 | 约束 |
|---|---|
| CON-1 | 技术栈固定为 Tauri 2（Rust 后端 + React/TypeScript 前端），UI 使用 TailwindCSS + shadcn/ui |
| CON-2 | 目标工具为 Claude CLI、Claude 桌面端、Codex 桌面端，三个全做 |
| CON-3 | 路由策略为"故障转移优先"：主 Key 失败/超时才切下一个（非负载均衡、非轮询） |
| CON-4 | 大脑聚合提供两种聚合方式可配：A 压缩汇总后交决策者；B 全量上下文交决策者 |
| CON-5 | Key 存储采用应用内加密文件（不使用明文配置、不强制依赖云端） |
| CON-6 | MVP 范围对齐 cc-switch 全功能（卡片式列表、托盘快切、深浅主题等） |
| CON-7 | UI 风格对齐 cc-switch 与 echobird |

### 3.5 假设与依赖

| 编号 | 假设/依赖 |
|---|---|
| ASM-1 | 上游厂商提供 `/models` 类接口用于拉取可用模型列表；若个别厂商不提供，需支持手动录入（见 FR-004） |
| ASM-2 | 目标工具支持将请求端点指向自定义 base_url（Claude CLI、Codex 通常支持；Claude 桌面端是否支持见 Open Questions） |
| ASM-3 | 各上游厂商接口协议兼容 Anthropic Messages 或 OpenAI Chat Completions 之一，代理可完成必要的协议适配 |
| ASM-4 | 用户对本机有写入配置文件与启动本地端口监听的权限 |
| ASM-5 | 加密密钥来源（用户口令 / 系统密钥库 DPAPI）需在设计阶段确认（见 Open Questions） |

---

## 4. 功能需求

> 编号规则：FR-xxx。每条含：描述、输入、处理、输出、优先级、验收标准。
> 功能分组：
> - 4.1 配置管理（分类/厂商/Key/模型/参数/映射）
> - 4.2 Key 无感切换（功能1）
> - 4.3 大脑聚合（功能2）
> - 4.4 路由代理服务生命周期
> - 4.5 系统托盘与主题（cc-switch 类基础功能）

### 4.1 配置管理

#### FR-001 三分类配置管理
- 描述：系统必须提供三个固定分类——Claude CLI、Claude 桌面端、Codex 桌面端。每个分类下可管理多个厂商 Key。
- 输入：用户选择分类；新增/编辑/删除厂商 Key 的操作。
- 处理：按分类隔离存储各自的 Key 列表与代理配置；分类间配置互不影响。
- 输出：卡片式 Provider 列表，展示每个 Key 的名称、启用状态、健康状态、映射模型摘要。
- 优先级：P0
- 验收标准：
  1. 三个分类均可见且可切换。
  2. 在任一分类下新增的 Key 只出现在该分类，不串到其他分类。
  3. 列表以卡片形式呈现，风格对齐 cc-switch。

#### FR-002 厂商 Key 增删改查
- 描述：用户必须能在某分类下新增、编辑、删除、复制一条厂商 Key 配置。
- 输入：Key 名称（备注）、厂商标识、base_url、API 密钥、可选的自定义请求头。
- 处理：校验必填项与 base_url 合法性；密钥写入加密存储（见 FR-018）；保存后刷新列表。
- 输出：新增/更新后的 Key 卡片；操作成功/失败提示。
- 优先级：P0
- 验收标准：
  1. 新增 Key 后在列表可见，密钥不以明文写入磁盘。
  2. 编辑保存后再次打开显示为最新值。
  3. 删除需二次确认，删除后不可在列表看到。

#### FR-003 Key 启用/停用开关
- 描述：每个 Key 必须提供"是否启用"开关。启用状态决定其是否参与直连/路由/聚合。
- 输入：用户在 Key 卡片上切换启用开关。
- 处理：更新该 Key 的启用状态；触发对该分类"启用数量"的重新评估（联动 FR-006/FR-007）。
- 输出：Key 卡片状态更新；若启用数量变化触发路由模式变更提示。
- 优先级：P0
- 验收标准：
  1. 停用的 Key 不参与任何请求转发与聚合。
  2. 启用数量从 1 变为多个时，系统提示需开启"无感切换"（见 FR-007）。

#### FR-004 模型列表拉取（/models）
- 描述：每个 Key 应支持从厂商 `/models` 接口拉取可用模型列表；对不提供该接口的厂商，应支持手动录入模型名。
- 输入：Key 的 base_url + 密钥；用户点击"拉取模型"或手动添加模型名。
- 处理：调用厂商 `/models`；成功则填充模型下拉列表并缓存；失败给出明确错误（鉴权失败/网络失败/接口不存在）并允许手动录入。
- 输出：该 Key 下的可用模型列表；拉取时间戳。
- 优先级：P0
- 验收标准：
  1. 拉取成功后模型列表可用于映射与选择。
  2. 拉取失败时不阻塞流程，可手动录入模型名。
  3. 显示上次拉取时间，可重新拉取。

#### FR-005 参数设置
- 描述：每个 Key 必须支持配置请求参数：temperature、max_tokens、base_url，及可扩展的其他参数（如 top_p、超时时间、自定义 header）。
- 输入：用户填写各参数值。
- 处理：校验取值范围（如 temperature 0–2、max_tokens 正整数）；保存到 Key 配置。**各参数的作用范围不同**（见下）。
- 输出：参数保存结果；非法值即时校验提示。
- 优先级：P0
- 作用范围（**2026-08-14 / 08-15 两次定调，改动前先读这一段**）：
  - `temperature` / `top_p`：转发时若客户端**未显式提供**则用 Key 上的值补齐；客户端给了则尊重客户端。
  - `timeout_ms`：转发的单请求超时。
  - `max_tokens`：**当前不参与任何请求，仅兼容旧配置**。两个用途已先后撤掉（见下）。

- **三类 token 预算必须分清**（历来最容易混的一处）：

  | 用途 | 谁决定 | 说明 |
  |---|---|---|
  | 客户端代理转发的输出上限 | **完全跟随客户端** | 客户端给了就原样用（跨协议只改字段名）；没给就不给。代理不补默认值、不设全局上限、不按目标 Key 二次钳制 |
  | 大脑聚合的输出预算 | **按协议与模型能力自动算** | OpenAI Chat / Responses：**不发**上限字段；Anthropic：API 必填，按「该模型 `contextWindow` − 本轮输入估算 − 安全余量」现算，工具循环**每轮重算** |
  | 大脑聚合的检索输入预算 | `brain.maxContextTokens` | 限制打包进 prompt 的**文件内容**大小，与输出长度无关 |

- 为什么代理转发不注入（2026-08-14）：本代理相对 cc-switch 的增量只有路由与自动故障转移，
  替客户端决定输出长度不在其内 —— 而 KeyEditor 新建 Key 默认带 8192，一旦注入就等于给几乎
  所有请求悄悄加了上限，用户看到长回答被截断只会去查上游中转商，永远查不到是代理加的。
- 为什么大脑聚合也不设固定上限（2026-08-15）：原实现一律用
  `key.params.max_tokens.unwrap_or(4096)`，参与者/汇总者/决策者的长回答会在 4096 token 处
  被切掉，症状是「模型答一半」而无处归因。决策者 prompt 里嵌着全部成员答案，正是最需要
  按实际输入算的场合。
- **Anthropic 的协议约束（不可绕过）**：Anthropic Messages 的 `max_tokens` 是**必填**字段，
  省略直接 HTTP 400。故 Anthropic 侧不可能既「请求合法」又「请求里没有上限」，只能取
  「能力范围内尽可能大」，取值为：

  ```text
  min(contextWindow − 本轮输入估算 − 1024 余量, 该模型最大单次输出)
  ```

  **两个钳制都不能省**：只按窗口算会在 200k 窗口 / 64k 最大输出的 Claude 4.5 上发出近 200k
  的 `max_tokens` → 官方与严格中转直接 400（**上下文窗口 ≠ 最大输出**，这是审计实测出的
  provider-break）；只按最大输出算则会在长输入时让「输入 + 输出」超窗口。
- **模型能力表**：最大输出按 Claude 家族片段匹配（`claude-*-4-5` = 64k、`claude-3-5` = 8192 等），
  用 `contains` 而非全等 —— 第三方中转普遍给模型名加前后缀，全等会把它们全判成未知。
- **数据缺失时的分级处理**（刻意不一刀切）：
  - 缺 `contextWindow`、但模型名可辨识 → **仍可用**，按该模型最大输出取值
    （真实 Claude 窗口都 ≥ 200k，输入通常远小于它，这个值实际安全）。
    这条是为了不拒掉「用户手填模型名 / 刚拉取完模型列表」这类完全可用的常见配置。
  - 模型最大输出**无法辨识** → 报可行动错误，**不猜**。此时无论填大还是填小都是赌博。
  - 输入（含余量）已占满窗口 → 报可行动错误并**不发请求**，而不是硬塞一个最小值
    让上游以 context overflow 拒绝（那既浪费额度又把本地已知的原因藏起来）。
- ⚠️ 「不发上限」不等于数学意义上的无限：仍受模型自身上下文窗口、服务商默认值与服务端策略约束。

- 验收标准：
  1. 非法参数值被拦截并提示。
  2. 客户端未给 temperature / top_p 时，转发请求携带该 Key 配置的对应值（可通过代理日志验证）。
  3. 客户端**未给**输出上限时，发往上游的请求体里**不出现**代理新增的
     `max_tokens` / `max_output_tokens` / `max_completion_tokens`（回归测试
     `key_max_tokens_is_never_injected_by_proxy` 钉住）。
  4. 客户端**给了**输出上限时原值保留：同协议原名直通，跨协议按上游协议改名，且不额外冒出同义字段。
  5. 同一请求故障转移到任何候选 Key，输出上限都不随 Key 的 `max_tokens` 变化。
  6. 大脑聚合打 OpenAI 上游时，**每一轮**请求体都不含三个上限字段中的任何一个
     （`openai_aggregation_requests_carry_no_output_cap` 钉住）。
  7. 大脑聚合打 Anthropic 上游时，请求体带 `max_tokens`，其值 **≤ 该模型最大输出**、
     **不等于 4096**，且工具循环历史增长后**不得变大**
     （`anthropic_aggregation_uses_dynamic_cap_not_the_old_default` 钉住）。
  8. Anthropic Key 缺 `contextWindow` 但模型名可辨识时，仍按该模型最大输出正常参与聚合
     （`anthropic_member_without_context_window_uses_known_model_cap` 钉住）。
  9. 模型最大输出无法辨识、或输入已占满窗口时，报可行动错误且不发上游请求
     （`anthropic_without_known_max_output_refuses_instead_of_guessing`、
     `anthropic_refuses_when_input_exhausts_context` 钉住）。
  10. **跨协议**（OpenAI 客户端 → Anthropic Key）客户端省略上限时，转换层不得凭空造 4096；
      由代理按同一套能力计算补齐（`cross_protocol_anthropic_budget_uses_model_cap_not_4096`、
      `o2a_omitted_cap_stays_omitted_for_key_aware_proxy_to_fill` 钉住）。
      客户端显式给的值优先，不被能力计算覆盖（`cross_protocol_anthropic_budget_preserves_client_cap`）。
  11. Key 上残留的旧 `max_tokens` 值对以上任何请求体都无影响。
  12. 图片 base64 是传输编码，**不得**按文本 token 计入输入估算
      （`image_base64_is_replaced_by_bounded_vision_estimate` 钉住）；
      CJK 按每字符 ≥1 token 取上界，不得低估
      （`token_estimate_handles_cjk_without_underestimating` 钉住）。

#### FR-006 模型映射
- 描述：每个 Key 必须支持"模型映射"——把厂商真实模型名映射为工具期望的模型名。例如 `GLM5.1 → opus-4-7`、`GLM5.2 → opus-4-8`。
- 输入：厂商真实模型名（来自 FR-004）+ 工具期望模型名。
- 处理：维护该 Key 的映射表；路由/聚合时，将下游工具请求的"期望模型名"翻译为该 Key 的"真实模型名"再发往厂商。
- 输出：映射表；映射生效后的转发日志。
- 优先级：P0
- 验收标准：
  1. 下游工具请求 `opus-4-7`，命中 GLM 厂商时实际发往厂商的是 `GLM5.1`。
  2. 一个期望模型名可在不同 Key 下映射到不同真实模型名。

#### FR-006a 模型映射兜底提示
- 描述：当多个启用 Key 之间"期望模型名"覆盖不一致（某 Key 缺少对应真实模型）时，系统必须提示用户补充模型映射，避免切换后出现"模型不存在"。
- 输入：当前分类下所有启用 Key 的模型映射表 + 用户选定的目标期望模型集合。
- 处理：计算每个目标期望模型在各启用 Key 上是否有可用映射/真实模型；对缺失项高亮并给出补映射入口。
- 输出：缺口清单（哪个 Key 缺哪个模型）+ 修复引导。
- 优先级：P0
- 验收标准：
  1. 存在缺口时保存/启用会被拦截或强提示。
  2. 补齐映射后缺口清单清空。

### 4.2 Key 无感切换（功能1）

#### FR-007 路由模式自动判定（单 Key 直连 vs 多 Key 无感切换）
- 描述：系统必须根据某分类下"启用 Key 数量"自动决定路由模式：
  - 仅 1 个启用 Key：不开启自动切换，直接直连该 Key（可不启动代理，或代理透明直连）。
  - 多个启用 Key：必须开启"Key 无感切换"，由 SynaRoute 生成本地路由代理端点，供目标工具连接。
- 输入：分类下启用 Key 集合。
- 处理：统计启用数量；单 Key 走直连配置，多 Key 走代理路由配置；模式变化时更新目标工具应使用的连接信息。
- 输出：当前分类的路由模式标识 + 目标工具应使用的端点/配置。
- 优先级：P0
- 验收标准：
  1. 启用 1 个 Key 时不生成/不依赖代理即可工作（或代理透明转发单一上游）。
  2. 启用 ≥2 个 Key 时必须生成本地代理端点，且无感切换生效。

#### FR-008 本地路由代理生成与工具接入配置
- 描述：多 Key 场景下，系统必须在本机生成路由代理端点（本地回环地址 + 端口），并给出/写入目标工具的接入配置。
- 输入：分类、启用 Key 集合、目标工具类型。
- 处理：分配本机端口，启动代理；生成对应工具的接入配置（如环境变量/配置文件中的 base_url 指向本地代理）；提供"一键写入"或"复制配置"。
- 输出：代理端点地址、目标工具接入配置文本、写入结果。
- 优先级：P0
- 验收标准：
  1. 目标工具按生成的配置连接后，请求经由本地代理转发到上游。
  2. 端口冲突时能自动改用可用端口并更新配置。

#### FR-009 故障转移路由（主备切换）
- 描述：代理必须按"故障转移优先"策略工作：优先使用主 Key；当主 Key 请求失败或超时，自动切换到下一个可用 Key 重试，直到成功或全部耗尽。
- 输入：下游工具请求；启用 Key 的优先级顺序。
- 处理：按优先级尝试；失败判定条件包含网络错误、鉴权失败、5xx、限流（429）、超时；触发切换时应用目标 Key 的参数与模型映射。
- 输出：最终成功响应或"全部 Key 不可用"错误；切换过程写入日志。
- 优先级：P0
- 验收标准：
  1. 主 Key 人为置为失败时，请求在无用户干预下由下一个 Key 成功返回。
  2. 切换过程对下游工具透明（工具侧无需重试即拿到结果）。
  3. 全部 Key 失败时返回清晰错误信息。

#### FR-010 Key 优先级排序
- 描述：用户应能设定同一分类下启用 Key 的优先级顺序（决定谁是主、谁是备）。
- 输入：用户拖拽/上下移动 Key 顺序。
- 处理：持久化顺序；故障转移按此顺序尝试。
- 输出：更新后的顺序；即时生效。
- 优先级：P1
- 验收标准：调整顺序后，主 Key 变为排在最前的启用 Key。

#### FR-011 切换前健康检查
- 描述：系统必须在切换（及必要时启动时）对候选 Key 做可用性检查，避免切到不可用 Key。
- 输入：候选 Key 的 base_url + 密钥（+ 轻量探测请求或 `/models`）。
- 处理：发起轻量探测；标记每个 Key 的健康状态（可用/不可用/未知）与最近检查时间；不健康的 Key 在故障转移时被跳过。
- 输出：各 Key 健康状态徽标；检查耗时。
- 优先级：P0
- 验收标准：
  1. 不可用 Key 被标红且在故障转移中被跳过。
  2. 健康检查有超时上限（见 NFR-002），不长时间阻塞。

#### FR-012 熔断与恢复
- 描述：对连续失败达到阈值的 Key，应临时熔断（一段时间内不再作为候选），到期后半开重试恢复。
- 输入：Key 的连续失败计数、熔断阈值与冷却时长。
- 处理：达到阈值进入熔断；冷却期过后半开探测，成功则恢复，失败则继续熔断。
- 输出：Key 的熔断状态与剩余冷却时间。
- 优先级：P1
- 验收标准：连续失败的 Key 在冷却期内不被选用；冷却后可自动恢复。

### 4.3 大脑聚合（功能2）

#### FR-013 大脑聚合开关与开启条件
- 描述：大脑聚合应可在功能1基础上开启，也可按条件单独开启。用户可为某分类开启/关闭该能力。
- 输入：用户开关；开启条件（如仅对某些请求/某工具启用——具体条件粒度见 Open Questions）。
- 处理：开启后请求走聚合流程；关闭则走普通路由（FR-009）。
- 输出：聚合开关状态。
- 优先级：P1
- 验收标准：开关关闭时行为等同普通故障转移路由；开启时进入聚合流程。

#### FR-014 聚合成员配置
- 描述：用户必须能配置参与聚合的成员——即哪些 Key + 哪些模型参与并行解答，并指定一个主 Key 与一个最终决策者。
- 输入：成员集合（Key+模型）、主 Key、最终决策者（Key+模型）。
- 处理：保存成员配置；校验决策者与成员的有效性。
- 输出：成员列表、主 Key、决策者标识。
- 优先级：P1
- 验收标准：
  1. 至少 1 个成员 + 1 个决策者可配置并保存。
  2. 未配置决策者时不允许开启聚合。

#### FR-015 不可用模型跳过
- 描述：聚合执行前/中，若某成员配置的模型不可用，则本次思考跳过该成员，避免长时间无返回。
- 输入：成员模型的健康/可用性状态。
- 处理：对不可用成员直接跳过并记录；若可用成员数为 0，则回退到普通路由或返回主 Key 结果（回退策略见 Open Questions）。
- 输出：本次实际参与成员清单 + 跳过清单。
- 优先级：P1
- 验收标准：不可用成员不拖慢整体；被跳过项在日志/结果元信息中可见。

#### FR-016 并行解答 + 聚合 + 最终决策
- 描述：聚合请求流程必须为：多个成员模型并行解答 → 收集结果 → 按所选聚合方式处理 → 交最终决策者产出最终答案。
- 输入：下游工具的原始请求；成员集合；聚合方式（A/B）；决策者。
- 处理：
  1. 并行向各可用成员发起请求（含各自的映射与参数）。
  2. 汇集结果，按超时上限收敛（见 NFR-001）。
  3. 聚合：方式 A 先压缩汇总各结果（减少上下文与 token）后交决策者；方式 B 将全量结果交决策者。
  4. 决策者综合产出最终答案，作为响应返回下游工具。
- 输出：最终答案；可选的过程元信息（参与成员、聚合方式、耗时）。
- 优先级：P1
- 验收标准：
  1. 可观测到多个成员被并行调用（非串行）。
  2. 方式 A 与方式 B 均能产出最终答案，且 A 的交给决策者的输入体量小于 B。
  3. 决策者输出即为下游工具收到的最终内容。

#### FR-017 聚合方式可配（压缩汇总 / 全量上下文）
- 描述：系统必须提供两种聚合方式供用户选择：A 压缩汇总后交决策者；B 全量上下文交决策者。
- 输入：用户选择的聚合方式。
- 处理：方式 A 需对成员结果做汇总/裁剪以降低 token；方式 B 保留完整结果。
- 输出：所选方式；对应的决策者输入。
- 优先级：P1
- 验收标准：切换方式后，决策者输入构造相应变化，且可在日志中区分。

### 4.4 路由代理服务生命周期

#### FR-018 Key 加密存储
- 描述：所有密钥必须以应用内加密文件形式存储，不得明文落盘。
- 输入：待保存的密钥与敏感配置。
- 处理：使用加密算法（如 AES-GCM）加密后写入本地文件；密钥/口令来源见 Open Questions（用户口令派生 / 系统 DPAPI）。
- 输出：加密存储文件；应用内解密后使用，界面默认掩码显示。
- 优先级：P0
- 验收标准：
  1. 配置文件中不含明文密钥。
  2. 界面默认掩码，提供"显示"临时查看。

#### FR-019 代理服务启动/停止/状态
- 描述：系统必须管理本地代理服务的生命周期：随需启动、可手动停止、显示运行状态与监听端口。
- 输入：用户操作或分类路由模式变化（FR-007）。
- 处理：启动/停止本机监听；健康自检；端口占用处理；应用退出时优雅关闭。
- 输出：服务状态（运行中/已停止/端口/错误）。
- 优先级：P0
- 验收标准：
  1. 界面显示当前代理运行状态与端口。
  2. 停止后目标工具请求不再被转发；重启后恢复。

#### FR-020 请求日志与可观测性
- 描述：系统应记录路由/聚合的关键事件：使用了哪个 Key、是否发生切换、健康检查结果、聚合参与成员与耗时、错误原因。
- 输入：请求处理过程中的事件。
- 处理：结构化记录，支持在界面查看最近事件；敏感信息（密钥）脱敏。
- 输出：可查看的事件/日志视图。
- 优先级：P1
- 验收标准：
  1. 发生一次故障转移后，日志能定位是从哪个 Key 切到哪个 Key 及原因。
  2. 日志不含明文密钥。

#### FR-021 配置导入/导出
- 描述：用户应能导出/导入配置以便备份或迁移；导出时密钥需保持加密或提供是否包含密钥的选项。
- 输入：导出/导入操作。
- 处理：序列化配置；导入时校验版本与完整性。
- 优先级：P2
- 验收标准：导出文件可在同版本重新导入并还原配置（含或不含密钥按选项）。

### 4.5 系统托盘与主题（cc-switch 类基础功能）

#### FR-022 系统托盘常驻与快速切换
- 描述：系统必须提供系统托盘常驻，支持从托盘菜单快速切换当前 Key/Provider、开关代理、显示/隐藏主窗口、退出。
- 输入：托盘菜单点击。
- 处理：执行相应操作并即时生效；托盘图标反映代理运行状态。
- 输出：托盘菜单与状态图标。
- 优先级：P0
- 验收标准：
  1. 关闭主窗口应用仍驻留托盘。
  2. 从托盘可完成主 Key/Provider 快切与代理开关。

#### FR-023 深浅主题切换
- 描述：系统必须支持深色/浅色主题，且可跟随系统。
- 输入：用户选择主题或系统主题变化。
- 处理：切换主题并持久化偏好。
- 优先级：P0
- 验收标准：切换后 UI 立即变化，重启后保持。

#### FR-024 卡片式 Provider 管理界面
- 描述：Provider/Key 列表必须以卡片式呈现，风格对齐 cc-switch 与 echobird，展示名称、启用状态、健康状态、映射模型摘要等，并提供快捷操作。
- 输入：配置数据。
- 处理：渲染卡片列表，支持编辑/启用/删除/排序入口。
- 优先级：P0
- 验收标准：界面观感与交互对齐 cc-switch 卡片式列表。

#### FR-025 开机自启动（可选）
- 描述：系统应支持配置开机自启动并最小化到托盘。
- 输入：用户开关。
- 处理：写入/移除系统自启动项。
- 优先级：P2
- 验收标准：开启后重启系统应用自动驻留托盘。

#### FR-026 聚合参与者按需检索（只读工具调用）
- 描述：大脑聚合的参与者应可在回答过程中主动调用一组**只读**工具按需取上下文，而不是仅依赖
  FR-016 之前一次性注入的前置检索结果（前置检索靠 prompt 关键词猜哪些文件相关，猜错即白填
  token、猜漏则模型盲答）。
- 输入：分类级开关 `toolsEnabled`（默认**关**）、轮数上限 `maxToolRounds`（默认 6，运行时夹在 2~12）。
- 处理：
  1. 工具集固定四个：`read_file`（可带行区间）、`grep`（正则，ripgrep 不可用时降级为字面量匹配
     并在结果里明示）、`list_dir`、`codegraph_query`（未安装或项目未建索引时**不声明**该工具）。
  2. 每轮把模型请求的工具调用在本地执行，结果按各协议形状回填（Anthropic `tool_result` 打包进
     一条 user 消息；OpenAI 每条一个 `role:"tool"` 消息），进入下一轮。
  3. 到轮数上限或探索预算（整轮成员预算的 70%）用尽时，追加一条「不要再调用工具、直接出结论」
     的指示再发一轮；该轮仍须声明 tools（Anthropic 规定历史中出现 tool_use/tool_result 时必须带 tools）。
  4. 单次工具结果截断到 8000 字符并告知模型如何缩小范围。
- 安全约束（**恒定生效、无开关**）：
  - 只读。不提供写文件与执行命令的工具（落盘由客户端自行执行，与既有设计一致）。
  - 路径三道防线：① 拒 `..`/绝对路径/盘符/UNC；② canonicalize 后必须仍在工作目录内（堵符号链接
    与 junction 逃逸）；③ 凭据类文件（`.env`/密钥/证书/密码库）一律拒读，且按「模型给的名字」与
    「解析链接后的真实落点」各判一次。
  - `grep` 命中结果逐条过敏感文件过滤后才回给模型（ripgrep 搜所有文件，`.env` 命中会直接出现在
    stdout 里；工具结果会进上游请求体，等于外发）。
- 优先级：P2
- 验收标准：
  1. 开关关闭时行为与开启前完全一致（单轮、请求体不含 tools 字段）。
  2. 开启后提出「需要读多个文件才能回答」的问题，运行日志出现 `read_file`/`grep` 调用记录，
     且答案引用了具体文件内容。
  3. 让模型去读 `.env` → 被拒且日志留痕；模型据拒绝原因改读其他文件、成员不失败。
  4. 模型持续要求调用工具时，在轮数上限处停止并给出结论，不无限循环。

#### FR-027 聚合图片输入（多模态）
- 描述：客户端调用 `synaroute_ai` 时应可附带图片，使「这个报错截图是什么问题」这类任务可用。
- 输入：MCP 工具参数 `images`（字符串数组，**相对 `cwd` 的路径**）。桌面端 UI 不提供上传入口。
- 处理：按扩展名判定 `media_type`，base64 编码后按各协议形状拼进 user content
  （Anthropic `{type:"image",source:{type:"base64",…}}`；OpenAI `{type:"image_url", url:"data:…"}`），
  图片块置于文本块之前。
- 约束与失败口径：
  - 最多 4 张、单张 ≤ 5MB、仅 `png/jpg/jpeg/gif/webp`（两家协议的交集）；路径同样过 FR-026 的三道防线。
  - 任何一项不满足即**整次调用报错**并说明原因，**不静默丢弃**——静默丢会让用户拿到一个看起来
    正常、实则没看图的答案。
  - 传了 `images` 但未提供工作目录时报错要求同时传 `cwd`。
  - 成员模型不支持图片时上游回 4xx，失败原因里显式附「该模型可能不支持图片输入」的提示
    （判据：本次带了图 且 错误含 `HTTP 4xx`）。
- 优先级：P2
- 验收标准：传一张截图提问，答案应描述图片内容；传 6MB 图应报「超过单张 5MB 上限」；
  传 5 张应报张数超限；传 `.bmp` 应报格式不支持。

#### FR-028 用量统计 / 健康告警 / 请求路径可视化
> 本条在 v0.1.13–0.1.16 分批实现，当时只记在 docs/14 的进度表里、漏登在此。
> 补登以免 FR 编号出现空档被误读为「跳号」。实现细节与审计结论见
> [docs/08 A4 轮](08-代码审计报告.md)。
- 描述：三件让「代理到底在干什么、花了多少」可见的事。
  - **用量统计**：按「分类 × Key」聚合 token 消耗，独立面板呈现。
  - **健康告警**：Key 熔断 / 恢复时发系统通知 + 分类页常驻警告条。
  - **路径可视化**：运行日志折叠态直接显示「这次走了哪个 Key」。
- 处理要点（都是踩过的坑，改动前先读）：
  - 用量总量走**独立累加器**，不从 500 条事件环现算 —— 环满后现算值会停止增长，
    一个「累计用量」面板越用数字越小，用户据此估额度会严重低估。
  - 累加器**跨重启保留**（`usage.json`，每 60s 且仅在有新消耗时落盘，退出补落一次）。
  - 熔断跃迁判据按「窗口是否还活着」算，不能用 `breaker_until.is_some()` ——
    窗口自然到期后该字段仍有残留，会导致**第二次及以后的熔断永远不发通知**。
  - Key 名由后端按 id 回填进事件（`key_name`），前端不靠解析 detail 字符串。
- 约束：只统计 token，**不换算金额**（各中转商单价差异巨大，本应用无从得知）。
- 优先级：P1
- 验收标准：连续请求超过事件环上限后累计值仍在涨；重启后累计不归零且「统计起点」不变；
  Key 连续失败应收到熔断通知、恢复后收到恢复通知，且第二轮熔断同样有通知。

#### FR-029 代理启用状态的记忆与自动恢复
- 描述：上次退出前处于启用状态的代理分类，下次启动应用（含开机自启动）应自动恢复启用，
  用户不必每次手点「启动」。绝大多数人的用法是「一直开着」，每次手点是纯粹的重复劳动。
  MCP 早已具备同等能力（`mcp_enabled` + 启动时的门），本条把代理补齐到同一水平。
- 输入：无用户输入。状态由系统自行记录。
- 处理：
  - **记录**：周期性（60s，搭用量落盘那条后台线程）+ **退出时**各采样一次
    「当前哪些分类的代理在跑」，写入 `settings.proxyRunningCategories`。
    采样的是 `ProxyManager` 的**真实运行态**，不是用户点击意图 —— 启停的意图点有四处以上
    （命令 / 接入 / 托盘 / 首启向导 / 改端口），逐点埋标记漏一处就是静默失效。
  - **恢复**：启动时逐个走与界面按钮、托盘**完全相同**的入口
    （`service::apply_tool_config` = 监听端口 + 写客户端工具配置）。
    绝不能只 `proxy.start()`：那样客户端读到的仍是官方端点，会出现
    「界面显示已启动、客户端根本没走代理」。
- 约束与失败口径：
  - 幂等落盘：与上次快照相同（顺序无关）则**一个字节都不写**。空闲的应用零磁盘写入
    —— 否则等于「开着不用也每分钟重写一次含全部 Key 的 config.json」。
  - 逐个独立：一个分类恢复失败（端口被占、配置文件被占用）**不得阻断**其余分类。
  - 失败必须可见：记 error 事件 + 发系统通知。此处静默失效最坏 ——
    用户以为在跑、实际没跑，客户端报错却找不到原因。
  - 全新安装无任何记忆，不得凭空自启动代理。
  - **已知窗口**：崩溃且距上次状态变更 < 60s 时，这次变更会丢。代理是可重建的运行态
    （再点一次即可），故接受；不像用量是纯累加历史值。
- 不做：不提供「是否恢复上次状态」开关 —— 用户要的就是别每次手点，加开关等于把要解决的
  问题又还给用户。想下次不自启，退出前点一次「停止」即可（语义自解释）。
- 优先级：P1
- 验收标准：启动某分类代理 → 退出应用 → 重新打开，该分类应自动在跑，且客户端配置里的
  端口与实际监听端口一致；退出前先「停止」再退出，则下次不应自启动；
  全新安装首次启动不应有任何代理自动启动；恢复失败时应有系统通知与日志，界面不得显示为已启动。

---

## 5. 非功能需求

> 编号规则：NFR-xxx。注：以下数值为建议目标值，标注 [待确认] 者需产品与用户对齐（见第 7 节）。

### 5.1 性能

#### NFR-001 聚合响应时间收敛
- 需求：大脑聚合必须设总超时上限，避免长时间无返回。建议单次聚合总耗时上限 [待确认，建议默认 60s] 内收敛；超时未返回的成员按跳过处理，用已到结果继续聚合。
- 优先级：P0
- 验收：模拟某成员超时，整体仍在上限内返回最终答案。

#### NFR-002 健康检查耗时
- 需求：单次 Key 健康检查超时上限建议 [待确认，建议 3–5s]；不得因单个 Key 探测拖慢整体切换。
- 优先级：P0
- 验收：不可用 Key 探测在上限内失败并被跳过。

#### NFR-003 故障转移切换延迟
- 需求：主 Key 判定失败到切至下一个 Key 的额外开销应尽量低（建议除上游本身耗时外新增 < 200ms 决策开销）；对下游工具应表现为一次正常（稍慢）请求。
- 优先级：P0
- 验收：日志显示切换决策开销在目标范围内。

#### NFR-004 代理转发开销
- 需求：单 Key/普通转发场景下，代理引入的额外延迟应尽量小（建议 < 50ms，不含上游耗时）。
- 优先级：P1

#### NFR-005 资源占用
- 需求：常驻托盘 + 代理的空闲内存与 CPU 占用应保持在桌面软件合理范围（建议空闲内存 [待确认] ，空闲 CPU 近 0）。
- 优先级：P1

### 5.2 安全

#### NFR-006 密钥加密与脱敏
- 需求：密钥必须加密存储（FR-018），界面默认掩码，日志脱敏，剪贴板复制敏感信息应有提示。
- 优先级：P0
- 验收：全链路（磁盘/界面/日志）均无明文密钥。

#### NFR-007 本地监听最小暴露
- 需求：路由代理默认仅监听本机回环地址（127.0.0.1），不对外网暴露；如需局域网访问必须显式开启并告警。
- 优先级：P0
- 验收：默认端口仅本机可访问。

#### NFR-008 传输安全
- 需求：与上游厂商通信必须使用 HTTPS/TLS，校验证书。
- 优先级：P0

### 5.3 可靠性

#### NFR-009 故障转移正确性
- 需求：在任一/多个 Key 故障时，只要存在至少一个可用 Key，请求就应成功返回；全部失败时给出明确错误。
- 优先级：P0

#### NFR-010 熔断保护
- 需求：连续失败的 Key 应被熔断，避免反复重试拖慢整体（FR-012）。
- 优先级：P1

#### NFR-011 崩溃恢复与配置一致性
- 需求：应用异常退出后重启，配置不丢失、不损坏；写配置应具备原子性/备份，防止半写损坏。
- 优先级：P0

### 5.4 可用性

#### NFR-012 上手成本
- 需求：新用户应能在 [待确认，建议 5 分钟] 内完成"新增一个 Key 并让目标工具连通"。
- 优先级：P1

#### NFR-013 错误可读性
- 需求：所有失败（鉴权失败、网络、超时、模型不存在、端口冲突等）必须给出可读的中文错误与建议动作。
- 优先级：P0

### 5.5 兼容性

#### NFR-014 操作系统
- 需求：支持 Windows 10 / 11（64 位）。首发仅 Windows。
- 优先级：P0

#### NFR-015 目标工具接入兼容
- 需求：生成的接入配置必须能被 Claude CLI、Codex 桌面端识别接入；Claude 桌面端接入方式取决于其是否支持自定义端点（见 Open Questions）。
- 优先级：P0

#### NFR-016 上游协议兼容
- 需求：代理应兼容 Anthropic Messages 与 OpenAI Chat Completions 两类主流协议，并在故障转移/聚合时完成必要的请求/响应适配（协议转换范围见 Open Questions）。
- 优先级：P1

---

## 6. 关键用例

> 编号规则：UC-xxx。用步骤流程描述。

### UC-001 单 Key 直连
- 参与者：用户、目标工具、SynaRoute。
- 前置：某分类下仅 1 个 Key 启用。
- 主流程：
  1. 用户在 Claude CLI 分类下新增并启用 1 个 Key。
  2. SynaRoute 判定为单 Key 直连模式（FR-007），不开启自动切换。
  3. 系统给出直连配置（或透明直连），用户接入目标工具。
  4. 目标工具正常请求，直达该上游 Key。
- 后置：无代理路由参与（或代理透明转发）。

### UC-002 多 Key 无感切换（含模型映射，示例场景）
- 参与者：用户、Claude CLI、SynaRoute、厂商1/2/3。
- 前置：Claude CLI 分类下配置三家厂商：
  - 厂商1：opus-4-6 / opus-4-7 / opus-4-8
  - 厂商2：opus-4-6 / opus-4-7 / opus-4-8 / fable-5
  - 厂商3：GLM5.1（映射 opus-4-7）/ GLM5.2（映射 opus-4-8）
- 主流程：
  1. 用户为三家厂商各配置 Key，拉取模型（FR-004），为厂商3配置模型映射 GLM5.1→opus-4-7、GLM5.2→opus-4-8（FR-006）。
  2. 用户最终选定要使用的期望模型：opus-4-7、opus-4-8。
  3. 系统做模型映射兜底检查（FR-006a）：确认三家在 opus-4-7 与 opus-4-8 上都有真实模型可用（厂商1/2直接命中、厂商3走映射）；如有缺口则提示补映射。
  4. 用户启用多个 Key，系统判定为多 Key 无感切换（FR-007），生成本地路由代理端点并给出 Claude CLI 接入配置（FR-008），用户一键写入/复制到 Claude CLI。
  5. 设置 Key 优先级：厂商1为主（FR-010）。
  6. Claude CLI 发起对 opus-4-7 的请求，经代理：
     - 主 Key（厂商1）：直接以 opus-4-7 请求。
     - 若厂商1失败/超时，故障转移到厂商2（opus-4-7），仍失败则到厂商3（映射为 GLM5.1）。切换前对候选做健康检查（FR-011），不可用者跳过。
  7. 请求由第一个成功的 Key 返回，过程对 Claude CLI 透明。
- 异常流程：
  - 全部 Key 不可用：返回明确错误（NFR-009）。
  - 某期望模型在某 Key 无映射：切换时跳过该 Key，并在配置阶段已被兜底提示（FR-006a）。
- 后置：日志记录使用的 Key、切换过程与原因（FR-020）。

### UC-003 切换前健康检查
- 主流程：
  1. 代理准备把请求发给主 Key。
  2. 触发健康检查（或使用最近的健康状态）；主 Key 判定不可用。
  3. 代理跳过主 Key，选下一个健康 Key（NFR-002 内完成探测）。
  4. 请求成功返回。
- 后置：不可用 Key 标红并进入熔断计数（FR-012）。

### UC-004 大脑聚合（多模型协作）
- 参与者：用户、目标工具、SynaRoute、成员模型若干、决策者。
- 前置：用户开启大脑聚合（FR-013），配置成员=（厂商1/opus-4-8、厂商2/opus-4-7、厂商3/GLM5.2），主 Key=厂商1，最终决策者=厂商2/opus-4-8，聚合方式=A 压缩汇总（FR-014、FR-017）。
- 主流程：
  1. 目标工具发起一次请求。
  2. SynaRoute 检查各成员模型可用性，跳过不可用成员（FR-015）。
  3. 向可用成员并行发起解答请求（FR-016），在总超时内收敛（NFR-001）。
  4. 按方式 A 压缩汇总各成员结果，降低交给决策者的上下文与 token。
  5. 将汇总交最终决策者（厂商2/opus-4-8）思考，产出最终答案。
  6. 最终答案作为响应返回目标工具。
- 异常流程：
  - 所有成员不可用：按回退策略处理（回退普通路由/返回主 Key 结果，策略见 Open Questions），不长时间无返回。
  - 决策者不可用：给出错误或回退（策略见 Open Questions）。
- 后置：记录参与成员、被跳过成员、聚合方式与耗时（FR-020）。

### UC-005 系统托盘快速切换
- 主流程：
  1. 用户关闭主窗口，应用驻留托盘（FR-022）。
  2. 用户右键托盘菜单，切换当前主 Key/Provider 或开关代理。
  3. 变更即时生效，托盘图标反映代理状态。

### UC-006 首次配置目标工具连通
- 主流程：
  1. 用户新增 Key（FR-002），拉取模型（FR-004），设置参数（FR-005）。
  2. 启用 Key，系统生成接入配置（FR-008）。
  3. 用户一键写入/复制到目标工具。
  4. 目标工具发起请求验证连通；界面显示代理状态与最近事件（FR-019、FR-020）。
- 后置：目标工具通过 SynaRoute 正常工作（NFR-012 建议 5 分钟内完成）。

---

## 7. 待确认问题清单（Open Questions）

> 本节为需求草案中仍模糊、需产品经理进一步与用户/技术确认的点。按优先级排序，前列为最建议优先确认。

| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| OQ-1 | **Claude 桌面端是否支持自定义端点/base_url？** 若不支持，SynaRoute 无法通过"改端点"接入 Claude 桌面端，需替代方案（如代理系统/进程级网络劫持、插件、或该分类降级为仅管理不路由）。 | 直接决定"Claude 桌面端"这一分类的路由能否落地（CON-2 关键风险） | 优先技术验证 Claude 桌面端的接入方式；若不支持自定义端点，明确该分类的降级范围 |
| OQ-2 | **上游协议转换范围（Anthropic ↔ OpenAI/Codex）？** 故障转移与聚合可能跨协议（Claude 系与 Codex/OpenAI 系模型混用），需明确代理要做多深的请求/响应/流式/工具调用（function/tool call）转换。 | 决定 NFR-016 与聚合成员可跨协议混用的可行性与工作量 | 明确 MVP 是否允许跨协议成员；若允许，界定转换字段范围（含流式 SSE、tool_use、多模态） |
| OQ-3 | **大脑聚合"聚合"由谁执行、方式 A 的压缩汇总如何实现？** 是本地规则/模板拼接，还是额外调用一个"汇总模型"（会产生额外 token/延迟/成本）？ | 决定聚合成本、延迟与 NFR-001 达成方式 | 明确方式 A 的汇总是否消耗一次模型调用；若是，是否复用决策者或指定汇总模型 |
| OQ-4 | **聚合并行成员数量上限？** 并行请求会放大 token 成本与限流风险，需设默认与上限。 | 成本、限流、NFR-001 | 建议设默认并发上限（如 3–5）并可配置；确认默认值 |
| OQ-5 | **健康检查频率与时机？** 是每次切换前实时探测、定时后台探测并缓存、还是二者结合？探测用轻量请求还是 `/models`？ | 影响 NFR-002、切换延迟、上游调用成本与限流 | 建议后台定时 + 缓存 + 切换前判定，探测方式与频率（如每 30–60s）需确认 |
| OQ-6 | **成本/用量统计是否纳入 MVP？** 多 Key/聚合会显著影响 token 成本，用户可能需要用量与成本视图。 | 决定是否新增计量/报表模块（当前范围外） | 建议 MVP 至少记录 token/调用次数原始数据，报表可延后；确认是否要 |
| OQ-7 | **加密密钥来源？** 应用内加密文件的密钥由用户主口令派生，还是使用 Windows DPAPI/系统密钥库自动保护？ | 决定 FR-018/NFR-006 的安全强度与用户体验（是否每次输入口令） | 建议默认 DPAPI（免口令）+ 可选主口令增强；确认取舍 |
| OQ-8 | **聚合/成员全部不可用或决策者不可用时的回退策略？** 回退到普通故障转移路由、返回主 Key 结果、还是直接报错？ | 影响 FR-015/UC-004 异常流程的确定性 | 建议默认回退到普通路由，确认是否可接受 |
| OQ-9 | **"故障"判定的精确条件？** 哪些状态码/错误算失败并触发切换（429 限流是否等待重试还是立刻切换？部分成功的流式响应如何处理？）。 | 影响 FR-009 正确性与用户体验 | 需列出明确的失败判定表与 429/超时/流式中断的处理策略 |
| OQ-10 | **大脑聚合的"按条件单独开启"中的条件粒度？** 是按工具、按模型、按请求特征（如长度/是否代码）还是全局开关？ | 影响 FR-013 交互与实现复杂度 | 建议 MVP 先做分类级全局开关，条件化延后；确认 |
| OQ-11 | **模型映射是否需要"期望模型名"的全局字典？** 多 Key 共享期望模型命名空间，是否需要集中管理期望模型名以保证一致。 | 影响 FR-006/FR-006a 的一致性体验 | 建议在分类级维护期望模型名集合；确认 |
| OQ-12 | **单 Key 场景是否仍启动代理？** 直连是"完全不经代理"还是"代理透明转发"？影响接入配置是否需要在 Key 数量变化时改动。 | 影响 FR-007/FR-008 的接入切换体验 | 建议始终经代理（透明直连），使端点稳定、Key 增减不需改工具配置；确认 |
| OQ-13 | **多模态与工具调用（tool use）在聚合下如何合并？** 不同成员可能返回工具调用或图片，决策者如何整合。 | 影响聚合可用场景 | 建议 MVP 聚合仅支持纯文本响应，多模态/工具调用延后；确认 |
| OQ-14 | **是否需要请求级路由/映射覆盖？** 例如对特定模型强制走某厂商。 | 影响路由灵活度 | 建议延后；确认是否 MVP 需要 |

---

## 8. 验收标准汇总

> 以下为 MVP 发布的关键验收门槛（对应 P0 需求）。全部通过方可视为 MVP 可发布。

| 验收项 | 关联需求 | 通过标准 |
|---|---|---|
| A1 三分类配置隔离 | FR-001 | 三分类可见可切，配置互不串扰 |
| A2 Key 增删改查 + 加密 | FR-002、FR-018、NFR-006 | 增删改查可用；磁盘/界面/日志无明文密钥 |
| A3 启用开关联动路由模式 | FR-003、FR-007 | 1 个启用=直连；≥2 个启用=生成代理并无感切换 |
| A4 模型拉取与手动录入 | FR-004 | 拉取成功可用；失败可手动录入不阻塞 |
| A5 参数生效 | FR-005 | 转发请求携带配置的 temperature / top_p；输出上限只跟随客户端原请求，代理不注入 |
| A6 模型映射生效 + 兜底 | FR-006、FR-006a | 期望模型正确翻译为真实模型；缺口被拦截/提示 |
| A7 代理生成与工具接入 | FR-008、FR-019、NFR-015 | 目标工具按配置连通，请求经代理转发 |
| A8 故障转移正确性 | FR-009、NFR-009 | 主 Key 失败时无干预自动切换成功；全失败明确报错 |
| A9 切换前健康检查 | FR-011、NFR-002 | 不可用 Key 被跳过，探测在超时上限内 |
| A10 本地最小暴露 + TLS | NFR-007、NFR-008 | 默认仅本机可访问；上游走 TLS |
| A11 托盘常驻与快切 | FR-022 | 关窗驻留托盘，可从托盘快切与开关代理 |
| A12 深浅主题 | FR-023 | 主题切换即时生效并持久化 |
| A13 卡片式界面风格 | FR-024 | 观感/交互对齐 cc-switch |
| A14 崩溃恢复与配置一致性 | NFR-011 | 异常退出重启后配置不丢不坏 |
| A15 错误可读性 | NFR-013 | 各类失败给出可读中文错误与建议 |
| A16 大脑聚合基础流程（若纳入 P0/P1） | FR-013–FR-017、NFR-001 | 并行解答→聚合（A/B）→决策者产出最终答案，在总超时内收敛；不可用成员被跳过 |

> 说明：大脑聚合整体标注为 P1，若产品决定纳入 MVP 首版，则 A16 视为发布门槛之一；否则作为紧邻的下一迭代验收项。

---

（文档结束｜版本 v0.1｜2026-07-15｜状态：待评审）
