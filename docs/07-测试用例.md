# SynaRoute 测试用例清单

本清单覆盖 SynaRoute 桌面端全部功能。按模块分组，每条用例可独立打勾。

**测试环境准备**
- Windows 11，已安装 SynaRoute（`F:\SynaRoute\synaroute.exe`）
- 至少准备 2 个可用的上游 Key：建议 1 个 Anthropic 协议、1 个 OpenAI 协议（用于跨协议测试）
- 已安装 Claude Code CLI 和/或 Codex（用于工具接入与自动跟随测试）
- 建议装 `ripgrep`（测检索主路径）；再单独找一台没装 rg 的场景测兜底（或临时改 PATH）

**结果记录说明**：每条用例末尾「实际」栏，测试时填 ✅通过 / ❌失败（附现象）/ ⚠️部分通过。

---

## 1. 应用启动与窗口

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 1.1 | 首次启动 | 双击 synaroute.exe | 主窗口正常显示，无崩溃、无空白 | |
| 1.2 | 版本号显示 | 查看设置页/关于区域 | 显示正确版本号（与 Cargo.toml 一致） | |
| 1.3 | 单实例 | 已开着应用时再次双击 exe | 不新增进程，聚焦已有窗口（任务管理器仅 1 个 synaroute.exe，多个 msedgewebview2.exe 属正常） | |
| 1.4 | 首启健康检查 | 启动后立即看分类页 Key 状态 | 启动即开始探测，不需等 60s；有密钥的 Key 很快显示 up/down | |
| 1.5 | 版本更新检查 | 启动后观察 | 后台检查更新，有新版本时弹提示（无网络时静默失败不崩溃） | |
| 1.6 | 关闭再开 | 关闭窗口后重新启动 | 之前配置的 Key、分类、设置全部保留 | |

---

## 2. 厂商管理（Vendor）

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 2.1 | 新增厂商 | 厂商页 → 新增，填名称 + base_url | 保存成功，列表出现新厂商 | |
| 2.2 | 编辑厂商 | 点已有厂商改 base_url | 修改保存，列表更新 | |
| 2.3 | 删除厂商 | 删除某厂商 | 从列表移除；重启后仍不在 | |
| 2.4 | 空校验 | 名称或 URL 留空保存 | 有校验提示，不写入脏数据 | |

---

## 3. Key 管理

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 3.1 | 新增 Anthropic Key | 分类页 → 新增 Key，选 Anthropic 协议，填 base_url + 密钥 | 保存成功，列表出现，抽屉关闭 | |
| 3.2 | 新增 OpenAI Key | 同上选 OpenAI 协议 | 保存成功 | |
| 3.3 | 密钥加密落盘 | 保存后查看 `secrets.enc` | 密钥密文存储，非明文 | |
| 3.4 | 密钥不回显 | 重开 Key 编辑抽屉 | 密钥框显示 `••••••••`，不显示明文 | |
| 3.5 | reveal 查看密钥 | 点显示密钥按钮 | 能查看已存明文（仅本机 UI，后端解密） | |
| 3.6 | 编辑 Key | 改名称/base_url 保存 | 修改生效，列表更新 | |
| 3.7 | 启用/禁用 Key | 切换 Key 开关 | 状态变化并持久化；禁用的 Key 不参与路由 | |
| 3.8 | 删除 Key | 删除某 Key | 从列表移除，密钥一并清除 | |
| 3.9 | 保存失败处理 | 制造保存失败（如无效数据） | 按钮显示"保存中…"→报错提示，抽屉不关、列表仍刷新，不卡死 | |
| 3.10 | 分类隔离 | 在 claude-cli / claude-desktop / codex 三分类各建 Key | 三分类 Key 池互相隔离，切分类只显示对应 Key | |

---

## 4. 模型拉取与映射

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 4.1 | 拉取模型（已存 Key） | 已保存 Key 上点拉取模型 | 返回该 Key 可用模型列表，渲染为可选行 | |
| 4.2 | 草稿拉取（未保存） | 新建 Key 未保存时点拉取模型 | 用草稿密钥直接探测，返回模型列表 | |
| 4.3 | 模型 merge 保留 | 已配置 contextWindow 后再次拉取 | 已填的 contextWindow 值保留，不被覆盖 | |
| 4.4 | 上下文窗口填写 | 给某模型填 contextWindow | 保存并持久化，重开仍在 | |
| 4.5 | 模型映射 | 配置 expected_name → real_name 映射 | 下游请求该 expected 模型时，实际转发用 real 模型 | |
| 4.6 | 拉取失败 | 用错误密钥拉取 | 报错提示，不崩溃 | |

---

## 5. 健康检查

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 5.1 | 有效 Key 探测 | 配好有效密钥的 Key | 显示 up（健康） | |
| 5.2 | 无效 Key 探测 | 密钥错误的 Key | 显示 down（不可用） | |
| 5.3 | 无密钥 Key | Key 未填密钥 | 显示 unknown（未探测），不误判 | |
| 5.4 | 后台轮询刷新 | 保持页面开着等待 | 状态自动刷新（约 5s 轻量轮询 + 后台检查线程），无需手动 | |
| 5.5 | 手动重测 | 点某 Key 的健康检查 | 显示"检测中"→出结果；即使抛错也在 finally 里刷新，不永久卡"检测中" | |
| 5.6 | down 不参与路由 | 有 down 的 Key 时发请求 | 路由跳过 down，unknown/up 都给机会尝试 | |

---

## 6. 代理路由与故障转移（核心）

> 前置：分类下配 ≥2 个 Key，启动代理，把客户端指向代理地址。

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 6.1 | 启动代理 | 分类页启动代理 | 显示运行中 + 监听地址（127.0.0.1:端口） | |
| 6.2 | 停止代理 | 停止代理 | 状态变为已停止，端口释放 | |
| 6.3 | 单 Key 正常转发 | 客户端发非流式请求 | 正确转发到上游，响应正常返回 | |
| 6.4 | 优先级顺序 | 配多个 Key | 按优先级/顺序先试第一个可用 Key | |
| 6.5 | 故障转移 | 让首个 Key 返回非 2xx（如额度耗尽/错误密钥） | 自动切下一个 Key，最终用可用 Key 成功；调用日志记录每次尝试 | |
| 6.6 | 连接层失败转移 | 首个 Key base_url 不可达 | 报连接失败并切下一个 Key | |
| 6.7 | 全部失败 | 所有 Key 都失败 | 返回错误，日志含每个 Key 的真实失败原因（含上游错误体） | |
| 6.8 | 模型名透传/映射 | 客户端指定模型 | 经映射后转发真实模型名 | |
| 6.9 | 请求头透传 | 用真实 Claude Code 客户端 | User-Agent / anthropic-beta / x-stainless-* 等头透传给上游，鉴权头用 Key 自己的覆盖 | |
| 6.10 | 鉴权头正确 | 观察上游收到的鉴权 | Anthropic 用 x-api-key，OpenAI 用 Bearer；下游带来的鉴权头被剔除 | |

---

## 7. 流式透传（SSE，本轮新增）

> 前置：客户端发 `"stream": true`，且下游协议 == 该 Key 协议（同协议）。

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 7.1 | 同协议流式基本 | Anthropic 客户端 → Anthropic Key，stream:true | 响应边生成边返回（增量可见），非等整段完成 | |
| 7.2 | content-type 正确 | 抓包/看响应头 | content-type 为 `text/event-stream`（上游真实值），不是 application/json | |
| 7.3 | 长回答不超时 | 请求一个长回答 | 流式不设 30s 总超时，长回答完整返回不被掐断 | |
| 7.4 | 流式前故障转移 | 首个 Key 返回非 2xx（首字节前） | 状态码探测阶段可安全切下一个 Key | |
| 7.5 | 流式中不切换 | 流已开始后上游断 | 不再切 Key（标准行为），错误如实反映 | |
| 7.6 | OpenAI 同协议流式 | OpenAI 客户端 → OpenAI Key，stream:true | 流式正常透传 | |
| 7.7 | Claude Code 真机流式 | Claude Code 指向代理，正常对话 | 打字机效果流畅，无卡顿到整段才出 | |

---

## 8. 跨协议转换（本轮新增响应翻译）

> 前置：下游协议 ≠ Key 协议。**注意：跨协议 + 流式暂不支持**，测试时对跨协议 Key 关闭流式。

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 8.1 | Anthropic 客户端 → OpenAI Key（请求转换） | 下游 /messages，Key 是 OpenAI，非流式 | 请求体正确转为 OpenAI Chat Completions 格式 | |
| 8.2 | Anthropic 客户端 → OpenAI Key（响应翻译） | 同上，看客户端收到的响应 | 响应体翻译回 Anthropic Messages 格式（content[].text / stop_reason / usage.input_tokens），客户端能正常解析 | |
| 8.3 | OpenAI 客户端 → Anthropic Key（请求转换） | 下游 /chat/completions，Key 是 Anthropic | 请求体转为 Anthropic Messages 格式 | |
| 8.4 | OpenAI 客户端 → Anthropic Key（响应翻译） | 同上，看响应 | 响应翻译回 OpenAI 格式（choices[0].message.content / finish_reason / usage.prompt_tokens / object / total_tokens） | |
| 8.5 | stop_reason 映射 | 触发 max_tokens 截断 | Anthropic max_tokens ↔ OpenAI length 正确互转 | |
| 8.6 | system 消息处理 | 请求带 system | Anthropic system 字段 ↔ OpenAI system 消息正确转换 | |
| 8.7 | 非 2xx 不翻译 | 跨协议时上游报错 | 错误体原样透传（便于日志看上游真实错误），不做翻译 | |
| 8.8 | 跨协议流式的边界 | 跨协议 Key 开流式 | 已知限制：不支持 SSE 双向翻译（文档已注明），验证提示/降级行为符合文档 | |

---

## 9. 大脑聚合（多模型并行分析）

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 9.1 | 分类 tab 切换 | 大脑页顶部切 claude-cli/claude-desktop/codex | 停留在大脑页且高亮正确，加载对应分类的配置与 Key（三端聚合配置隔离） | |
| 9.2 | 已拉模型 Key 加成员 | 点选已拉取模型的 chip | 成员加入聚合 | |
| 9.3 | 手动加成员 | 未拉模型的 Key 手动填模型名 → 加入 | 手动成员加入（可 X 移除） | |
| 9.4 | 设决策者 | 指定某成员为决策者 | 决策者标记保存 | |
| 9.5 | 工作目录设置 | 填 work_dir 或开自动跟随 | 保存；自动跟随时取最近活跃项目 | |
| 9.6 | 运行聚合（Plan） | 填 prompt → 运行 | 参与者并行分析 → 聚合 → 决策者出计划；返回建议/计划（消耗 Key 额度） | |
| 9.7 | 执行计划（Apply） | 桌面端确认后执行 | 按计划写文件（需用户确认；MCP 通道不走此） | |
| 9.8 | 参与者失败容错 | 某参与者 Key 失败 | 可选参与者静默跳过，聚合继续（不因单个可选成员失败整体崩） | |
| 9.9 | 决策者失败 | 决策者 Key 不可用 | fail-loud 报错（决策者是必需组件） | |
| 9.10 | 上下文窗口限制 | 大项目 + max_context_tokens | 检索内容裁剪到上限（默认 50000 token）不超发 | |
| 9.11 | 语言跟随 | prompt 用中文/英文 | 回答语言跟随（或按 languageHint） | |

---

## 10. 文件检索

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 10.1 | ripgrep 主路径 | 装了 rg，检索关键词 | 用 rg 快速返回命中 | |
| 10.2 | rg 未命中 | rg 装了但无匹配 | 返回空结果，不触发兜底遍历 | |
| 10.3 | 纯 Rust 兜底 | 未装 rg（或临时移出 PATH）检索 | 自动回退内置目录遍历，功能不受影响（稍慢） | |
| 10.4 | 兜底成本上界 | 超大仓库兜底遍历 | 限深度 5、最多 3000 文件、只扫文本扩展名、跳过 node_modules/target/dist/.git | |
| 10.5 | 中文内容检索（UTF-8 边界） | 检索含中文的文件 | 不 panic（read_file_capped 和文件裁剪按字符切，不按字节） | |
| 10.6 | codegraph 检索 | 项目有 .codegraph/ + 装了 CLI | 走索引检索；否则自动跳过不报错 | |

---

## 11. 工具配置写入（一键接入）

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 11.1 | 写 Claude CLI 配置 | 应用工具配置（claude-cli 分类） | 写入 `~/.claude/settings.json` 的 env.ANTHROPIC_BASE_URL / ANTHROPIC_AUTH_TOKEN，原文件已备份 | |
| 11.2 | 写 Codex 配置 | 应用工具配置（codex 分类） | 写入 `~/.codex/config.toml` 的 [shell_environment_policy.set].ANTHROPIC_BASE_URL（尊重 CODEX_HOME），原文件已备份 | |
| 11.3 | 备份生成 | 应用后查看目录 | 原配置文件有带日期/后缀的备份 | |
| 11.4 | 还原配置 | 执行 restore_tool_config | 恢复到写入前的配置 | |
| 11.5 | 端到端接入 | 写配置后启动 Claude Code | Claude Code 请求经 SynaRoute 代理转发成功 | |

---

## 12. MCP 服务器（本轮补完 UI）

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 12.1 | MCP 卡片显示 | 打开设置页 | 出现 MCP 服务器卡片：开关 / 端口输入 / 服务地址 / 状态指示灯 | |
| 12.2 | 启用 MCP | 打开 MCP 开关 | 服务启动，状态指示灯变运行中，`mcp_status` 返回运行 | |
| 12.3 | 关闭 MCP | 关开关 | 服务立即停止，下次启动不自动运行 | |
| 12.4 | 端口修改 | 改端口（默认 9527）保存 | 按新端口监听 | |
| 12.5 | 端口占用 fallback | 端口被占用 | 有 fallback 提示 / 故障原因显示，不静默失败 | |
| 12.6 | 地址复制 | 点服务地址复制按钮 | 复制成功（短暂 ✓ 反馈） | |
| 12.7 | 状态轮询 | 保持设置页开着 | 状态约 3s 轮询刷新 | |
| 12.8 | 配置向导弹窗 | 点配置向导 | 弹出 4 步向导：接入命令 / hook 提示 / 验证 / 使用，每段代码可复制 | |
| 12.9 | 仅监听本机 | 检查监听地址 | 只监听 127.0.0.1，不对外暴露 | |

---

## 13. MCP 工具调用（synaroute_ai 端到端）

> 前置：MCP 已启用；Codex/Claude Code 已按向导接入 SynaRoute MCP。

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 13.1 | 工具可见 | 客户端列出 MCP 工具 | 看到 `synaroute_ai`，描述正确（多模型聚合、只返回建议不改文件） | |
| 13.2 | 基本调用 | 传 prompt 调用 synaroute_ai | 触发聚合，返回分析/计划文本 | |
| 13.3 | 传 cwd | 带 cwd 绝对路径调用 | 按该目录检索项目文件 | |
| 13.4 | 省略 cwd | 不传 cwd | 用自动跟随的最近活跃项目 | |
| 13.5 | category 参数 | 传 category=codex | 用 codex 分类的 Key 池与聚合设置 | |
| 13.6 | languageHint | 传 languageHint=en | 回答用英文 | |
| 13.7 | 缺 prompt | 不传 prompt | 报参数错误（prompt 必填） | |
| 13.8 | 未知工具名 | 调用别的工具名 | 返回 -32602 未知工具错误 | |
| 13.9 | 只返回建议 | 调用后看是否改文件 | 不修改任何文件，仅返回计划（由客户端自己编辑 + 用户确认） | |
| 13.10 | 调用日志 | 调用后查日志 | 记录 `synaroute_ai · 参与者数 · 文件数 · 耗时ms` | |

---

## 14. 工作目录自动跟随

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 14.1 | Claude CLI 跟随 | 在 Claude CLI 打开某项目后触发聚合 | 从 `~/.claude/projects` 识别最近活跃项目作为 work_dir | |
| 14.2 | Codex 跟随 | 在 Codex 打开某项目 | 从 `~/.codex/sessions` 识别活跃项目 | |
| 14.3 | Claude 桌面端兜底 | 桌面端建过 git worktree | 从 `git-worktrees.json` 补充识别路径 | |
| 14.4 | 桌面端无记录 | 桌面端无 worktree 记录 | 无法自动跟随，需显式传 cwd（不报错，提示明确） | |
| 14.5 | detect_recent_workdirs | 调用该命令 | 返回近期检测到的工作目录列表 | |

---

## 15. 设置页

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 15.1 | 主口令开关（降级） | 看安全卡片主口令行 | **开关禁用**、标"开发中"、说明"DPAPI 已启用、主口令模式开发中"，点不动 | |
| 15.2 | 局域网访问开关 | 切 LAN 开关 | 保存；开启后同网络设备可连（危险项有标识） | |
| 15.3 | 日志目录设置 | 填日志目录 / 浏览选目录 | 保存并生效；默认写安装目录 logs/ | |
| 15.4 | 日志落盘 | 触发事件后查日志目录 | 事件写 `logs/YYYY-MM-DD.jsonl`；只读位置回退 %APPDATA%\SynaRoute\logs | |
| 15.5 | 目录选择器 | 点浏览按钮（pick_directory） | 打开系统选目录对话框，选中路径回填 | |
| 15.6 | 导入导出（降级） | 看配置备份卡片 | **导出/导入按钮禁用**，下方标"开发中"说明 | |
| 15.7 | 语言切换 | 切换 zh / en | 界面文案全切换，无遗漏空串 | |
| 15.8 | 设置持久化 | 改设置后重启 | 设置项全部保留 | |

---

## 16. 调用日志与可观测

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 16.1 | 事件列表 | 发几次请求后查日志（list_events） | 显示调用记录（URL / 模型 / 请求体快照 / 结果） | |
| 16.2 | 故障转移可见 | 触发转移后查日志 | 每次尝试都有记录，含失败原因 | |
| 16.3 | 密钥不入日志 | 检查日志内容 | 请求体快照中无明文密钥（密钥在头部不在 body） | |
| 16.4 | 分类过滤 | 按分类查日志 | 只显示该分类的事件 | |

---

## 17. 安全与边界

| ID | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|----------|------|
| 17.1 | 密钥不出后端 | 全流程检查 | reveal 之外，密钥明文绝不返回前端 | |
| 17.2 | 默认仅本机 | 未开 LAN 时外部设备连代理/MCP | 连不上（仅 127.0.0.1） | |
| 17.3 | 原子写入 | 并发/中断写配置 | atomic_write 保证不产生半截文件（无 os error 17/32 类损坏） | |
| 17.4 | 非 JSON 响应兜底 | 上游返回非 JSON | 跨协议翻译解析失败时原样透传，不崩 | |

---

## 附：本轮重点回归（明天优先测）

这些是本轮刚开发/修改的，优先验证：

1. **流式透传**（模块 7 全部）— 让 Claude Code/Codex 真正可用的关键，重点测 7.1 / 7.3 / 7.7
2. **跨协议响应翻译**（模块 8：8.2 / 8.4 / 8.5）— 之前只翻译请求不翻译响应的 bug 修复
3. **MCP 设置卡片 + 向导**（模块 12 全部）— 之前 UI 完全缺失
4. **MCP 工具端到端**（模块 13）— Codex/Claude Code 真机接入 synaroute_ai
5. **主口令诚实降级**（15.1）+ **导入导出禁用**（15.6）— 避免安全误导
6. **检索兜底 + 中文 UTF-8**（10.3 / 10.5）— 无 rg 环境 + 中文文件不 panic

## 18. Windows / macOS 双平台链路回归（2026-08-07 审计新增）

| ID | 平台 | 场景 | 步骤 | 预期结果 | 实际 |
|----|------|------|------|----------|------|
| 18.1 | 双平台 | 上下文窗口单位 | 模型窗口填 `200` 选 K、填 `1` 选 M、填 `1000001` 选 token，保存重开 | 分别落盘 200000 / 1000000 / 1000001；1M 徽标仅窗口 ≥1M 时显示 | 自动：TS/Vite通过；真机待测 |
| 18.2 | 双平台 | 1M 请求头 | Anthropic 模型窗口设 1M，分别走流式/非流式；再改为 999999 | 1M 时上游收到 `context-1m-2025-08-07` 且不重复；差1时不加 | Rust 自动通过 |
| 18.3 | 双平台 | OpenAI 上游不误加 beta | OpenAI Chat / Responses Key 窗口设 1M 后转发 | SynaRoute 不新增 Anthropic 1M beta 头 | Rust 自动通过 |
| 18.4 | Windows | DPAPI 与缓存失效 | 保存/更新/删除密钥，启停/修改主口令，重启 | 明文正确、旧缓存不可读、迁移往返值不变 | Rust 自动通过 |
| 18.5 | macOS | Keychain 首次并发 | 首次安装同时发多条请求触发密钥库初始化，重启后再读全部密钥 | 只生成一把库密钥；所有条目重启后可解 | 算法自动通过；真机待测 |
| 18.6 | macOS/Unix | 硬链接凭据别名 | `ln .env notes.md` 后让只读工具读 notes.md / 当图片加载 | `nlink>1` fail-closed，文本和图片入口均拒绝 | mac CI 待本轮提交验证 |
| 18.7 | Windows | MSIX 虚拟化边界 | 用户双击与包应用派生进程分别运行，核日志/配置/MCP端口 | 配置遵循既有虚拟化规则；日志和 MCP 端口不分裂 | 真机待测 |
| 18.8 | macOS | 标准目录 | 启动后查看 `~/Library/Logs/SynaRoute` 与 `~/Library/Application Support/SynaRoute` | 日志/配置/MCP端口均在标准目录，`.app` 内无运行数据 | 真机待测 |
| 18.9 | macOS | 生命周期与托盘 | 关窗口、Cmd+Q、托盘退出；深浅色切换 | 关窗隐藏；Cmd+Q/托盘退出均执行 Exit 清理；template 图标清晰，tooltip 显示状态 | 真机待测 |
| 18.10 | macOS | 构建产物 | CI 构建并检查 `.app/.dmg/.app.tar.gz/.sig` | 最低系统版本、icns、arm64、前端嵌入、可执行位、产物存在六项全过 | CI 已通过 |
| 18.11 | macOS | 正式签名更新 | 用 Developer ID + 生产 updater 私钥发布并从旧版升级 | Gatekeeper 无手动绕过；公证通过；签名匹配内置 pubkey；更新成功 | 正式发布前阻断 |

详细审计与复审结论见 [08-代码审计报告.md](08-代码审计报告.md)。

## 已知限制（非缺陷，无需报 bug）

- 跨协议 **+ 流式** 组合不支持 SSE 双向翻译 → 跨协议 Key 请关流式或用同协议 Key
- codegraph 检索需目标项目有 `.codegraph/` 目录 + 装了 codegraph CLI
- Claude 桌面端不落盘可解析项目路径，仅 git-worktrees.json 有记录时能识别
- 聚合参与者/决策者调用为非流式纯文本，不处理 tool_use / 图片等多模态
- 跨协议转换仅覆盖纯文本消息
- 主口令增强模式、配置导入导出为开发中功能
