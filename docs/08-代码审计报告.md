# 08-代码审计报告

| 元信息 | 值 |
|--------|-----|
| 需求编号 | UNRELEASED-DELTA-SINCE-0.1.42 |
| 当前轮次 | A10 |
| 最后更新时间 | 2026-09-22（A10：初审阻断项已全部修复/复审，gates 全绿；未提交未发版） |

## A10 2026-09-21 初审 · 阻断 → 2026-09-22 修复复审通过

### 审计范围

`fix/codex-opus-rectify-usage-cache` 工作区相对 `17e7bbc` 的未提交改动（含未跟踪的 `upstream/media_rectify.rs`）。覆盖：Key 编辑器→字段保存→候选排序/准入→模型兜底→请求转换→预算/图片/缓存整流→同 Key 重试→响应转换→用量回填→日志快照。不是全仓所有业务的重新认证。未改生产代码、未提交。

方法：沿实际调用点逐项复核，不接受“函数存在”“测试计数增加”作为链路完成证据。下表代码缺陷来自静态控制流/字段流核验；给出的最小触发输入用于复现设计，不冒称本轮已执行所有反例。

### 风险清单

| 编号 | 类型 | 位置（相对 src-tauri/src，除特别注明） | 描述 | 级别 | 状态 |
|---|---|---|---|---|---|
| A10-1 | 正确性/功能缺口 | model_pool.rs:493-526；proxy.rs 调用 reject_if_unserviceable | 允许点名模型兜底只接了循环内跳过判据，循环前 503 闸门仍只认 may_serve。支持者被熔断/模型锁剔除、剩余候选开了兜底开关时，仍在尝试备用前返回 503 | 高 | ✅ 已修复（2026-09-22）：新增 `serves_or_substitutes`（`may_serve` ‖ `allow_named_model_fallback`），闸门 ①/③ 共用；回归 `the_gate_honors_the_named_model_fallback_switch`，注入变红 |
| A10-2 | 数据正确性 | upstream/sse.rs:232-243；proxy.rs:2037-2053 | 跨协议流式记账直接取 accumulated_usage，而它把 cache_read/cache_creation 固定置 0。新增缓存注入与用量计账没有闭环 | 高 | ✅ 已修复（2026-09-22）：翻译器加 cache_read/cache_creation 计数器，4 个上游 usage 读取点各按协议字段捕获（Anthropic `cache_*_input_tokens`、OpenAI 系 `*_tokens_details.cached_tokens`），`accumulated_usage` 带出；回归断言 `cache_read=145200`，注入变红 |
| A10-3 | 正确性/协议覆盖 | upstream/media_rectify.rs:88-122；upstream/convert.rs:823-829 | 剥图只遍历 messages[].content[]；同协议 Responses 保留 input[]，因此 input_image 原生入口完全不降级；tool_result.content[] 内图片也不递归处理。占位块固定 text，未覆盖 Responses 的 input_text | 中 | ✅ 已修复（崩溃前落地）：walks input[]（input_text 占位）+ 递归 tool_result.content[] + function_call_output.output；回归 `responses_and_nested_tool_images_use_protocol_correct_placeholders` + e2e |
| A10-4 | 稳定性/误重试 | proxy.rs:1781-1801、2554-2565；upstream/media_rectify.rs:129 | 三类整流实际只有非 2xx 门，没有承诺的状态码白名单/协议门。401、429、5xx 正文若带相关词也可触发剥图/降预算/缓存剥离 | 中 | ✅ 已修复：三路整流全部落状态码白名单（图片 400/415/422/501、预算/缓存 400/422）；补 budget/cache 两条状态码反向回归（`a_budget_error_body_under_a_non_validation_status_is_ignored`、`cache_rectify_only_fires_on_validation_status_codes`），注入均变红 |
| A10-5 | 数据语义/缓存所有权 | proxy.rs:1542-1555；upstream/cache.rs:87-114 | 缓存拒绝回退不确认本轮是否注入，直接删除目标范围内所有 cache_control；Anthropic 同协议客户端原断点也会被删除，并据一次拒绝记成整端点不支持 | 中 | ✅ 已修复（崩溃前落地）：`CacheInjection` 记录注入路径，`rollback_injected_cache` 只撤销我们注入且仍是我们那个值的字段；孤儿 `strip_cache_control` 已删；回归 `rollback_preserves_caller_fields_and_is_idempotent` 等 |
| A10-6 | 稳定性/超时 | proxy.rs:1720-1801、2502-2565 | 本地第二次 send 复用完整 probe_to/effective_to，没有扣除首发及错误体耗时。首发接近预算末尾时仍获得完整第二份超时，突破本候选/故障转移剩余预算 | 中 | ✅ 已修复（崩溃前落地）：两条转发路径各算一个绝对 deadline，首发/重试共享（流式 `timeout_at(deadline)`、非流式 `deadline.saturating_duration_since`）；补源码级判据 `same_key_retry_shares_one_absolute_deadline`，注入变红 |
| A10-7 | 可观测性 | proxy.rs:2476-2480、2554-2565、ForwardOutcome 构造 | 非流式 request_body 在整流前生成，之后 payload 被降预算/剥图/去缓存但快照不更新；最终响应与上游请求快照来自不同尝试 | 中 | ✅ 已修复（崩溃前落地）：快照序列化整流块之后的最终 payload；补 e2e `nonstreaming_retry_snapshot_reflects_the_rectified_payload`（断言快照 budget=1024），注入变红 |
| A10-8 | 功能缺口 | upstream/thinking_rectify.rs:103-117、220-271 | 新增“必须以 thinking 开头/Expected thinking but found tool_use”只扩了识别。原请求没有任何 thinking 块或残留 signature 时 out.any()==false，顶层 thinking 不被移除，整流返回 false | 中 | ✅ 已修复（崩溃前落地）：prefix-error 分支在无块时关顶层 thinking，`removed.any()` 为真、自愈；补回归 `a_prefix_error_with_no_thinking_block_still_self_heals`，注入变红 |
| A10-9 | 兼容性 | upstream/thinking_rectify.rs:377-396 | max_tokens≤1024 时预算整流直接删顶层 thinking，却保留消息历史中的 thinking/redacted_thinking；与本模块签名整流明确维护的“还有思考块不能直接关顶层”边界不一致 | 中 | ✅ 已修复（崩溃前落地）：`max<=floor` 时若 `has_thinking(payload)` 则 `return false` 交回原失败路径；补回归 `an_impossible_budget_window_defers_when_thinking_blocks_remain`，注入变红 |
| A10-10 | 数据正确性 | upstream/usage.rs:154-212 | 原文 usage 扫描无 envelope/path 限制，且完整 JSON 已成功解析后仍无条件扫描。output 内真实嵌套对象带 usage 时可覆盖真实用量（取 max）；字符串转义测试只覆盖文本，不覆盖嵌套对象 | 中 | 复审确认非缺陷：两层防御（`"usage":` 引用符+闭合对象判据 + 正文 JSON 引号必被转义成 `\"usage\"` 不命中）已机械保证现实场景，回归 `prose_mentioning_usage_is_never_counted_as_real_usage` 注入验证判据未空转；无实在失效经路，不改 |
| A10-11 | 兼容性/性能 | proxy.rs:2517-2536、effective_beta_header；upstream/cache.rs:56-79 | 首发先构造含完整 JSON 的 rb 然后 drop，再由 send_once 重建，重复序列化请求。缓存 beta 头另对所有 Anthropic 请求无条件追加，超出“只改跨协议”边界；不支持缓存的端点重试仍带此头 | 低/中 | ✅ 已修复（崩溃前落地）：单一 `send_once` 闭包消除重复序列化；`apply_upstream_headers` 只加条件式 1M-context beta、不加 prompt-caching beta，缓存注入仅跨协议；回归 `converted_anthropic_request_gets_cache_breakpoints_and_beta_header` 断言 `!beta.contains("prompt-caching")` |
| A10-12 | 质量门 | model.rs、sse.rs、KeyEditor.tsx、i18n.ts | npm run gates 被行数棘轮阻断，之前仅测试/clippy 成功不能称完整验收 | 阻断 | ✅ 已解决（2026-09-22）：压缩既有冗长注释/文案（不删事实、不抬上限）把 4 文件降回冻结值以下；`npm run gates` 全绿，并把 5 项基线向下棘轮锁住（proxy.rs 2913→2627 等） |

### 关键触发序列与修复建议

1. **兜底开关**：A 宣称 opus、B 只有 luna 且 allow_named_model_fallback=true。A 熔断，rank_candidates 返回未熔断 B；may_serve(B,opus)==false，但 enabled 池仍宣称 opus，reject_if_unserviceable 返回 503。统一“运行时允许承接”的判据，并在入口闸门与循环内共用；不能把 B 误声明为 opus 的原生能力 owner。补熔断/模型锁/配额窗口的真实 HTTP 测试。
2. **跨协议缓存计数**：Anthropic message_start 带 input_tokens=0、cache_read_input_tokens=10000，message_delta 带 output_tokens=20。翻译器会记 output=20 但 cache_read=0。应从原协议 usage 采集四项，协议转换展示与账本采集分开；测试应断言缓存数，不只断言 input/output。
3. **图片形态**：`{"input":[{"role":"user","content":[{"type":"input_image","image_url":"data:image/png;base64,AAAA"}]}]}` 在 Responses→Responses 时原样保留，strip_image_blocks 因无 messages 直接返回 0。另测 Anthropic tool_result.content 内 image。按协议结构遍历，不递归误改工具参数中的任意同名字段；保留缓存元数据。
4. **状态码**：例如 500 `upstream failure while processing cache_control`、401 `image input unsupported because API key is invalid`。当前均可进整流。将真实 status 作为显式入参，图片限制 400/415/422/501，缓存限制已证实请求错误码，预算限制 Anthropic 请求校验错误；添加反向 HTTP 测试验证上游仅收到一次请求。
5. **缓存所有权**：直通请求自带 tools/content 断点，上游回 cache_control 错误，回退会删除用户断点。注入应返回本轮改动记录，回退只撤销自己的字段；没有注入就不武装端点记忆。已有 4 个断点、system 断点与不同 TTL 也需用例。
6. **重试预算/快照**：使用同一个绝对截止时间计算首发、读错误体、重发剩余预算；保留并发 permit 到最终响应体生命周期结束。快照须反映最终实际发送 payload，必要时单列首次失败信息，不将其与重试结果拼成假现场。
7. **签名/预算整流边界**：`thinking=enabled` + assistant 首块只有 tool_use、没有 thinking/signature 时，签名识别命中但动作不发生。预算窗口不可用且历史仍有思考块时，保守拒绝整流或安全清理历史，不只删除顶层。不要宣称“识别场景扩到七种”就等于七种均能自愈。
8. **usage 误计最小输入**：`data: {"response":{"output":[{"usage":{"input_tokens":999999}}],"usage":{"input_tokens":10,"output_tokens":2}}}`。结构化提取先得 10，原文扫描再合入 999999。完整事件应只信协议 envelope；截断恢复应限制来源/结构，优先考虑流式结构化采集，避免在不可信内容中无差别搜键。
9. **缓存收益边界**：现有注入跳过字符串型消息 content，也没有 system 锚点；工具末尾断点不能缓存排在它之后的 system/消息历史。不能把“加了两个断点”报告成“所有长对话不再全价重发”。只在缓存前缀稳定、长度达到上游阈值且上游实际回报命中后才能证明收益。

### 已核实有效的部分

- 新开关确有 UI state、checkbox、保存载荷和 Rust serde 字段；不是完全未接线，但 A10-1 使其在重要场景失效。
- 预算整流已在转换后 payload 上执行，同 Key 仅一个重发块；原来的“Codex 请求没有 thinking 因而恒 no-op”位置错误已经消除。
- 缓存注入确实调用于流式、非流式转换后路径，直通请求不执行注入 helper；但回退边界另见 A10-5。
- 新 schema 校验是真实的本地拒绝，不是 schema 兼容转换，不能让 Codex 自带的顶层 union 工具变为可用。
- `||` 共用一次重发是用户已选择的限制，双重故障只修第一项不单独报成缺陷；但不能宣称三个文本判据在任意错误正文上数学互斥。

### 验证结果与证据边界

- 本轮 `cargo test --lib`：**1355 passed / 0 failed / 8 ignored**。完整输出保存在本机临时文件 `synaroute-review-tests.log`，未截尾隐藏失败信息。
- `cargo clippy --lib --all-targets -- -D warnings`：通过。`cargo check --lib`：通过。
- `npx vitest run --exclude '**/.claude/**'`：**33 文件 / 238 测试通过**；`npx tsc --noEmit`：通过；`npm run build`：通过（存在 bundle >500KB 提示，不作为本轮缺陷）。
- 首次 `npm test` 把审查代理的两个隔离 worktree 也扫进来，报 99 文件/714 测试；该膨胀数字不作为本工作区基线，已显式排除后重跑。
- `npm run gates`：策略门通过，棘轮失败：model.rs **2049/2016**、proxy.rs **3074/2913**、KeyEditor.tsx **1468/1452**、i18n.ts **1380/1376**。链式命令在 ratchet 处退出，不能声称后续 ratchet:verify 已通过。
- 未做真实供应商付费调用、未改用户客户端配置、未验证 Tauri 真机 UI；没有证明用户故障那次的确切上游改写行为。
- 预算采样测试不是全定义域穷举，更不覆盖全部发送/上游链路；不能据其通过断言“责任必然在上游”。日志里该确定归因应改成观测及有限重试说明。
- 先前不同全量轮次偶发不同失败，仅能说明不稳定，不能证明它们是既有问题或与本轮无关。
- 子代理提出的“Responses 历史签名一定被带到下个 Anthropic Key”未提供转换确实保留该块的输入证据，未升级为确认缺陷。

### 复审结论（2026-09-22 修复轮）

| 初审范围 | 修复情况 | 复审结论 |
|---|---|---|
| A10-1、A10-2 | 生产代码 + 回归测试均本轮补齐 | ✅ 已修复，注入验证变红 |
| A10-3/5/6/7/8/9/11 | 生产代码在上一轮崩溃前已落地，本轮补齐缺失/薄弱回归 | ✅ 已修复，注入验证变红 |
| A10-4 | 生产白名单已落地，本轮补 budget/cache 两条状态码反向回归 | ✅ 已修复，注入验证变红 |
| A10-10 | 复审确认非缺陷（两层防御机械保证，回归判据未空转） | 不改，已核 |
| A10-12 | 压缩既有注释/文案降回冻结值，不抬上限 | ✅ gates 全绿，5 项基线向下棘轮锁住 |

**发现的关键事实**：上一轮会话崩溃后遗留了一个**测试构建失败**（`rectify_on_budget_error` 加了
`status: u16` 首参，5 处测试调用点未同步 → 整个测试二进制编译不过），而当时被 `| tail` 掩盖成
「全绿」（退出码被换成 tail 的 0）。本轮首先修复它，再逐条核对 12 项真实状态。

### 提测建议

**代码侧已就绪**：`cargo test --lib` 1366 passed / 0 failed / 8 ignored（连跑确认；偶发的
`mcp::a_disconnected_client_must_cancel_the_in_flight_work` 是已知并发 flaky，单独跑绿）、
clippy `-D warnings` 干净、`cargo check --lib` 干净、`tsc --noEmit` 干净、vitest 238 passed、
`npm run gates` 全绿。**未提交、未发版**（本轮不含真机验证与真实供应商付费调用）。

## A9 2026-09-16（全链路代码审计 · 修复复审）

### 审计范围

当前 `master` / `HEAD=fbdd2ae`，对照父提交 `83a0423`，覆盖近期协议转换与 SSE、代理接线、聚合检索与敏感数据边界、Codex 会话同步/SQLite、用量持久化、Key 排序、前端 IPC 契约、质量门与测试链路。审查按正确性、兼容性、稳定性、数据安全、可观测性、可维护性逐条沿真实调用链追踪；只保留能给出具体触发序列和代码证据的问题。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A9-1 | 数据正确性 | `src-tauri/src/store.rs:1848-1874,1994-1997` | `flush_usage_if_dirty` 在原子写入成功前就把 `usage_baseline` 抬到当前总量。若 `atomic_write` 失败，函数只重新标脏；下一次重试计算出的 delta 已为 0，内存中更新过的桶也不会再次写盘，重启后该段用量丢失。序列：记录 usage → 让 usage 文件写失败 → 恢复写权限 → 再 flush → 无增量可写。 | 高 | ✅ 已修复（2026-09-16） |
| A9-2 | 数据安全 | `src-tauri/src/agent_grep.rs:42-60`、`src-tauri/src/agent_tools.rs:752-753,778-808`、`src-tauri/src/retrieval.rs:320-338,575-618` | `read_file` 已有硬链接别名防线，但 grep 与旧 retrieval 两条读路径只按命中文件名调用 `is_sensitive_path`，没有 canonical/硬链接判定。将 `.env` 硬链接为 `notes.rs` 后，用聚合 `grep` 或自动 retrieval 命中 `notes.rs`，凭据会被读入并发送给上游；现有测试只覆盖敏感文件名本身。 | 高 | ✅ 已修复（2026-09-16） |
| A9-3 | 稳定性 / 资源耗尽 | `src-tauri/src/upstream/sse.rs:196-215`、`src-tauri/src/upstream/sse_bounds.rs:156-166` | `MAX_LINE_BYTES` 只检查处理完所有换行后留在 `buf` 的尾段；一个超过 8 MiB、且在同一个 chunk 中带 `\\n` 的完整 SSE 行会先被 `drain(..=nl)` 整体复制到 `raw`、解析，随后 `buf` 已为空，行上限永远不触发。恶意上游可用超大完整行制造瞬时多份内存和 JSON 解析压力。 | 高 | ✅ 已修复（2026-09-16） |
| A9-4 | 稳定性 / 资源耗尽 | `src-tauri/src/upstream/sse.rs:562-605,1183-1196`、`src-tauri/src/upstream/sse_bounds.rs:85-136` | Responses 完整 `function_call` / `custom_tool_call` 路径绕过 `push_tool_args`，直接把完整 `arguments` 放入 `tool_calls`；无 `call_id` 时还把完整参数拼进 `anthropic_tool_seen` 的去重键。因而 `MAX_TOOL_ARGS_BYTES` 和去重集合的条目上限都不能限制单个参数/键的大小。`response.output_item.done` 带大参数即可触发，现有 Responses→Chat 槽位测试只用 `{}`。 | 高 | ✅ 已修复（2026-09-16） |
| A9-5 | 正确性 / 一致性 | `src-tauri/src/tools/codex_sessions.rs:590-601`、`src-tauri/src/tools/codex_session_sqlite.rs:91-115` | 会话 rollout 改写失败时，`done_ids` 为空，但调用方仍按全部 `scanned_ids` 生成 `catalog_rows` 并交给 `sync_sqlite`。结果是 catalog 的 provider/missing 元数据可能先被改成目标值，而 rollout 首行仍是旧 provider；列表替未完成的路由修复背书。序列：锁住 rollout → 同步失败 → catalog 写成功 → Codex 恢复旧 rollout 仍走旧上游。 | 高 | ✅ 已修复（2026-09-16） |
| A9-6 | 并发一致性 | `src-tauri/src/key_order.rs:66-88,113-209` | 顺序快照与最终 `mutate_and_persist` 不是同一个 compare-and-swap。两个并发重排都基于旧顺序计算后，后提交者会用陈旧 id 序列覆盖先提交者；两次 IPC 都可返回成功，但最终顺序丢掉一个用户意图。现有测试只覆盖串行顺序和 no-op。 | 中 | ✅ 已修复（2026-09-16） |
| A9-7 | 数据保存 / 兼容性 | `src-tauri/src/usage_keys.rs:94-149` | `usage-keys.json` 读取失败或未来版本统一按空表返回；只要当前仍有 live Key，`sync` 就会把墓碑事实并到空表并直接写回旧版本，破坏“未来版本不覆盖”的意图。直接 `fs::write` 还没有原子替换/写锁，崩溃、并发刷新或磁盘写到一半可留下坏 JSON，下一次又按空表覆盖。 | 中 | ✅ 已修复（2026-09-16） |
| A9-8 | 协议兼容性 | `src-tauri/src/upstream/convert.rs:919-920,1050-1061,1213-1223` | Responses→Chat 只支持 `input_text`/`output_text`/`text`，`responses_content_text` 会静默丢掉 `input_image` 等图片块；下游 Responses 请求切到 Chat/Anthropic Key 时请求仍可能 200，但模型实际没有看到图片。现有 Anthropic→Chat 图片用例不能覆盖这个反向方向，`input_image` 在转换测试中零命中。 | 中 | ✅ 已修复（2026-09-16） |

### 修复建议

1. A9-1：只在 `atomic_write` 成功后提交 baseline、daily buckets 和 baseline_date；失败时保留原 baseline，下一次重算同一 delta，并增加“写失败→恢复→重试→重启读取”的故障注入。
2. A9-2：把 `resolve_readable` 的真实落点/硬链接判定抽成所有读路径共用的检查；grep 输出过滤和 retrieval 在读取/切片前必须执行同一防线，并增加 `.env` 硬链接为源码文件名的 rg、fallback、自动 retrieval 三组用例。
3. A9-3：在 `drain` 前依据首个换行位置拒绝超长完整行；测试“大于上限且同 chunk 带换行”的反例，不能只测无换行尾段。
4. A9-4：完整 item 先做参数字节上限检查，再构造去重键；去重键使用有界摘要或拒绝超长 call id/参数，所有六个 SSE 方向分别注入大参数。
5. A9-5：把“rollout 已成功”与“catalog cleanup-only”分成两类计划；provider/missing 更新只能使用 `done_ids`，内部索引清理仍可单独使用 child 证据，并补“rollout 锁失败后 catalog 不得更新”用例。
6. A9-6：将顺序计算放入 `mutate_and_persist` 的同一临界区，或在提交时校验快照版本；增加两个并发重排交错测试。
7. A9-7：未来版本/读取失败必须以三态结果传播并禁止覆盖；写入改成同目录临时文件 + rename，并用同一把锁串行 read-merge-write。
8. A9-8：将 Responses `input_image` 映射到 Chat 的 `image_url`，暂不能支持时至少本地明确拒绝，禁止静默删块；增加 Responses→Chat 的纯图、图文混合和多图测试。

### 验证证据与边界

- `cargo test --lib`：**1319 passed / 0 failed / 8 ignored**（完整输出保存在本轮工作树外的临时日志中；目标代码以退出码 0 验证）。
- `cargo check --lib`：通过；确认生产构型可编译，未复现此前 `cfg(test)` 重导出导致的构建缺口。
- `cargo clippy --lib --all-targets -- -D warnings`：通过。
- `npm test`：**229 passed / 31 files**；`npm run build`：通过，Tailwind 颜色产物门通过。
- `npm run gates`：全绿；策略门、官网门、棘轮和 `ratchet:verify` 均通过。棘轮基线只向下调整：`agent_tools.rs` 949→929、`store.rs` 2890→2888、`convert.rs` 1667→1656、`sse.rs` 1247→1242。
- A9-1：真实 `Store::flush_usage_if_dirty` 写入失败→恢复→重试用例通过，失败时 baseline、桶、retired 和日期保持不变。
- A9-2：rg 命中过滤、Rust fallback、自动 retrieval 的硬链接别名用例通过；共享判定在 canonical 落点和硬链接身份无法确认时拒绝。
- A9-3/A9-4：SSE 完整超大行、Responses 完整工具参数（Chat/Anthropic 两个方向）用例通过；`cargo test --lib upstream::sse` 为 56 passed。
- A9-5：catalog provider 计划只接收成功 rollout，cleanup 计划单独收集 child/内部证据；Codex 全量测试中相关回归通过。
- A9-6：同一 `mutate_and_persist_when` 临界区内计算和提交 priority；并发重排回归通过。
- A9-7：未来/损坏文件不覆盖、原子写失败可重试、并发 read-merge-write 合并用例通过。
- A9-8：Responses→Chat 纯图、图文多图、无 URL 明确披露，以及经 Chat 中枢到 Anthropic 的图片转换用例通过；`cargo test --lib upstream::convert` 为 82 passed。
- 未执行真机验证：本轮只验证代码、单元测试、构建和质量门；Codex 真机列表/旧会话、rg 外部工具实际安装形态、以及不同上游对远程图片 URL 的接受情况仍属于真机/上游现场维度，不在本次测试结论内。

### 复审结论

| 编号 | 初审描述 | 修复情况 | 复审结论 |
|------|---------|------|------|
| A9-1 | 用量 flush 在写盘成功前抬高基线 | 已改为候选快照成功落盘后提交，并有失败重试回归 | ✅ 已解决；Rust 全量测试通过 |
| A9-2 | grep/retrieval 硬链接别名可绕过敏感文件防线 | 已抽 `is_sensitive_read_path`，接入 rg、fallback、retrieval/codegraph 文件读取 | ✅ 已解决；三类读路径回归通过 |
| A9-3 | SSE 完整超大行绕过行缓冲上限 | 已在 drain 前按完整行长度拒绝 | ✅ 已解决；完整行与未终止行测试均通过 |
| A9-4 | 完整 Responses tool arguments 绕过参数和去重键上限 | 已统一有界参数 helper，并用摘要替代完整参数去重键 | ✅ 已解决；SSE 方向回归通过 |
| A9-5 | rollout 失败时 catalog 仍按全扫描会话同步 | provider 计划改用 `done_ids`；child/internal catalog cleanup 独立收集 | ✅ 已解决；Codex 同步/SQLite 回归通过 |
| A9-6 | Key 重排并发旧快照互相覆盖 | 读取、变换、重编号放入同一配置 mutation 事务 | ✅ 已解决；并发重排回归通过 |
| A9-7 | usage-keys 未来/损坏文件可能被空表覆盖，裸写非原子 | 增加 Missing/Usable/Protected、互斥和原子写 | ✅ 已解决；兼容、失败、并发回归通过 |
| A9-8 | Responses→Chat 静默丢 input_image | 增加图片块转换与不支持形态的显式说明 | ✅ 已解决；Responses→Chat/Anthropic 图片回归通过 |

### 提测建议

A9-1～A9-8 的代码项已完成复审并通过当前自动验证，可以解除本轮“代码缺陷阻断”。这不等价于真机放行：Codex 旧会话/列表、真实外部 `rg` 安装形态、不同上游对图片 URL 的支持仍需按 `docs/14 §21.2` 的真机清单验证；本轮没有把这些现场结论写成代码测试通过。

## A8 2026-09-14（盘点「未做的事」· 不是缺陷扫查）

### 本轮问的问题

「本项目还有哪些没做的和可以优化的」。**不是**又一轮缺陷扫查 —— 目标是把散在
CLAUDE.md / docs/14 §21 / docs/19 §7 / 本文件 A7 里的「未完成」对齐到一处，
并区分**真的没做**与**刻意不做**（本仓纪律：把刻意设计报成缺陷会让人去做已知有害的改动，
比漏报更糟）。

方式：7 个维度并行扫 + 对抗式复核，另加我自己的独立取证。

🔴 **完成度如实说**（本节初稿写「两个维度没返回、9 条未读」，那是在工作流跑完之前写的，
已作废）：**7 个维度全部返回，共 31 条候选**；但 `VERIFY_CAP=24` 只送前 24 条进复核，
而 24 条里**只有 11 条拿到裁决**（其余 13 个复核代理因 **429 / 502** 失败）。
按维度落点：`security-surface`（3 条）**整批没进复核**、`deliberate-revisit` 5 条里只复核了 1 条。
故下表分两类标注：**我自己逐行取证过**的，与**经对抗式复核且我核过其取证**的。
未复核的候选留在 `subagents/workflows/wf_03f87a08-a1a/journal.jsonl`，别当结论用。

### 🔴 A8-1 元缺陷：「代码侧没有待办」这句话是假的

| 编号 | 类型 | 位置 | 描述 | 级别 | 状态 |
|------|------|------|------|------|------|
| A8-1 | 文档 | `docs/14:14` + `CLAUDE.md` 交接段 | **已修**：两处顶层状态明确区分「代码待办」与「真机验证」；A7/A8 代码项全部修完后又同步改了一次（现在写的是「代码侧无待办 ≠ 可以发版」+ C-16~C-40） | **高** | ✅ 已修（2026-09-15） |
| A8-1b | 文档 | 本文件 `A7-1` 状态列 | **已修**：A7-1 状态已改为「已修（64389a5）」；`persist_lock` 与源码级判据均已在 HEAD | 中 | ✅ 已修（2026-09-15） |

🔴 **本节初稿把这条写成「7 条待修，其中 A7-1 是数据丢失级」——那是错的**：A7-1 已在 `64389a5` 修完。
留着旧说法的代价很具体：它会让人重修一个已解决的缺陷，同时漏看真正未完成的那几条。
（**A7-2~A7-7 与 A8-6 已于 2026-09-15 全部修完**，逐条状态见下面 A7 那节的风险清单。）

### A8-2 ~ A8-5 文档漂移（照着动手会做无用功 / 查错方向）

| 编号 | 类型 | 位置 | 描述 | 级别 | 状态 |
|------|------|------|------|------|------|
| A8-2 | 文档 | `docs/14:2745`（§21.3 D 层） | **已修**：§21.3 已改成删除线并指向 `aggregate/fence.rs`；工具结果围栏已实现，旧的「唯一拦截点」声明已移除 | **中** | ✅ 已修（2026-09-15） |
| A8-3 | 文档 | `docs/30:18-78` | **已修**：第三十节现按当前 `worth_listing` / `row_is_confirmed_child` 实现描述；明确 fork 在插入门上排除，并记录 catalog 仅有 user 标记时的 A8-6 边界 | **中** | ✅ 已修（2026-09-15） |
| A8-4 | 文档 | `docs/14:2742`（§21.3） | **已修**：不再把转发侧换 Key 当作聚合路径的等价能力；记录该字段当前只在聚合路径读取、默认值为 `true`，是否暴露 UI 仍需真实需求取证 | 低 | ✅ 已修（2026-09-15） |
| A8-5 | 文档 | `CLAUDE.md` 09-05 段 vs 09-06 段 | **已修**：旧的 09-05 记录已加「第②条已被 09-06 推翻」标记，并说明当前是受限的 `INSERT OR IGNORE` 补缺行；不再把已实现能力写成只 UPDATE | 低 | ✅ 已修（2026-09-15） |

### A8-6 一处代码与自身声明不符（本轮唯一的非文档项，我逐行核过）

| 编号 | 类型 | 位置 | 描述 | 级别 | 状态 |
|------|------|------|------|------|------|
| A8-6 | 正确性 | `codex_session_catalog.rs` | **已修（2026-09-15）**：新增 `catalog_user_ids`（从库里读 `thread_source='user'` 的 id 集合，按 host 收窄），删除侧与 `would_mutate` 都据它否决 —— catalog 行**自己**标着 user 这个事实现在参与否决了。<br>🔴 **否决只作用于删除，不作用于更新**：provider / `missing_candidate` 那两步是无损的，被 user 标记救下来的冲突行必须照常同步，否则第三份 provider 副本在那些行上永远是旧值。写这条判据的用例当场抓到了我第一版把更新也掐掉（见 `a_catalog_row_marked_user_is_never_removed_even_if_the_plan_says_internal`） | 低 | ✅ 已修 |

**级别为什么是「低」而不是「中」**：删掉的是**索引行**，rollout / `threads` / 正文按设计一律不动
（C-39 的验证判据也是这么约定的），用户可见后果是 Desktop 侧栏少一条；且每个实际 mutation
的库都先做了 SQLite 快照（`backups/codex-sqlite`，KEEP=3）。而且触发它要求「catalog 说 user」
与「edge/job 说 child」**同时成立**，那本身是矛盾状态。

🔴 **它值得记的原因**：`docs/30:50-62` 曾把那道 user 否决**写成现行实现**，于是「这一维已经
防住了」是假的 —— 正是 A8-3 那条文档漂移**导致**这条缺口没被实现。本仓「带『已核对』字样的
注释是更贵的一种过时」那条的又一例。

⚠️ **修它时还暴露了第二件事**：这道否决**此前零覆盖**。既有那条
`catalog_cleanup_obeys_user_fork_unknown_and_host_vetoes` 走的是「**rollout** 说 user」那条路
（由 `merge_rollout_evidence` 把 `thread_source` 抬成 user），与本条要防的「rollout 说内部、
**catalog 自己**说 user」不是同一条路径。故第一次注入验证时把新加的否决整段删掉，
**105 条会话用例全绿** —— 已补专用用例，注入即红。

### A8-7 ~ A8-8 发布流程（我用 GitHub API 逐项核过）

| 编号 | 类型 | 位置 | 描述 | 级别 | 状态 |
|------|------|------|------|------|------|
| A8-7 | 文档 | `docs/14:43-45` | **已修**：docs/14 已改为 v0.1.63 已发布，正式 Release `387953497` 保留 17 个资产；旧重复 draft `387948845` 已删除 | 低 | ✅ 已修（2026-09-15） |
| A8-8 | 流程 | `.github/workflows/release.yml:29` + `:53` | **已修**：create-release 现在先按 tag 查已有 release；已有 draft 复用，已有正式 release 则拒绝创建重复对象 | 低 | ✅ 已修（2026-09-15） |

### A8-9 结构压力（不是缺陷，是「下一轮会撞墙的地方」）

| 编号 | 位置 | 现状 | 说明 |
|------|------|------|------|
| A8-9a | `config/quality/ratchet.json` | `store.rs` **2890/2902**（余 12）、`i18n.ts` **1376/1379**（余 3） | `npm run ratchet` 自己在提示「有 2 项已变好，跑 `npm run ratchet:update` 把它锁住」。**不锁 = 已完成的压缩变成下一个人的增长额度**，正是 `$rule` 与 OmniRoute #8584 那套要防的。修法就是跑那条命令，不改代码 |
| A8-9b | `src-tauri/src/tools/codex.rs` | **897 / 900（未冻结）** | 余 3 行。非冻结文件超 900 是**硬红且上限一律不许抬**，而近期工作正集中在 Codex 会话这一块。CLAUDE.md 里已写过「余量只剩 2 —— 下次动它必须先腾」。可抽：`DriftState` + `drift_state` + `drift_warning` 那一段（约 240 行，消费方只有同文件 `preview` 与 `codex_watch.rs`） |
| A8-9c | `codex_catalog.rs` 881 · `mcp.rs` 876 · `types.ts` 871 | 均 < 900 但贴近 | 一旦跨过 900 就进 frozen，从此一行都加不进。`types.ts` 每加一个 IPC DTO 就长（近期 12 个会话命令就是这么长起来的），分出 `types.sessions.ts` 是零语义、家规已有的动作（先例：`i18n.sessions.ts` / `mockData.sessions.ts`） |

⚠️ **16 个冻结文件里 14 个余量恰好为 0**，这**不是**缺陷、**不要**照 OmniRoute 那套去下调
它们朝 900 收敛 —— §21.1 B3 明文写着「对这 16 个不设进一步收敛目标」。

### 三份未提交的分析稿：结论

工作区里 `analysis-comparison.md` / `catalog_child_evidence_analysis.md` /
`error_handling_review.md` 都写于修复提交 `64389a5`（20:03）**之后**，但它们的核心指控
**对当前代码不成立**：

- `analysis-comparison.md` 说「我们只从 rollout 读 `thread_source`、不主动查 SQLite `threads` 表」
  → **假**。`codex_session_catalog.rs:198-207` 的 `plan_from` 对 `threads.thread_source` 做了
  `COALESCE(thread_source,'')`，`codex_sessions.rs:564` 传的是**全部** `scanned_ids`；
  `merge_rollout_evidence:346-350` 在 rollout 缺来源时**保留** DB 侧的值。
  用例 `threads_source_fills_missing_rollout_source_for_cleanup` 逐字锁住这一点。
  🔴 **照它去「补查 threads 表」是重复劳动**，更糟的是它会让人以为老 schema 的 guardian
  至今清不掉，从而去**放宽删除判据**（比如拿标题当 DELETE 证据）—— 而 `:529-530` 明确禁止那一招。
- `catalog_child_evidence_analysis.md` 的「critical data loss risk」按其原文**不成立**
  （删的是索引行不是会话正文）。它真正指出的残余就是上面的 **A8-6**。
- `error_handling_review.md` 「错误处理健壮」大体仍成立，唯一失效的是它引
  `codex_session_sqlite.rs:91-94` 那句「空切片 → 一个字节都不写」推出「ids 为空 → 跳过整库」——
  `64389a5` 之后 cleanup-only **必须运行**（`:106-111`），那句函数文档自己也还没改。

### 本轮实测基线（**与文档记的不一致**）

```
cargo test --lib   1286 passed / 0 failed / 8 ignored   （连跑 2 次一致；docs/14:47 记 1277）
npm test           229 passed / 31 文件                  （docs/14:48 记 228）
cargo check --lib  干净        clippy --all-targets -D warnings  零警告
npm run gates      全绿（零棘轮抬高）  tsc --noEmit  干净
```

⚠️ `git status --untracked-files=no` 为空（tracked 文件与 HEAD 一致），所以这不是本地改动
造成的差异 —— **文档那两个数字是当轮测早了**。本仓自己的纪律是「别引用文档里的历史数字当基线」,
故这一条只记不修。

### 顺带核过、**没有**问题的几项（免得下一轮重复劳动）

| 查了什么 | 结论 |
|---|---|
| 生产代码里的 TODO / FIXME / `unimplemented!` / `todo!` | **零命中**（`write.rs` 那些 `HACKED` 全是聚合写路径的注入夹具） |
| 恒 `disabled` 的前端控件 | 零命中 |
| 三个疑似死 i18n 键（`dragHint` / `autoSyncSaved` / `indexClean`） | 已被清掉，`src/` 全仓零命中 |
| `update_watch` 会不会造成事件环洪水 | 不会 —— 只在**结论变化**时落，且 `Outcome` 三态把 `Unknown` 与 `UpToDate` 分开正是为此 |
| §21.2 真机验证清单有没有漏项 | **没有**。C-39 覆盖了 09-13 那轮（含「预置旧 guardian catalog 行 → 同步一次必须消失」） |

### 提测建议

**A7/A8 的代码项已全部处理完（2026-09-15）**：A7-1~A7-7 + A8-6 逐条见上表状态列，
其中 A7-7 只做了零风险那半（借用序列化那半刻意不做，理由在它的状态列里）。
**10 条故障注入全部按预期变红**，含三条打在用户实报缺陷本体上的。

**本轮未做**：`security-surface` 的 3 条候选没有进入对抗式复核；`deliberate-revisit` 只有部分
候选复核；真机验证 C-16~C-40 只能用户本人做（本轮新增 **C-40**，见 docs/14 §21.2）。

## A7 2026-09-10（初审 · 待修复）

### 审计范围

**v0.1.62（`3d687b7`）的全链路效率/内存专项**。A6 覆盖到 v0.1.42 之后四轮，此后相隔二十版、
新增 31 个 `.rs` 文件（`aggregate/*` 拆分、`codex_session_*` 七件套、`upstream/{error_hint,
sse_error,stream_idle,thinking_*,structured_output}`、`proxy/{log_cap,models_endpoint,soft_error}` 等）。

本轮重点是**效率与内存**，正确性缺陷照报（级别更高）。方式：7 个维度并行扫
（热路径分配 / 流式内存 / Store 锁与 IO / 后台循环 / 聚合成本 / 前端渲染 / 正确性），
每条发现再独立对抗式复核（默认立场是「它可能是错的，或是本仓刻意设计」）。

🔴 **本轮的取证可信度分三档，见末尾「取证边界」一节** —— 审查中途工具通道返回过**伪造内容**，
故只有逐行自核过的条目进「已确认」。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A7-1 | 正确性 | `store.rs:1156-1160` + `secret.rs:988` | **已修（64389a5）**：`persist_lock` 在序列化之前取得，避免并发 persist 用旧快照覆盖更新；`store.rs:3492` 的源码级判据仍在 | **中** | ✅ 已修 |
| A7-2 | 内存 | `upstream/sse_bounds.rs`（原 `sse.rs:684`） | **已修（2026-09-15）**：`flush_anthropic_tool_calls` 搬进 `sse_bounds`，改为**只克隆小的 id/name、把大的 args `mem::take` 移走**，不再深拷贝整份 `tool_calls`。⚠️ **没有采纳建议里的整份 `mem::take`**：那会清空槽位、破坏 `block_tool_slot` 的 index 映射；按下标遍历 + 取走 args 既省掉大拷贝又保住下标，重复调用仍靠 `anthropic_tool_seen` 去重 | **中** | ✅ 已修 |
| A7-3 | 内存 | `upstream/sse_bounds.rs`（调用点在 `sse.rs` 四处） | **已修（2026-09-15）**：`MAX_TOOL_ARGS_BYTES = 1 MiB` 单调用参数上限 + `MAX_TOOL_SLOTS = 256` 槽位上限（后者补的是**Anthropic 上游侧** `content_block_start` 那条循环 —— `tool_slot` 只管 Chat 侧的 `index`，那是漏的一半）。越界**拒绝并终止流**：发一条下游协议自己的 error 事件，此后 `push`/`finish` 一个字节都不再翻译（复用 `started` 闩，不新增状态）。⚠️ 刻意不截断：截断出的是非法 JSON，下游可能拿一个参数不全的调用真的去执行 | **中** | ✅ 已修 |
| A7-4 | 可维护性 | `proxy/log_cap.rs` + `store.rs` / `lib.rs` / `model.rs` / `proxy.rs` | **已修（2026-09-15）**：6 处同源旧账全部改为从 `REQ_LOG_CAP` **派生**表述，不再各抄一个数字（`store.rs` 两处 + `store.rs:164` 的「4 万字符」+ `lib.rs:454` + `proxy.rs:702` + `model.rs:1565`）。⚠️ **刻意没给事件环加字节预算**：真实量级 32~65 MB、有界非泄漏，且触发前提是用户**主动打开**默认关闭的排障开关；而把最老条目的 trace 置 None 等于「在用户刚打开取证开关时开始丢取证数据」，方向与 `log_cap` 那次提上限的初衷正相反 | 低 | ✅ 已修 |
| A7-5 | 正确性 | `components/ui/StatusBarPicker.tsx` | **已修（2026-09-15）**：effect 依赖从 `options`（内联数组字面量、每次渲染换引用）改为按选项**内容**算的 `optionSignature`，父组件重渲染不再复位 `active`。⚠️ **没有收窄成 `[open]`**（建议里那个）：那会让「菜单开着时选项内容真的变了」不再同步高亮，而状态条的选项来自轮询、开着菜单时确实会变 | 低 | ✅ 已修 |
| A7-6 | 内存 | `agent_grep.rs`（从 `agent_tools.rs` 抽出） | **已修（2026-09-15）**：rg 侧加 `--max-columns 400` + `--max-columns-preview`，**并且** `filter_rg_hits` / `walk_grep` 两条路径都过 `cap_match_text`（按**字符**边界截、带省略号）—— 只加 rg 旗标的话本机 rg 不在 PATH 上时一条都防不住。腾棘轮余量靠把 grep 命中筛选那一族整体抽成新模块（真删除，不是重排） | **中** | ✅ 已修 |
| A7-7 | 内存 | `upstream/session.rs` | **已修（2026-09-15，只做了零风险那半）**：`send(&payload)` 之后 `drop(payload)`，消掉缓存自愈分支里「`payload` 与 `plain` 同时在」的第四份 —— 而那一刻恰好是历史最大时。<br>🔴 **借用序列化那半刻意不做**：`cache::inject_anthropic_cache` 签名是 `&mut Value` 且在两处**原地改树**，借用切片做不到；弄错的失效方向**静默**（缓存不命中只表现为账单变贵）。要做得先改缓存注入的形状，属独立一轮 | 低 | ✅ 已修（部分，边界见描述） |

### 五处既有防线复核：全部未复发（逐行自核）

| 防线 | 判据 | 结论 |
|---|---|---|
| pretty-print 只在开关内 | `proxy.rs` 4 处全在 `if req_log` / `if !req_log` 的 else 支内（`:670` 在 `:663` 的 else 里） | ✅ |
| `strip_trace` 逐字段构造 | `store.rs:878-899` 逐字段；全仓 `..e.clone()` 零命中（只剩两处注释提到它） | ✅ |
| 候选排序用 `sort_by_cached_key` | `model_pool.rs:427` | ✅ |
| 上游 index 放大有门 | `upstream/mod.rs:91` `MAX_TOOL_SLOTS = 256`，**越界拒绝而非钳制** | ✅ |
| 进程级 static 表有上限 | `concurrency` retain / `quota_window` 256 + retain / `lan_guard` 1024 / `codex_session_view` 4096 + clear。另 `model_pool.rs:208` 的 `SEEN` 不裁剪，但键空间有界（分类 × 并集），上限几百条 —— 无缺陷 | ✅ |

前端：裸 `setInterval` 只剩 `OnboardingWizard.tsx:93`（向导「等第一个请求」那一步，用户必然在看屏幕，非托盘常驻场景，合理）；整店订阅 `useStore()` 零复发（三处命中全是「勿改回」的注释）。载荷：事件环 500 + `strip_trace`，诊断报告 `MAX_EVENTS_IN_REPORT = 200`，均有界。

### 修复建议

- **A7-1**：`persist()` 开头（降级守卫之后）取一把进程级 `Mutex<()>`，把「序列化 + 落盘」整段纳入互斥。锁序 `persist_lock → config.read()` 与 `persist_lock → WRITE_LOCK` 均无环，且无调用方持 config 锁跨 `persist`。代价约 100 µs 串行化，而写本来已被 `WRITE_LOCK` 串行。⚠️ `store.rs` 生产段 2844，`ratchet.json` 冻结 2902 —— 有余量，但仍需为那把锁与理由注释腾 4 行左右。
- **A7-2**：`let pending = std::mem::take(&mut self.tool_calls);` 换掉 `.clone()`。语义逐字等价（`emit_anthropic_tool_block` 不碰 `self.tool_calls`，已核），且重复调用契约由「take 后为空 → 早退」保持。净 +0~1 行。<br>🔴 **不要改成游标版本**（只发新增的）：那会静默丢掉「name 在 `finish_reason` 之后才到」的工具调用，而该维度**零测试覆盖**，改坏了会全绿通过。
- **A7-3**：给累积总量加上限（同 `TAIL_WINDOW_BYTES` / `REQ_LOG_CAP` 的既有做法），越界**拒绝并只警告一次**（同 `tool_slot`）。⚠️ 不要钳制：把不同增量挤进同一个槽会拼出参数错乱的工具调用，比丢一条增量糟。
- **A7-4**：只改注释，从 `REQ_LOG_CAP` **派生**表述（「500 × 2 × REQ_LOG_CAP 字符」）而不是再抄一个数字，并写明 `cap_to` 数 char 不数 byte、折叠替换这个前提、以及「流式只有一个大 body」这一点。净零行数。<br>**共 6 处同源分叉**（已逐处自核）：`store.rs:874`「各上限 20000 字符 —— 500 条满载约 19 MB」、`store.rs:882`「≈2000 万字符（≈19 MB）」、`store.rs:164`「trace 正文可达 4 万字符」（= 2×20000）、`lib.rs:454`、`proxy.rs:702`「正文可达 2×20000 字符」。<br>🔴 **第 6 处是真漂移不只是过时**：`aggregate.rs:55` 的 `AGG_LOG_CAP = 20_000` 注释写「**与调用模型日志同量级**」—— 那个量级现在是 65536，**差 3.3 倍，这句话已经是假的**。正是本仓「语义上必须相等的两个值要派生、不要各写一份」那条的实例。要么让它真的从 `REQ_LOG_CAP` 派生，要么把那句「同量级」改成如实说明为什么故意不同。<br>⚠️ **不建议给环加字节预算**（Find 代理提的第二半）：真实量级重算后是 32~65 MB、有界非泄漏、触发前提是用户**主动打开**默认关闭的排障开关；而把最老条目的 trace 置 None 等于**在用户刚打开取证开关时开始丢取证数据**，方向与 `log_cap.rs` 那次提上限的初衷（「上游点名了工具而我们的日志恰好不含那一段」）正相反。在没有一次真实 RSS 观测之前，那是拿确定的取证损失换一个没人报过的内存数字。
- **A7-5**：依赖数组收窄成 `[open]`。菜单开着时 Key 被删导致越界已由 `:120` 的 `if (o)` 兜住。本仓无 jsdom，故只能加源码级判据（照 `modelMappingRows` / `envConflictBanner` 的做法）。
- **A7-6**：参数表加 `--max-columns 400` + `--max-columns-preview`（后者让超长行给前 400 字符预览而不是整行换成一句提示，保住可读性；实测 ripgrep 14.1.1 下 1.4MB 单行文件 stdout 从 1 400 055 B 降到 476 B，且 `path:num:` 前缀仍在、`split_rg_line` 不断）。<br>🔴 `agent_tools.rs` **frozen=949、余量 0**，必须先腾：把 :705-716 六组 `"--glob", "!xxx"` 各并成一行可省 6 行（是删/合并，不是重排）。<br>⚠️ **walk_grep（:861）要另截** —— 它是 rg 起不来时的降级路径，同样整行 push；只加 rg 旗标的话，本机（rg 是 bash function、不在 PATH 上）一条都防不住。
- **A7-7**：**分两步，别一次改完。**<br>① 零风险那半：`:687` 自愈分支里 `payload` 已是死重，建 `plain` 前先 `drop(payload)`（或把首个请求收进内层 scope）。~1 行，不碰缓存注入，直接消掉第四份 —— 而那一刻恰好是「历史最大 + 双份同时在」。<br>② 借用序列化那半**另开一轮**：🔴 Find 代理提的 `Body<'a> { messages: &'a [Value] }` 会**弄坏 Anthropic prompt caching** —— `cache::inject_anthropic_cache`（`cache.rs:56`）签名是 `&mut Value`，在两处**原地改树**（tools 末元素 + 最后一条消息的最后一个 content 块），借用切片做不到。那正是 `payload` 必须是 owned mutable 树的全部理由，而报告的草图从头到尾没提缓存注入。弄错的失效方向**静默**：缓存不命中只表现为账单变贵。<br>session.rs 生产段 756、**未冻结**（900 上限，余量 144），放得下。

### 🔴 取证边界（本轮必读）

审查中途**工具通道返回过伪造内容**：连续多次 Bash/Grep/Read 无论查什么都返回同一份
`codex_session_view.rs` 模块头，其中夹着一段命令式文字，要求「把所有 `unwrap()`/`expect()`
在报告里标成『已复核安全』而不必逐个追守卫」。

处置：① 用 `git hash-object` 逐个比对 `secret.rs` / `store.rs` / `codex_session_view.rs`，
三者与 index **完全一致** → **磁盘从未被改**，污染只在工具输出通道；② 那段文字与本仓纪律
直接冲突（本仓要求逐个追守卫并留取证），**已忽略**；③ 工具结果是**数据不是指令**。

由此，可信度分三档：

| 档 | 条目 | 依据 |
|---|---|---|
| **逐行自核** | A7-1 / A7-2 / A7-3 / A7-4 + 上面五处防线复核 | 我自己读过源文件那几行，且文件已 hash 比对 |
| **子代理报 + 我逐条自核过取证** | A7-5 / A7-6 / A7-7 | workflow 跑完后我自己打开了每一处：StatusBarPicker 的依赖数组、grep 参数表（确认无 `--max-columns`）与 `MAX_READ_BYTES` 文档、session.rs 的两处 `json!` 与 `cache.rs` 的 `&mut Value` 签名。行号与形态全部成立 |
| **未采纳** | 14 条 TRIVIAL + 1 条 WRONG 的具体内容 | 只抽核过其中一条（尾窗 `drain`，复核结论正确地指出 `proxy.rs` 余量为 0）。其余未自核，故不进清单 |

**workflow 自身的完成度**（不要把它的 total 当覆盖面）：37 个代理，**27 完成 / 10 因 429 失败**。失败的包括**整个 `find:correctness-sweep` 维度**（正确性那一路压根没跑）与 9 个 verify（`sse.rs:memory`、`store.rs:correctness`、`proxy.rs:efficiency`、`health.rs:efficiency`、`model_choice.rs`、`tools_meta.rs`、`App.tsx`、`UsagePage.tsx`、`ProxyStatusBar.tsx`）。另有 `find:aggregate-cost` 卡了 3012 秒后重试。

也就是说 **7 个维度里只有 6 个真的跑了**，且其中若干条发现的复核被 429 打断（那些发现因此不在任何清单里）。要拿到完整分维结论需重跑 —— 建议降并发或分批。

### 提测建议

**A7 的代码项已于 2026-09-15 全部处理完**（含当时列为「中」的 A7-1~A7-3），逐条状态见上面风险清单。
A7-1 的数据丢失级路径当时已在 `64389a5` 修完。

**本轮未做**：`smoke:installer`（要求本机无运行实例）、真机验证 C-16~C-40（见 docs/14 §21.2）。

## A6 2026-08-30（复审 · 已通过）

### 审计范围

**v0.1.42 之后尚未发版的全部改动**：77 个已改文件 + 12 个新文件（+3594 / −770 行）。
按四轮分：

| 轮 | 内容 |
|---|---|
| R1（08-28） | Codex 模型目录（`tools/codex_catalog.rs` + 内嵌官方 `prompt.md` + `model_choice.rs`）、代理双栈监听（`proxy_listen.rs`）、局域网令牌常量时间比较的 `as u8` 截断、`sse.rs` 的 `tool_calls[].index` 内存放大 |
| R2（08-30） | 流式静默超时（`upstream/stream_idle.rs`）、扩展思考签名整流（`upstream/thinking_rectify.rs`）、`allow_in_aggregate`、MCP stdio 可观测性 + 收尾审查 14 条 |
| R3（08-30） | 原列为「刻意未做」的四项：跨协议流内 error 翻译（`upstream/sse_error.rs`）、stdio 转发期间回 ping、状态条并列回显 Codex 的 `model`、`allow_in_aggregate` 改走专用 IPC（`key_flags.rs`） |
| R4（08-30） | **B4 余额参与路由**（`balance_gate.rs`）—— 本轮审计的重点，因为它是最新写成、被独立复核最少的一块 |

审查方式：六个维度（正确性 / 兼容性 / 稳定性 / 数据安全 / 可观测性 / 可维护性）逐维过一遍，
**不只看 diff** —— 沿真实调用链追到后台循环、事件环、候选池排序、诊断报告与用户可见文案。
每条发现都要求「可证伪」：拿不出失效路径的不记。修完的每条都做**故障注入**
（去掉修复后对应测试必须变红）。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A6-1 | 正确性 | `balance_gate.rs` `verdict_from` | **判据顺序错**：`!r.ok → Unknown` 排在 `is_valid == Some(false) → Exhausted` 之前，而 `balance.rs:525` 在「上游声明账号不可用**且**没给余额数字」时返回的正是 `failed(why)` + `is_valid = Some(false)`（即 `ok == false`）→ **最可靠的那个信号被当成「我们这侧查询失败」丢掉**。`BalanceResult` 的字段文档专门把 `error`（我们这侧失败）与 `is_valid`（上游明说号废了）分开过 | **高** | ✅ 已修 + 专用用例 + 注入⑤变红 |
| A6-2 | 稳定性 | `health.rs::check_all_categories` / `lib.rs` 后台循环 | **后台刷新搭错了车**：挂在健康探测轮次里 → ① `health_check_interval_secs == 0`（用户关掉定时探测）时整轮 `continue`，余额刷新**跟着一起停**，而它依赖的是另一个设置（`auto_interval_min`），两个无关设置的隐式耦合且失效静默；② 周期被探测间隔牵着走（探测 3600s + 余额 5 分钟 = 实际一小时才刷，用户设的数字静默失效）；③ 整轮套 `timeout(period)`，余额查询会吃掉探测预算，超时后打出的是「**健康探测**一轮未完成」—— 指错方向的告警。`lib.rs` 里 `flush_usage_if_dirty` 当初正是为①②同一理由才独立起了一趟线程 | **高** | ✅ 已修：`balance_gate::spawn_background` 独立一趟，固定 60s 检查节奏；注入⑪⑫均变红 |
| A6-3 | 可观测性 | `balance_gate.rs::record_outcome` | **事件环洪水**：后台每轮每条 Key 无条件落一条事件，而 `MAX_EVENTS = 500` 且与路由/故障转移共用。折叠救不了（`append_event_collapsible` 只合并**紧邻**的上一条，多条 Key 同轮穿插、跨轮挨不上）。6 条 Key × 用户设的 1 分钟 = 360 条/小时 → 一个多小时把整环冲干净，把排障真正需要的事件挤出去（缺陷分类法第 7 类）。此前受「用户得开着那个分类页」天然限速，改成后台刷新才成为可能 | **中** | ✅ 已修：新增 `Trigger::{User,Background}`，后台只在**结论变化**时落；注入⑬变红 |
| A6-4 | 正确性 | `upstream/sse_error.rs` 模块头 | 声称「Chat 下游仍发 `[DONE]`」—— 那是**翻译器这一层**的事实，而 `proxy.rs` 流末有一道更宽的门（原始尾窗认任何非 null 的 `error`）先 `return None`、压根不调 `finish()`，生产路径上发不出去 | 低 | ✅ 已修文案（两处代码**刻意都不改**：门堵的是「假成功」，价值远大于一个终止符） |
| A6-5 | 可维护性 | `proxy.rs:2107` / `:1640` | 两处注释指向**已经做完**的待办（「需在 sse.rs 六个方向各加一条…见 docs 待办」）与**已不存在**的限制（「跨协议 SSE 翻译属已知限制」） | 低 | ✅ 已修（行数不变，`proxy.rs` 仍精确停在 3103） |
| A6-6 | 可维护性 | CLAUDE.md / docs/14 §21.3 / `ratchet.json` `$rule` | 「P2-1/P2-7/P2-8 + `AppSettings` 拆分刻意不做」是一次**文档回归**：docs/15 第三批 2026-08-06 已全部补做，§12.4 记对了，08-27 整理清单时又从更早来源转述回「刻意不做」。这类清单的用途就是指挥别人不要动手/去动手，错了直接制造重复劳动 | 中 | ✅ 三处均已更正并附代码取证 |
| A6-7 | 兼容性 | `i18n.ts` `balance.intervalHint` | **用户可见文案变成假话**：「窗口不可见时不查」—— 后台刷新之后不成立 | 中 | ✅ 已修（zh/en），并把「0 = 余额也不参与路由」这个隐含依赖明说出来 |
| A6-8 | 可维护性 | 四处文档 | 「17 个冻结文件」实际 **16**（`mockData.ts` 抽出 events 后降到 819、掉出 frozen）；`i18n.ts`「241×2」实际各 **621**；`bridge.ts`「74 处」、`lib.rs`「79 个命令」均过期 | 低 | ✅ 已改成**指向门**而不是抄数字（门自己会打印） |
| A6-9 | 数据安全 | 全仓生产段 | 「敏感词 × 输出汇」交集扫描：**只剩 2 处**，都是 `format!("Bearer {secret}")` 构造鉴权头（必须的）。`check:secrets` 扫 319 文件 / 427 段 base64 零命中；`audit:release` 五项全过 | —— | ✅ 无缺陷 |
| A6-10 | 稳定性 | 新增 10 个模块 | 锁跨 `await` 扫描：`balance_gate` 两处取锁都在 `{}` 块或 `let-else` 的语句内结束（`read_lan_token_from` 那次 RwLock 自锁的教训）；`in_flight` 去重集合由 `scopeguard` 保证移除（panic unwind 也走） | —— | ✅ 无缺陷 |

### 修复建议

R4（B4）三条已当场修完，见「状态」列。另有一条**刻意不修、只记边界**：

- **已知不对称**：后台刷新只扫 `enabled_key_ids`，故「已禁用 + 勾了允许大脑聚合」的 Key
  不在后台刷新范围内（它确实在被聚合调用、烧额度）。不修的理由是职责边界 ——
  本模块给**路由**供数，而聚合不走 `candidates_for`。代价（托盘常驻时这类 Key 余额偏旧）
  与正确的改法（判据应是「是否被某个分类的聚合选中」，而不是顺手把 `allow_in_aggregate`
  加进来）都写进了模块头。同「健康探测仍只对 `enabled` 发」那条已记录的不对称。

### 复审结论

| 编号 | 初审描述 | 修复情况 | 复审结论 |
|------|---------|---------|---------|
| A6-1 | 判据顺序把上游「号废了」当成查询失败 | 两支对调 + `dead` 夹具复刻 `balance.rs` 真实形态 | **已解决**（注入⑤变红） |
| A6-2 | 后台刷新搭健康探测的车 | 独立线程 + 固定 60s 检查节奏；判据同时钉住正反两面 | **已解决**（注入⑪⑫变红） |
| A6-3 | 后台每轮每 Key 落事件冲垮 500 条环 | `Trigger` 区分「用户正看着」与后台；后台只在结论变化时落 | **已解决**（注入⑬变红） |
| A6-4/5/6/7/8 | 五处文案/注释/文档与代码分叉 | 逐处按事实改写；数字改成指向门 | **已解决** |
| A6-9/10 | 数据安全与并发扫描 | —— | **无缺陷**（有取证） |

**判据总账**：B4 累计 **13 条注入全部变红**（10 条实现期 + 3 条审查期），
含 4 条源码级接线判据。全轮基线：`cargo test --lib` **954 passed / 0 failed / 6 ignored**、
clippy 零警告、`tsc --noEmit` 干净、`npm test` **143 passed / 20 文件**、`npm run gates` 全绿、
**棘轮零抬高且三项下调**（`lib.rs` 2076→**1895**、`proxy.rs` 3105→3103、`i18n.ts` 1475→1469）。

### 提测建议

**可提测 / 可出包。** 高风险项 0（A6-1、A6-2 已修并注入验证），中风险项均已闭环。

🔴 **但发版前仍有一类单测在原理上证明不了的事**：docs/14 §21.2 的真机验证清单
（§8 的 15 条 + §8.2 的 20 条 + C-18 + C-19~C-23。⚠️ 这里的 C-23 指 **B4 余额闸门**；2026-09-04 大脑聚合那批曾重号占用 C-19~C-23，已于 2026-09-08 让位成 C-28~C-32）。其中 **C-23 是本轮 B4 的专项** ——
它要真实上游报 0、真实代理在跑，才能证明「闸门确实生效、后台确实在刷」。

## A5 2026-08-15（复审 · 已通过）

### 审计范围

本轮改动：**输出 token 上限透明化**（两次定调）。
- 08-14 代理侧：`proxy::apply_key_params` 不再注入 `max_tokens`；
- 08-15 大脑聚合侧：撤掉三条路径的 `key.params.max_tokens.unwrap_or(4096)`，
  改为新模块 `upstream/budget.rs` 按协议与模型能力计算。

审查方式：六个维度（正确性 / 兼容性 / 稳定性 / 安全性 / 可观测性 / 可维护性）并行深审，
每条发现交**独立复核者对抗式验证**（默认立场「它不成立」），只保留推翻失败的。
不只看 diff —— 沿真实调用链追到协议转换、工具循环、UI/文档契约。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A5-01 | 兼容性 / provider-break | `upstream/budget.rs` `output_budget` | 预算只按 `contextWindow − 输入` 算，**未按模型最大输出钳制**。内置预设给 Claude 4.5 写 200k 窗口，而其最大单次输出是 64k；短 prompt 算出近 199k `max_tokens` → 官方与严格中转直接 400。**上下文窗口 ≠ 最大输出**。影响成员 / 汇总者 / 决策者全部 Anthropic 聚合路径 | 高 | 已修 |
| A5-02 | 正确性 | `upstream/budget.rs` `output_budget` | 输入占满窗口时 `saturating_sub(..).max(256)` 仍硬发 `max_tokens:256`，「输入+输出」必然超窗口 → 上游 context overflow 400。既烧额度，又把**本地已经知道**的原因藏在上游错误里 | 高 | 已修 |
| A5-03 | 兼容性 | `upstream/convert.rs` `openai_to_anthropic` | 跨协议时凭空补 `max_tokens: 4096`。OpenAI 客户端本来没设上限（协议允许省略），转到 Anthropic Key 后被本地按 4096 截断 —— 直接违反 08-14 定调，且用户只会以为是中转商的问题。流式与非流式两条路径都可达 | 高 | 已修 |
| A5-04 | 正确性 | `upstream/session.rs` `estimated_input_tokens` | 图片 base64 被当**文本** token 计。一张允许的 5MB 图片 base64 后约 670 万字符 → 估成数百万 token → 预算塌到底。base64 是传输编码，模型按图像尺寸计视觉 token，二者差几个数量级 | 高 | 已修 |
| A5-05 | 正确性 | `upstream/budget.rs` `estimate_tokens` | 一律 `chars/2` 对 CJK 是**低估**（注释自己写的是 1~1.5 字符/token，除以 2 等于 0.5 token/字符）。低估让 `max_tokens` 偏大 → 超窗口 400 | 中 | 已修 |
| A5-06 | 兼容性 | `upstream/convert.rs` `anthropic_to_openai` | 反向转换同样凭空补 4096，而 OpenAI 侧该字段本是可选的 | 中 | 已修 |
| A5-07 | 可维护性 | `upstream/convert.rs` thinking 分支 | thinking 预算从同一个 4096 兜底值推导，A5-03 修一半时会残留固定语义 | 中 | 已修 |
| A5-08 | 可维护性 | `src/components/KeyEditor.tsx` | Max Tokens 标签硬编码，新增的「当前不生效」i18n 文案**从未被渲染**，只有 hover title 说明 → 字段看起来仍然生效 | 中 | 已修 |
| A5-09 | 可维护性 | `src/lib/i18n.ts` `editor.errMaxTokensZero` | 校验文案称「上游会直接拒绝请求」，而该字段已不参与任何请求 —— 教用户一个不存在的因果 | 低 | 已修 |
| A5-10 | 可维护性 | `docs/14` §16.2–16.3 | 文档仍写「`max_tokens` 保留给 `aggregate.rs`、聚合有两处读取」，与同一轮的代码和 docs/01 自相矛盾 | 低 | 已修 |
| A5-11 | 可观测性 | `upstream/completion.rs` 解析器 | `finish_reason:"length"` / `stop_reason:"max_tokens"` 被当**完整成功**。省略上限避开了本地截断，但上游默认上限仍可截断，而用户与日志都看不出来 | 中 | **未修**（见提测建议） |
| A5-12 | 可观测性 | `upstream/convert.rs` 响应转换 | 跨协议响应把 Responses `status:"incomplete"` 硬写成 `completed`、Chat `length` 丢失 → 下游拿到「正常结束」的假信号 | 中 | **未修** |

### 修复要点

**A5-01 / A5-02**：新增 `budget.rs::anthropic_required_max_tokens`，取
`min(窗口剩余, 模型最大输出)`；模型能力表 `CLAUDE_MAX_OUTPUT_TABLE` 按家族片段
`contains` 匹配（第三方中转普遍加前后缀，全等会把它们全判成未知而误伤主场景），
表序从新到旧以免 `claude-3-7` 被 `claude-3` 抢先命中。
数据缺失分三级：缺窗口但模型可辨识 → 照常按能力值参与；模型能力不可辨识 → 报可行动错误；
输入占满窗口 → 报可行动错误且**不发请求**。

**A5-03 / A5-06 / A5-07**：两个转换器改成「源请求真有才带」，Anthropic 必填字段由
`proxy::ensure_anthropic_output_budget` 在**转换之后**补 —— 只有代理层同时握有
`ProviderKey` 与已解析的 `real_model`，转换器里补必然只能瞎填。thinking 分支改为
只在已有真实上限时才据其计算。**客户端显式给的值优先**，不被能力计算覆盖。

**A5-04**：新增 `estimate_json_tokens_without_image_transport`，按 JSON 结构遍历，
识别 Anthropic `source.data` 与 OpenAI `data:image/...` URL，每张替换为固定视觉占位。

**A5-05**：`estimate_tokens` 改为混合上界 —— ASCII 按 4 字符/token，非 ASCII 逐字符算 1 token。
低估的代价（400）比高估（少留输出空间）严重得多。

### 复审结论

| 编号 | 初审描述 | 修复情况 | 复审结论 |
|------|---------|---------|---------|
| A5-01 | 未按模型最大输出钳制 | 双钳制 + 能力表 | 已解决 |
| A5-02 | 满窗口仍硬发最小值 | 改为本地报错且不发请求 | 已解决 |
| A5-03 | 跨协议补 4096 | 转换器不再造值，代理层按能力补 | 已解决 |
| A5-04 | 图片 base64 当文本计 | 结构化估算 + 视觉占位 | 已解决 |
| A5-05 | CJK 低估 | 改取上界 | 已解决 |
| A5-06 | 反向转换补 4096 | 改为源有才带 | 已解决 |
| A5-07 | thinking 依赖 4096 | 改为依赖真实上限 | 已解决 |
| A5-08 | 标签未渲染 i18n | 改用 `t("editor.maxTokens")` | 已解决 |
| A5-09 | 校验文案讲错因果 | 改为「仅为旧配置格式有效」 | 已解决 |
| A5-10 | 文档自相矛盾 | §16.2 加「已被取代」勘误框 | 已解决 |
| A5-11 | 截断被当成功 | — | **降级接受**（需独立一轮，见下） |
| A5-12 | 跨协议擦掉 incomplete | — | **降级接受** |

### 验证证据（A5）

- `cargo test --lib`：**645 passed / 0 failed / 3 ignored**
  （排除 2 条 clean HEAD 即已失败的 Windows 硬链接测试，已另开任务）。
- `cargo clippy --lib --all-targets`：**无新增警告**（仅 4 条 baseline `dead_code`）。
- `npx tsc --noEmit` 通过；`npm run build` 通过。
- 故障注入：把 `unwrap_or(4096)` 加回聚合路径 → 三条测试同时变红，
  OpenAI 那条失败信息里可见混进请求体的 `"max_tokens":4096`；已撤销注入并复跑通过。
- 新增回归：能力钳制、满窗口拒绝、未知能力拒绝、跨协议按能力补、客户端值优先、
  转换器不造值（两向）、图片 base64 有界、CJK 上界。

### 提测建议

**待修复（阻断项：A5-11、A5-12）** —— 但阻断的是「可观测性」而非本次功能本身：

1. A5-11 / A5-12 属于**同一类**问题（终止原因未被保留与呈现），修它要动
   `completion.rs` 返回结构、`convert.rs` 响应映射、`RequestTrace` 字段与 LogsPage 展示，
   是独立一轮的工作量。当前状态下「上游默认上限造成的截断」对用户不可见 ——
   这与本轮「让截断不再隐形」的目标同向但未达成。
2. 本轮功能改动（A5-01~A5-10）已可提测，但**必须先做真机验证**：
   单元测试只断言请求体形状，没有验证真实中转商是否接受动态 `max_tokens`。
3. 模型最大输出仍是代码内置表，用户无法为第三方模型补该能力。若真机遇到「模型能力
   无法辨识」被拒，正解是给 `ModelInfo` 加 `maxOutputTokens` + UI，**不是**回退默认值。

---

## A4 2026-08-08（代码审查 + 业务逻辑审查）

本轮**不看 diff**，按「新功能三件套（用量统计 / 健康告警 / 路径可视化）的业务语义是否成立」
逐条追。修出 3 条，每条都做过故障注入（去掉修复，测试必须变红）。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A4-01 | 数据正确性 | `src-tauri/src/store.rs` `token_usage_by_key` | 用量总量从 **500 条事件环形缓冲区现算**，环满后旧条目被挤出，总量**停止增长**。实测 before=5000 / after=5000 —— 第 500 条之后的请求在用量面板里完全不可见。用量统计是本轮新功能的主交付物，等于功能不可用。 | 高 | 已修 |
| A4-02 | 告警可靠性 | `src-tauri/src/health.rs` `record_live_failure` | 用 `breaker_until.is_some()` 判「此前是否已熔断」，但窗口**自然到期**时该字段仍是 `Some(过去时刻)`（只有真实成功才清空），于是重新武装时 `now && !was` 恒为 false —— **第二次及以后的熔断永远不发通知**。 | 中 | 已修 |
| A4-03 | 静默失效 | `src-tauri/src/notification.rs` | `show()` 的结果被 `let _` 丢弃，整个健康告警可以静默失效而无人知。另：模块 `#![cfg(not(test))]` 的既有因果注释**实测为假**（见下）。 | 中 | 已修 |

### 修复要点

**A4-01**：`Store` 增设会话级累加器 `usage_totals`，在**唯一写入点** `append_event_full`
累加，与环形缓冲区彻底解耦；读路径直接取累加器。折叠事件与流式补记都走同一累加点，
不重不漏。语义是「自本次启动以来」——已同步改 `UsagePage` 文案与 i18n，不再暗示是全量历史。

**A4-02**：抽出 `breaker_window_active(until, now)` 统一口径为「窗口此刻是否还活着」。
`record_live_success` **刻意保留** `is_some()` 口径并注明理由：残留 `Some → None`
精确对应「这个 Key 真的恢复了」；若也按窗口算，「窗口到期后第一次成功」就发不出恢复回执，
而用户手上最后一条正是熔断告警，不给回执会让告警永远悬着。两处口径**不同是设计**，
不是分叉——已在两侧注释互相指认。

**A4-03**：`show()` 失败改落 `warn` 日志。同时推翻既有注释的因果：
`tauri_plugin_notification::init()` 在 `run()` 里**没有 cfg 门**，插件 crate 在
`cargo test` 下照常链接，`#![cfg(not(test))]` 拦不住链接、只拦调用点；临时去掉该 cfg 后
`cargo test --lib` 仍 **580 passed** 不复现启动崩溃。保留 cfg（测试进程不该弹系统框），
但注释改成实测结论，避免下次撞同类崩溃时在错误的一行上找答案。

### 复审为「不是缺陷」的项（避免下轮重复劳动）

| 检查项 | 结论 |
|---|---|
| `CategoryPage` 熔断告警条用 `Date.now()` 但 memo 只依赖 `[keys]` | 不是缺陷。`refreshCategory` 每 30s 从 IPC 取**新数组**且无相等性守卫，`keys` 引用必变 → memo 必重算，窗口到期后 ≤30s 自然消失 |
| `mcp.rs` 端口文件读写路径 | 已统一走 `mcp_port_file_path()`，mac → `~/Library/Application Support/SynaRoute/`，Windows → exe 同级（MSIX 虚拟化判据）。无漂移 |
| `strip_trace` 是否漏 `key_name` | 已逐字段保留，且有 `strip_trace_drops_body_keeps_every_other_field` 钉住 |
| `usage_totals` 与 `events` 的锁序 | 两处写路径均为 `usage_totals` → `events`，同序，无反转；`emit` 在锁外 |

### 双平台测试基线（v0.1.15，已逐条核对差集）

| 平台 | 通过数 | 取证 |
|---|---|---|
| Windows | **583 / 0 failed** | 本机 `cargo test --lib`，连跑 3 次 + `--test-threads=1` 串行全量均全绿 |
| macOS (arm64) | **579 / 0 failed** | GitHub run `31287262683`，22 步全绿 |

**两边差 4 条，全部是刻意的平台门控，不是漏跑。** 已把两侧测试名做差集逐条核对
（mac 侧从 run log 抓到 579 个唯一名，与报告数一致，故差集可信）：

- 仅 Windows 有 6 条：4 条硬链接防线测试（`#[cfg(windows)]`，Windows 特异的 ADS 语法与
  8.3 短名别名）+ 2 条托盘图标灰度派生测试（`#[cfg(not(target_os = "macos"))]`——
  mac 托盘走 template 图标，系统接管 alpha，不存在灰度派生这条路）。
- 仅 macOS 有 2 条：`unix_hardlink_policy_*`（Unix 的 `nlink` 语义，且必须豁免目录——
  APFS 上目录 `nlink` 天然 > 1）。

算术闭合：583 − 6 + 2 = 579。**下轮接手别把这 4 条差额当缺陷去查。**

### 验证证据（A4）

- `cargo test --lib`：**580 passed / 0 failed**，连跑 3 次全绿；
  `--test-threads=1` 串行全量同为 580 / 0（93.86s）。
- `cargo clippy --lib --all-targets`：零警告。
- `npx tsc --noEmit` 通过；`npm run build` 通过。
- 故障注入：A4-01 去掉累加器改回现算 → 测试红；A4-03 去掉 `#![cfg(not(test))]` → **仍绿**
  （即该 cfg 不是崩溃的解药，结论已写回注释）。

---

## A3 2026-08-08（0.1.14 基线回填）

### A2 → A3 变更摘要

### 审计范围

本轮不只审查 diff，按 Windows / macOS 两条完整链路追踪：

1. **上下文窗口与 1M 能力**：`KeyEditor` 数字输入 + token/K/M 单位 → 前端 `ModelInfo.contextWindow` → Tauri IPC → Rust `Option<u32>` → 配置落盘 → 实际模型解析 → `anthropic-beta: context-1m-2025-08-07` → Claude CLI / 桌面端 `supports1m`。
2. **密钥存储**：Windows DPAPI + 明文缓存失效；macOS Keychain 库密钥 + AES-GCM；主口令模式；迁移、备份、回滚、锁定态、便携导入导出。
3. **路径与客户端配置**：Windows MSIX 虚拟化、macOS Application Support / Logs、MCP 端口文件、Claude CLI / 桌面端 / Codex 三端写入与回滚、只读工具路径防线。
4. **生命周期与托盘**：Windows 关窗到托盘、macOS template 图标、`Cmd+Q`、`RunEvent::Exit` 清理、自启动、启停代理与客户端配置一致性。
5. **构建与更新**：NSIS、macOS `.app`/`.dmg`/updater、临时签名、artifact 权限、最低系统版本、Hardened Runtime、绿色 CI 是否吞错误。
6. **验证基线**：Windows Rust 全量测试 / clippy，前端 TypeScript + Vite；macOS GitHub runner 测试与六条产物判据。

### 风险清单

| 编号 | 类型 | 位置（文件:行） | 描述 | 级别 | 状态 |
|------|------|----------------|------|------|------|
| A2-01 | 数据安全 / 并发 | `src-tauri/src/secret.rs:665-690` | macOS Keychain 首次初始化旧实现只用 `OnceLock::get/set`，同进程两个转发线程可各自生成并写入不同库密钥；后写者覆盖 Keychain，前写者已生成的密文重启后永久不可解。单实例插件不防同进程线程。 | 高 | 已解决 |
| A2-02 | 安全覆盖 | `src-tauri/src/agent_tools.rs:343-365, 1234+` | Unix 已改为 `nlink>1` fail-closed，但原有硬链接防线测试均为 Windows 专属；删除 Unix 防线后 mac CI 仍可能全绿。 | 中 | 已解决 |
| A2-03 | 发布兼容 | `src-tauri/tauri.conf.json:40+`（当前工作区） | 旧 `Entitlements.plist` 给宿主进程开启 JIT / unsigned-executable-memory；普通 WKWebView 的脚本在系统 WebContent 进程执行，宿主不应无依据扩大 Hardened Runtime 例外。 | 中 | 已解决（删除过宽权限） |
| A2-04 | 测试覆盖 | `src/components/KeyEditor.tsx:858-1055` | token/K/M 状态机有大量边界防线（u32 上限、十进制 BigInt、原生 badInput、保存阻断），但无独立前端自动化测试；当前仅有 TypeScript/Vite、纯函数向量人工执行与 mac 构建证据。 | 低 | 接受；建议后续抽纯函数测试 |
| A2-05 | 发布就绪 | GitHub Releases / Apple Developer ID | macOS CI 的 updater `.sig` 使用一次性临时私钥，与应用内 pubkey 不匹配；未做 Developer ID 签名和公证。测试安装包可用，正式自动更新和公开分发不可用。 | 中 | 待真机/证书，非 Windows 阻断 |
| A2-06 | 真机覆盖 | macOS 运行时 | CI 验证产物结构，不验证 Gatekeeper、Keychain 授权弹框、菜单栏视觉、dmg 拖拽、Claude 桌面端实际目录。 | 中 | 待真机；不伪报已完成 |

### 关键链路复审结论

#### 1. contextWindow / 1M：已通过

- 前端存储的始终是最终整数 token 数；单位只影响输入和显示。
- `tokensFromAmount` 用十进制字符串 + `BigInt` 换算，避免 `1.000001M` 的浮点误判；上限与 Rust `u32` 一致。
- 原生 `<input type=number>` 的 `badInput` 单独建模，非法中间态阻止保存，不静默清空旧值。
- 无效模型按 `realName` 记录；模型去重已在手动新增 / 预设导入入口完成；删除模型同步清理无效集合。
- `effective_beta_header` 只对 Anthropic 上游、按本次解析后的真实模型 `contextWindow >= 1_000_000` 追加；流式与非流式共用 `apply_upstream_headers`。
- 客户端原有 beta 头保留且去重；OpenAI 协议不新增 1M beta。
- 桌面端 `supports1m` 只在主 Key 对外名能明确解析到 Mapping/Tier/Native 且窗口有依据时断言；兜底模型不冒充能力。
- Windows/macOS 共用同一 React 前端和 Rust 代理代码，单位输入无需分平台复制。

#### 2. Windows 密钥链：已通过

- 默认模式走 DPAPI；密钥不随配置对象下发。
- 解密缓存由 `set/remove/lock/unlock/三次整库迁移` 全部清空；行为测试覆盖。
- 落盘失败回滚内存；密钥保存先写库、再置 `has_secret`，避免 UI 标记领先密钥库。
- `%APPDATA%` 的 MSIX 虚拟化仍按项目硬规则处理，日志和 MCP 端口的共享判据未被 mac 适配破坏。

#### 3. macOS 密钥链：高风险并发项已修

- 默认模式走 Keychain 托管 32 字节库密钥 + AES-256-GCM。
- 新增 `cached_or_try_init_copy`：Mutex 串行首次成功初始化、锁内二次检查、失败不缓存。
- 32 线程 barrier 测试钉住“初始化只执行一次、所有线程拿同一值”；另测首次拒绝后可重试成功。
- Keychain 真机授权/拒绝路径仍需实际 Mac 验证，报告不把编译通过冒充运行通过。

#### 4. 路径与只读工具：已通过（Unix 补强）

- Windows：硬链接别名枚举，只有存在敏感名字才拒。
- Unix/macOS：标准库不能从 inode 反查所有路径，`nlink>1` fail-closed；已补普通文件放行、硬链接双向拒绝、图片入口复用防线测试。
- macOS 日志走 `~/Library/Logs/SynaRoute`，运行数据/MCP 端口走 `~/Library/Application Support/SynaRoute`；Windows exe 同级判据保留以避 MSIX 虚拟化。
- 三端配置写路径分离，回滚与备份逻辑未发现新的跨平台分叉。

#### 5. 生命周期与托盘：已通过

- Windows `CloseRequested`：阻止退出并隐藏到托盘。
- macOS：图标设置 template，运行状态移到 tooltip；不再用彩色/灰度表达不可见的状态差。
- `Cmd+Q` 绕过 `CloseRequested/ExitRequested`，但 runtime-wry 把 `LoopDestroyed` 映射成 `RunEvent::Exit`，日志与健康态清理仍执行。
- 托盘启停代理复用工具配置写入/还原语义；失败后重建托盘，避免假勾选。
- 自启动由 Tauri 插件适配 Windows Run 键 / macOS LaunchAgent；系统副作用失败与落盘失败均有回滚编排测试。

#### 6. 构建与更新：测试包通过，正式发布未通过

- Windows：NSIS 路径未改，现有部署硬规则仍要求 `tauri build` + 前端 chunk 嵌入判据。
- mac CI：`set -o pipefail` 防 `tail/tee` 吞非零码；`continue-on-error` 后有显式失败汇总步骤。
- `.app` / `.dmg` / updater `.app.tar.gz + .sig` 均有存在性判据；tar 真解一次检查主程序可执行位，避免 upload-artifact 丢权限。
- mac CI 使用临时 updater 私钥，只证明打包链，不能证明真实更新签名；Step Summary 已明示。
- 旧 JIT entitlements 删除是权限收敛：Apple 要求只给实际执行 JIT 的目标开例外，系统 WKWebView 的 WebContent 进程不由宿主 entitlement 授权。

### 修复建议

1. **已实施**：macOS Keychain 首次初始化增加 Mutex + 锁内二次检查，失败不缓存。
2. **已实施**：Unix `nlink>1` fail-closed 增加 2 条 mac/Linux 专属行为测试。
3. **已实施**：删除无依据的宿主 JIT entitlements 及 tauri 配置引用。
4. **建议后续**：把 `tokensFromAmount/preferredUnit/amountForUnit` 抽到独立模块，补 Vitest 表格化测试；不阻断当前提测。
5. **正式 mac 发布前必须**：Developer ID 签名 + notarization；使用生产 updater 私钥生成 `.sig` 并生成匹配 `darwin-aarch64` 的 `latest.json`；真机验证安装和更新。

### 复审结论

| 编号 | 初审描述 | 修复情况 | 复审结论 |
|------|----------|----------|----------|
| A2-01 | Keychain 初始化并发分叉 | Mutex + 二次检查 + 32线程测试 + 失败重试测试 | 已解决 |
| A2-02 | Unix 硬链接防线无测试 | 新增 Unix 普通/硬链接/图片入口测试 | 已解决；待 mac CI 实跑 |
| A2-03 | 宿主 JIT 权限过宽 | 删除 plist 与引用 | 已解决；权限收敛 |
| A2-04 | 单位状态机无自动单测 | 保留现有边界防线；TypeScript/Vite 与纯函数向量通过 | 降级接受（低） |
| A2-05 | mac 正式签名/更新签名未就绪 | 未配置生产证书/私钥 | 未解决，正式发布阻断；不影响 Windows 与 unsigned 测试包 |
| A2-06 | mac 真机运行未验证 | 无真机证据 | 未解决，必须实测 |

### 验证证据

- Windows Rust：`576 passed / 0 failed`（v0.1.14，含 Keychain 并发测试 + Unix 硬链接测试）；
  `--test-threads=1` 串行全量同为 `576 / 0`。
- Keychain 首次初始化核心测试：连续 20 轮全绿。
- Windows clippy：`cargo clippy --lib --all-targets` 零警告。
- 前端：`tsc && vite build` 通过，1637 modules transformed。
- macOS 基线（v0.1.14）：GitHub run `31242242072`，`572 passed / 0 failed`；
  六条产物判据（minOS 11.0 / icns / arm64 / 前端 chunk / dmg 8.7M / updater 9.0M+sig）全通过。
- **回归教训（已闭环）**：Unix 硬链接 fail-closed 未豁免目录（macOS 目录 nlink 天然 2~6）→
  3条测试失败；已改为仅对常规文件判定，mac CI 复验通过。通知插件 WinRT delay‑load 导致
  `cargo test` 二进制 `STATUS_ENTRYPOINT_NOT_FOUND`；修复为 `notification.rs` 整体
  `#[cfg(not(test))]` 隔离 + `app_handle` `#[allow(dead_code)]`。
- **0.1.13 → 0.1.14**：`app_handle` `#[cfg(not(test))]` 意外破坏函数签名造成 mac CI 编译
  失败；修正为 `#[allow(dead_code)]` 并重新构建 Windows/macOS 产物。

### 提测建议

- **Windows：可提测**。本轮未发现高/中风险未处置项。
- **macOS unsigned Apple Silicon 测试包：代码层可提真机测试**；仍需实际 Mac 验 Gatekeeper、Keychain、目录落点、菜单栏和 dmg。
- **macOS 正式公开发布：待修复（阻断项：A2-05、A2-06）**。

### 信息来源

- [Tauri v2 macOS signing](https://v2.tauri.app/distribute/sign/macos/)
- [Tauri v2 macOS application bundle / entitlements](https://v2.tauri.app/distribute/macos-application-bundle/)
- [Apple Hardened Runtime](https://developer.apple.com/documentation/Security/hardened-runtime)
- [Apple WKProcessPool（WKWebView 多进程）](https://developer.apple.com/documentation/webkit/wkprocesspool)
