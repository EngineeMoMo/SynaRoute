# 14 · 交接与待办清单

> 交接时间：2026-07-31 08:10（+08:00），**08:40 补录第三节与第六节第 1 条**
> 交接自：Windows 主机（`F:\soft\SynaRoute` 为安装目录）
> 最后提交：`06aa63a`（含 `c83022a` 六缺陷修复、`a2cf95d` cc-switch 导入、`f353170` 跨协议工具调用修复）
>
> **2026-08-01 更新（第二轮）**：审查阶段完成，五路并行审查 + 自审 lib.rs/tools/store，
> 共修出 **1 个 P0（切主题会把刚关掉的开机自启动重新装回系统）**、1 个 P1（路径遏制有两条绕过）、
> 2 个 P2（`Retry-After` 取最大值拖垮整池、硬错误被包装成「过载请重试」）、
> 若干 P3 与前端一致性问题。另做了**效率整治**：日志列表原先每 2s 搬最坏 19 MB（trace 正文），
> 已改按需拉取，降 99.5%。**当前版本 v0.1.18**（见版本变更记录与 docs/16 安装指南）。
> 当前基线：`cargo test --lib` **588 passed / 0 failed**（2026-08-09，含 FR-029 的 4 条；
> `--test-threads=1` 串行全量同为 588）；`tsc --noEmit` exit 0；clippy 零警告。
> FR-029 / FR-030 已出包为 **v0.1.18**（`F:\SynaRoute` 与 Release 均已更新，签名一致性已验证）。
> 三个新文件（`crypto.rs`、`portable.rs`、`UpdateBanner.tsx`）**已 `git add`**。
>
> **2026-07-31 换机后更新：第二节 P0、第三节测试不稳定、第四节 P1 三条已全部修完并验证；
> 「尚未开发的功能」四项也已全部处理完**（FR-021 导入导出、FR-022 托盘两处缺口、
> 主口令增强模式均已实现；docs/13 的移植只剩一次真机实测，刻意不动代码）。
> 单线程全量（`--test-threads=1`）同样全绿。

本文件是给**换机后接手的人（或 AI）**看的。读完这一篇即可继续，无需回溯聊天记录。

**新会话最省时间的读法**：第八节（真机验证清单）→ 第四节（各功能的实现判据与刻意取舍）。
第二、三节的缺陷已修并打包到 0.1.14。第六节第 1 条（曾经"仍未解决"）已在 2026-08-08 的
0.1.14 安装包中全部覆盖。

> 2026-08-01 追加：大脑聚合的**多模态（图片）与只读工具调用**已实现（FR-026 / FR-027），
> 见第一节末的专节；真机验证清单因此新增第 14、15 条。

---

## 一、这一轮做完了什么

| 提交 | 内容 | 状态 |
|---|---|---|
| `6928f4c` | 桌面端 MCP 改 stdio、**「全部 Key 失败」短路窗口**、日志目录探针竞态、健康探测图标 | 已验证；短路窗口的测试串台副作用已在换机后修掉（第三节） |
| `f353170` | 六条跨协议通道的工具调用透传（流式 4 向 + 非流式 2 个函数）、stop_reason/finish_reason 按有无工具切换、tool_choice 双向转换、空探测拦截、Codex 预览脱敏 | 已验证 |
| `a2cf95d` | 从 cc-switch 导入历史 Key（只读对方 SQLite、导入后不接入） | 已验证 |
| `c83022a` | 三端链路回归审查修出 6 处缺陷（下详） | 已验证 |
| `06aa63a` | 本交接文档 | — |
| 换机后（未提交） | 备份首写即锁 + 还原后删 `.bak`、短路窗口按代理实例隔离 + 启停即清、桌面端还原改原子、429 `Retry-After` 透传 + 全失败改 529 | 已验证（283 tests，连跑 5 次全绿） |
| 换机后（未提交） | **桌面端模型名判据**：新增 `model::is_desktop_acceptable_model_id`（1:1 复刻 `app.asar` 的 `sD()`），保存桌面端 Key 时无条件拒绝不合规对外名，接入时兜底强警告（见第九节） | 已验证（294 tests） |
| 换机后（未提交） | **补完第五节审查**：`parse_and_apply` 加 canonicalize 路径防线（堵符号链接逃逸）、`save_brain`/`save_settings` 改走 `mutate_and_persist`，+10 个测试 | 已验证（304 tests，连跑 5 次全绿） |
| 换机后（未提交） | **P2 九条处理完**：#5 `supports1m` 按 contextWindow 判定、#7 cc-switch 接管检测 + UI 明示、#8 加 `anthropicFamilyTier`/`isFamilyDefault`、#6 复核确认早已解决并补护栏；#4 保持对齐 cc-switch、#9 未动（见第四节 P2 表） | 已验证（311 tests，连跑 5 次全绿） |
| 换机后（未提交） | **FR-025 开机自启动接线**（原是个静默失效的开关：字段存在、UI 能点、值落盘，但后端从无代码注册自启动项）。接 `tauri-plugin-autostart`，见第十节 | 已验证（312 tests，连跑 5 次全绿） |
| 换机后（未提交） | **FR-021 配置导入/导出**（原是设置页两个 `disabled` 按钮）。新增 `crypto.rs`（Argon2id + AES-GCM 口令信封）+ `portable.rs`（导出/校验/两种导入模式），3 个 IPC 命令 + 两个对话框 UI。见第四节 | 已验证（333 tests，连跑 5 次全绿）；跨机往返待真机验（第八节第 9 条） |
| 换机后（未提交） | **FR-022 托盘补齐**（需求验收标准里明写、标 P0）：图标按代理运行状态灰度化派生、「代理」子菜单三分类启停（语义与界面按钮一致：起=顺带写工具配置、停=顺带还原）、「主 Key」子菜单快切；重排规则收归后端 `Store::set_primary_key` 做单一事实来源，前端改调它。见第四节 | 已验证（343 tests） |
| 换机后（未提交） | **主口令增强模式**（FR-018 可选增强，原是 `disabled` 开关 + 零使用点字段）。`crypto.rs` 加长驻密钥 API（解锁派生一次、各条只做 AES-GCM）+ 校验串；`secret.rs` 双模式 + 三个整库迁移操作（先全解成功→备份→写盘→失败回滚）；四合一对话框 UI + 锁定态常驻警告条。见第四节 | 已验证（**368 tests，连跑 5 次全绿；`--test-threads=1` 全量也全绿**），5 处做过故障注入双向验证 |
| 换机后（未提交） | **docs/13 现状回填**（那份文档的差距表写于 `2f51fa8` 前、已过时；三项已完成）。写明唯一剩余的未知项与实测步骤，**刻意不动代码** | 文档改动 |
| 2026-08-08（v0.1.14 已发布） | FR-028（用量统计 / 健康告警 / 路径可视化）/ 流式用量采集 / 安全复审 / 热路径免克隆 / macOS Keychain 并发修复 / 上下文窗口数字输入 + token/K/M 单位选择。基线：576/572 passed，clippy 零警告。 | macOS 真机发布与验证 |
| 2026-08-09（v0.1.15 已发布） | **A4 轮审查修出 3 条**：用量统计满环即冻结（面板数字停止增长）、熔断通知口径分叉（第二次及以后的熔断永远静默）、通知发送失败被 `let _` 丢弃。**用量累计改周期性落盘**（新增 `usage.json`，60s 合并写 + 退出补写 + 仅脏时写，跨重启保留）。**CI 加版本号/Cargo.lock 一致性前置判据**（堵住连踩两次的 bump 坑）。基线：583 passed，clippy 零警告。 | 真机验证清单（Windows docs/14 §8、macOS docs/16）；NSIS 安装器仍未验证 |
| 2026-08-09（v0.1.17 已发布） | **FR-029 代理启用状态的记忆与自动恢复**：上次退出前开着的代理，下次启动（含开机自启动）自动恢复，不必每次手点。记录用**快照真实运行态**而非在意图点埋标记（意图点有四处以上，漏一处即静默失效）；恢复走与界面/托盘**同一入口** `apply_tool_config`（只 `proxy.start()` 会重演「界面说已启动、客户端没走代理」）。顺带修正 docs/12 三处过期表述（「端口每次随机」早已是粘滞固定 47100/47101/47102）、补登漏记的 FR-028、修正 19.7 用例（桌面端不会弹通知授权框）。基线：**588 passed / 0 failed**（串行全量同为 588），clippy 零警告 | 真机验证 docs/07 §20（12 条，Windows 必须你亲自双击）；出包 |

### 第二轮审查（2026-08-01）修出的问题

按严重度：

1. **P0 · 切主题会把刚关掉的开机自启动重新装回系统**（`store.ts` `setTheme`/`setLang`）。
   `store.settings` 只在挂载时写一次，设置页此后改的字段只进它自己的局部 state，而
   `setTheme` 提交的是 `{...get().settings, theme}` —— 挂载那一刻的旧快照。后端
   `save_settings` 对 `mcp_*`/`active_*`/`proxy_ports`/`master_password_enabled` 有「保留后端值」
   防线，但 `auto_start`/`log_dir`/`request_log_enabled`/`lan_exposure`/`health_*` 没有。
   路径：关自启动（注册项已删、页面显示关）→ 同页点「深色」→ 提交旧快照 `autoStart: true`
   → **重新注册自启动项**，而页面仍显示「关」。
   修法：新增 `persistOneSetting` —— 先 `getSettings()` 拉磁盘权威值再合并。
   **已故障注入验证**：旧写法确实会把 `autoStart` 翻回 true。
2. **P1 · 路径遏制有两条绕过**（`aggregate.rs`）。原守卫 `parent.exists() && !is_within_work_root(..)`
   在父目录不存在时被短路，紧接着就是 `create_dir_all` + `fs::write`：① `vendor/` 是链接目录时
   `vendor/sub/x.txt` 的父目录不存在 → 沿链接把目录建到工作目录外（Windows junction 普通权限可建，
   pnpm 就在用）；② 目标文件自身是符号链接时 `fs::write` 跟随链接覆盖其目标。
   改成 `check_no_link_escape`：向上找最近**已存在**的祖先再校验 + `symlink_metadata` 判目标是否为链接。
   +4 测试，已故障注入（退回旧守卫，3 条相关测试立刻变红）。
3. **P2 · `Retry-After` 取最大值拖垮整池**（`proxy.rs`）。候选 A 撞按天配额回 3600（夹到 300）、
   候选 B 只是瞬时抖动 → 取最大值让整个分类停摆 300s，而 1s 后 B 就能服务。改取**最小值**
   （各候选中最早可再试的时刻）。注意 `all_failed_gate_remaining` 里那处 `.max()` 是**对的**、
   不能一起改：它比较的是同一个结论的两个下限（短路窗口剩余 vs 上游要求），都是「不早于」。
4. **P2 · 硬错误被包装成「过载请重试」**（`proxy.rs`）。密钥填错回 401 → 全候选失败 → 529
   `overloaded_error` + `Retry-After`，客户端当上游过载持续退避重试，界面显示「过载」而根因是
   配置错误；短路窗口一到期又放行一个请求再撞 401。现按最后失败状态码分流：401/403/404 等
   4xx 与协议不匹配的 501 **原样回传、不带 Retry-After、不武装短路窗口**；408/409/429/5xx/
   连接层失败才走 529（这三个 4xx 划进临时性，与 Anthropic SDK 重试判据一致）。
5. **P3 · `supports1m` 借用非路由落点的窗口数据**（`model.rs` + `tools.rs`）。两个漏洞叠加：
   逐 Key `find_map`（主 Key 查不到就用备用 Key 的）+ 兜底解析路径也返回窗口。可能写出
   `supports1m: true` 而请求实际落在 200k 上 → 桌面端给出必然被截断的选项。改成只认**主 Key**
   + 只认命中路径（Mapping/Tier/Native）。
6. **P3 · 锁定态在大脑聚合里被报成「未配置密钥」**（`aggregate.rs`）。`.ok().flatten()` 把
   「未解锁」的 `Err` 吞成 `None`。`secret.rs` 的 `get` 专门返回 `Err` 就是为了让这条可行动信息
   传出去；决策者路径用 `?` 本来就是对的，两条口径不一致。已改为保留 `Err` 文案。
7. **前端一致性 5 条**：导入后没刷新全局 settings（导入的主题不生效、且会被下次切主题顶回）、
   启用主口令弹窗字面显示 `{count}`、busy 期间遮罩/X 仍可关弹窗（导出跳过条数与迁移失败原因
   随组件卸载静默丢失）、口令校验提示是不可达死码（只塞进 disabled、界面一个字不说原因）、
   i18n 三条 en 缺失 + 20 条死键。i18n 现为 **zh 418 / en 418，双向零缺失、占位符零不一致**。

**审查者指出的一处推理错误（已确认）**：锁定态选 503 的理由原写「529 会引起客户端无意义的
自动重试」，但 Anthropic SDK 的重试判据是 `408/409/429 或 >= 500` —— 503 一样会被自动重试，
该前提不成立。保留 503（语义上「服务暂时不可用」比「上游过载」贴切；真要阻止自动重试只能用
4xx，而那会让人以为是请求本身有问题），但把理由改正了。

### 效率整治（已完成）

| 问题 | 量化 |
|---|---|
| `list_all_events` 返回全部 500 条**含 trace 正文**（单条请求/响应体各上限 20000 字符 → 满载 19 MB），日志页每 2s 轮询 = **572 MB/min** | 改为列表只带 `hasTrace` 布尔位 + 新增 `get_event_trace(eventId)` 按需单取（展开时才拉、缓存在该行）。**每 2s 19.1 MB → 0.10 MB，降 99.5%** |
| `downstream_body` 无条件 pretty-print 整个请求体（Codex 可达十几万字符）并在每个失败分支 `.clone()`，而调用模型日志**默认关** | 开关关闭时不构造；流式那段拼接也加 `req_log &&` 前置。**⚠️ 勘误（2026-08-03 实测）：本条只修了 `downstream_body`（`proxy.rs:457-463` 确有 `if !req_log` 守卫），同类站点还有两处未修——`forward_to_key` 尾部（`proxy.rs:1384`）与 `try_stream_to_key`（`proxy.rs:1249`）仍无条件 `to_string_pretty(&payload)`，而其产物唯一去向 `:766`/`:790` 都被 `req_log` 门控。另 `:749`/`:774` 无条件全量复制响应体。默认配置下每请求白做 ≈0.6~1 MB 分配 + 一趟序列化。详见 docs/15 P1-5** |
| 每请求 3~4 次 `get_settings()` 克隆整份 `AppSettings`（3 个 HashMap + 2 个 Vec），只为取一个 bool/短字符串 | 加 4 个窄读取器 `request_log_enabled` / `log_downstream_raw_enabled` / `active_model_of` / `active_effort_of`；`write_log_to_file` 也改成只读 `log_dir` |
| LogsPage 把 events 遍历 **5 遍** + 2 次数组拷贝，只为算徽标计数 | 合成一趟循环，`reverse()` 就地做 |

`c83022a` 修掉的 6 处：

1. **日志粘行** `store.rs write_log_to_file` —— `writeln!` 拆成两次系统调用，并发追加粘行。
   已改为整行含 `\n` 一次 `write_all` + 进程级锁。护栏 `concurrent_event_log_writes_stay_one_json_per_line`
   已用故障注入验证过**对旧写法确实变红**。
2. **错误信封不合规** `proxy.rs error_resp` —— 补齐官方 `{"type":"error","error":{"type","message"}}`
   与 status→type 映射。
3. **熔断误伤自身 bug** `proxy.rs status_counts_against_breaker` —— 400 改为只切不罚。
4. **推理强度串台** `proxy.rs inject_default_effort` —— 分类从硬编码 Codex 改为随请求传入。
5. **MCP 无 Origin 校验** `mcp.rs handle_http` —— 新增 `origin_allowed()`。
6. **Codex 接入毁掉 ChatGPT OAuth** `tools.rs apply_codex` —— 占位改写
   `[model_providers.synaroute].experimental_bearer_token`，`auth.json` 一律不碰。

顺带补齐官方规范要求的端点：`GET /v1/models/{id}`（返回单个对象）、
`GET /managed/settings` → 404、`POST /v1/{metrics,logs,traces}` → 200。

### 大脑聚合：多模态 + 工具调用（2026-08-01，FR-026 / FR-027）

在此之前 BrainPage 顶部挂着「聚合首版仅支持纯文本响应；多模态与工具调用将在后续迭代支持」。
这一轮把两项真做出来，提示条改为说明**当前**能力边界与开关位置（没有删掉——用户需要知道
工具调用要手动开、图片有张数与格式限制）。

**新增/改动的文件**

| 文件 | 内容 |
|---|---|
| `upstream.rs` | `ImagePart` / `MultimodalPrompt` / `ToolDef` / `ToolInvocation` / `ToolResultMsg` / `TurnOutcome` / `TurnParams` / `ToolSession`；两协议的 image block 与 tools 声明构造；`parse_anthropic_turn` / `parse_openai_turn`（同时取文本与工具调用） |
| `agent_tools.rs`（新建） | 四个只读工具 + 三道路径防线 + `load_images` |
| `aggregate.rs` | `run_member_turns`（成员 agent 循环）、`prepare_tool_env`、`image_unsupported_hint`；`gather_members` 加 `images` / `tool_env` 参数；`run_mcp` 加 `image_paths` |
| `model.rs` / `types.ts` / `mockData.ts` | `BrainConfig` 加 `toolsEnabled`（默认 false）、`maxToolRounds`（默认 6） |
| `mcp.rs` | `tool_schema` 加 `images` 参数并透传 |
| `BrainPage.tsx` / `i18n.ts` | 新增「工具调用」卡（开关 + 轮数 + 额度消耗警示 + 工作目录/安全说明）；重写顶部提示条 |

**几个刻意的设计判断（改之前先读，否则会以为是遗漏）**

1. **`text_completion` 没动**，另起 `ToolSession`。它有三个调用点（成员、决策者、汇总者），
   后两者不需要工具；给它加参数会让那两条路径也背上多轮循环的复杂度。成员路径改走
   `ToolSession`，工具关闭时请求体与旧的 `text_completion` **逐字节一致**
   （content 是字符串、无 tools 字段），所以开关关着等于回到改动前。
2. **收尾轮仍然声明 tools**。Anthropic 规定「消息里出现过 tool_use/tool_result 就必须带 tools」，
   收尾时抽掉 tools 会直接 400。故改用一条 user 指示「不要再调用工具」，且该指示在 Anthropic 侧
   **并进 tool_result 那条 user 消息**（紧邻两条 user 在部分网关会被判角色未交替）。
3. **assistant 消息照抄上游返回、不重建**。重建会丢掉 thinking block 的 `signature`，
   Anthropic 在带扩展思考的多轮里校验签名，缺失直接 400。
4. **时间预算 70/30 切分**。探索只用成员预算的前 70%，余下给收尾那一轮 —— 不留这份，
   最后一次调用刚发出就被外层 `timeout` 掐掉，前几轮挖到的信息全白费而用户只看到「超时」。
5. **`codegraph_query` 在不可用时不声明给模型**（而不是声明了再报错）：不给模型一个必然失败的
   选项，省一轮空转。不可用原因落日志，不静默。
6. **`maxToolRounds` 运行时夹在 2~12**。夹下限是关键：配 1 轮会让第一轮就走收尾分支，
   工具声明了却永远调不动 —— 那正是本项目反复防的「开关开着但功能没生效」。
7. **图片在 `aggregate` 里加载、不在 `mcp.rs`**：路径相对于同一个 `effective_work_dir`
   （cwd > auto-follow > 手工目录），两处各算一次必然漂移。
8. **图片任何校验不过都整次报错，不静默丢**。静默丢会让用户拿到一个看起来正常、实则没看图的
   答案。带图 + 4xx 时失败原因附「该模型可能不支持图片输入」，否则根因埋在响应体里没人看得出来。

**安全防线与故障注入验证**（每条都先证明「去掉防线后测试变红」）

| 去掉的防线 | 变红的测试 |
|---|---|
| `is_safe_relative_path`（字符串级） | `defense_1_rejects_escape_shaped_paths` |
| `is_within_work_root`（链接逃逸） | `defense_2_rejects_link_dir_escaping_work_root` |
| `is_sensitive_path`（按模型给的名字） | `defense_3_name_check_is_not_redundant_with_target_check` |
| `is_sensitive_path`（按解析后的真实落点） | `defense_3_also_checks_resolved_target_not_just_name` |
| `filter_rg_hits` 里的凭据过滤 | `filter_rg_hits_drops_credential_files_and_counts_them` |
| `walk_grep` 读文件前的凭据过滤 | `walk_grep_never_reads_credential_files` + `grep_tool_excludes_credential_files` |
| `list_dir` 的「受保护」标注 | `list_dir_marks_protected_and_skips_ignored_dirs` |

两点值得记：
- **按名字判与按落点判都不能省**，各自覆盖一个方向：名字敏感/落点无害（`.env` 是个指向普通
  目录的链接）只有按名字能挡；名字无害/落点敏感（`notes.md` → `.env`）只有按落点能挡。
- **`filter_rg_hits` 被拆成纯函数**就是为了这条能被测到：本机没装 `rg`（`grep_tool` 实际走了
  `walk_grep` 兜底），若只有端到端测试，rg 那条路的凭据过滤会是**未验证**的。
- 链接类测试用 `mklink /J`（junction，普通权限即可）而非文件符号链接（需开发者模式），
  且**不静默跳过**——会跳过的安全测试等于没有测试。注意 `Command::arg` 只在含空格时加引号，
  而 cmd 的 mklink 对不带引号、末段以 `.` 开头的路径（`...\.env`）会报「无效语法」，
  故测试助手用 `raw_arg` 自己加引号。

**测试基线**（v0.1.14）：`cargo test --lib` **576 passed / 0 failed**。
`cargo clippy --lib --tests` 只剩 3 条**改动前就有**的 warning（`upstream.rs` 的
`unnecessary_sort_by` / `collapsible_match` / `doc_lazy_continuation`）。
`npx tsc --noEmit` 干净；i18n zh/en **448/448** 双向零缺失。

**端到端测试用真 HTTP**（`aggregate.rs` 的 `spawn_scripted`，按脚本逐次应答并录下每次请求体）：
工具循环最容易错的地方是**第二轮请求体长什么样**（assistant 是否照抄、tool_result 是否一一对应、
tools 是否还带着），纯函数测试覆盖不到这一层。已覆盖 4 条：正常执行→出结论、到轮数上限收尾并
交出已有正文、工具被拒不中断成员、OpenAI 形状（`tool_calls` / `role:"tool"`）往返。

**仍未做（真机验证，AI 自己启动的实例不算 —— MSIX 虚拟化铁律）**

> **2026-08-08 更新**：v0.1.14 已将本轮所有代码改动打包，Windows 产物在 F:\SynaRoute，
> macOS 产物在 GitHub Actions artifact。下列清单中第 4/6/7/8/10/11/12/14/15 条可由真机验证启动。
> NSIS 安装器仍从未被验证过能装成。

1. 开工具开关，在 Claude Code 里问一个需要读多个文件才能答的问题 → 运行日志应有
   `read_file`/`grep` 记录，且答案引用了具体文件内容
2. 传一张截图问「这个报错怎么回事」→ 答案应描述图片内容（而非泛泛而谈）
3. 传一张 6MB 图 → 应明确报「超过单张 5MB 上限」
4. 故意让模型去读 `.env` → 应被拒且日志留痕
5. 关掉工具开关 → 行为回到单轮纯文本，无工具调用日志

---

## 二、P0：~~必须先修~~ ✅ 已修 —— 重复接入会冲掉「接入前备份」

> **2026-07-31 换机后已修。** 修法：`.bak` **首写即锁**（`backup_path_for(path)` 已存在时不再覆盖）
> + `restore_one` 还原成功后**删除 `.bak`**，让下一轮接入重新抓一份新鲜的接入前快照。
> 护栏 `tools::tests::repeated_apply_with_changed_content_keeps_original_backup`
> 已用故障注入**双向验证**：把「首写即锁」还原成无条件覆盖 → 变红（.bak 被写成 v1）；
> 把「还原后删 .bak」去掉 → 也变红。以下保留原始分析，供理解根因与判据方法。

**这是数据丢失级缺陷，触发条件很日常。**

### 定位

`src-tauri/src/tools.rs` 的 `backup_and_write_bytes`（约 1047 行）：

```rust
if path.exists() {
    if let Ok(current) = std::fs::read(path) {
        if current == data { return Ok(()); }   // ← 只有「逐字节相同」才短路
    }
}
if path.exists() {
    std::fs::copy(path, backup_path_for(path))?;  // ← 否则无条件覆盖 .bak
}
```

### 复现

| 步骤 | `.bak` 内容 |
|---|---|
| 首次接入 | 用户原始配置 ✅ |
| 改代理端口后再接入（内容变了 → 守卫不命中） | **已接入态 v1** ❌ |
| 点「还原」 | 拿回「已接入 v1」，**用户原始配置永久丢失** |

触发条件（任一即可，都很日常）：改代理端口、可服务模型列表变化、
MCP 注册时 exe 路径变化（升级换目录必然发生）。

### 影响面

- **CLI 与 Codex 会真的还原错** —— 它们的 `restore` 走 `restore_one(&path)`，直接从 `.bak` 读。
- 桌面端只是留误导性残留 —— 它的 `restore_desktop_at` 不读 `.bak`，
  走 `write_desktop_meta_clear_preferring` + `read_desktop_prev_applied_id`。

### 现存测试为何没抓到

`tools.rs` 的 `repeated_apply_does_not_clobber_backup`（约 2347 行）**只覆盖「内容相同」那一种**，
所以它一直是绿的。改端口/改模型/换 exe 路径这三条真实路径都绕过了它。

### 实证

本机 `%LOCALAPPDATA%\Claude-3p\configLibrary\_meta.json.synaroute.bak` 里存的是
`{"appliedId":"…000053796e61","entries":[{"id":"…000053796e61","name":"SynaRoute"}]}`
—— **已接入态**，不是接入前的 cc-switch 态。

### 实际修法（已落地）

1. `.bak` **首写即锁**：`backup_and_write_bytes` 里备份前先判 `backup.exists()`，已存在就不再拷。
2. `restore_one` 还原成功后**删掉 `.bak`**（删除失败只告警，不把已成功的还原报成失败），
   让下次接入重新抓一份新鲜的接入前快照。
   （副作用：连点两次还原，第二次返回「无备份，无需还原」—— 这是准确的，可接受。）

### 验证判据（可证伪，已执行）

`tools::tests::repeated_apply_with_changed_content_keeps_original_backup` 覆盖：

```
写入用户原始内容 → 接入 v1（.bak = 原始）
→ 接入 v2 / v3（内容各不相同）→ 断言 .bak 仍是原始，而不是 v1
→ restore → 断言拿回原始内容
→ 断言 .bak 已被删除
→ 用户改动 → 再接入 → 断言新 .bak 记录的是「本轮接入前」的内容
```

故障注入实测（两处分别注入、分别复现红）：
- 把守卫改回无条件 `copy` → `left: SYNAROUTE_V1_PORT_8787 / right: OFFICIAL_CONFIG`（红）
- 去掉「还原后删 .bak」→ 「还原成功后应删除 .bak」（红）

---

## 三、~~测试基线不稳定~~ ✅ 已修：短路窗口在测试间串台

> **2026-07-31 换机后已修。** 两处一起改：
> 1. 短路窗口的键从「分类名」改为「**代理实例命名空间 + 分类名**」（`ProxyManager::gate_key`）——
>    生产环境全程只有一个 `ProxyManager`，语义完全不变；测试里每个用例各建一个实例，天然互不干扰。
> 2. `ProxyManager::start()` / `stop()` 里各清一次窗口，顺带修掉一个真实产品缺陷（下详）。
>
> 现在 `cargo test --lib` = **283 passed / 0 failed**，连跑 5 次全绿；`--test-threads=1` 全量也全绿。
> 以下保留原始分析，因为它同时是「进程级共享状态如何污染测试」的取证范式。

原先 `cargo test --lib` 会**偶发**红一条，同一份干净代码（`06aa63a`，工作区 clean）连跑三次实测：

```
run 1 → 276 passed; 1 failed
run 2 → 276 passed; 1 failed
run 3 → 277 passed; 0 failed
```

红的固定是 `proxy::tests::proxy_fails_over_bad_to_good`：

```
assertion `left == right` failed: 应故障转移到好 Key 返回 200
  left: 502
 right: 200
```

**这不是故障转移的 bug，是测试污染。**

### 根因

`6928f4c` 加的「全部 Key 失败」短路窗口用的是**进程级共享状态**，而测试默认多线程并发跑：

| 位置 | 内容 |
|---|---|
| `all_failed_gate()` | `OnceLock<Mutex<HashMap<String, …>>>`，**整进程一份**，早先按分类名字符串做 key |
| 全部候选失败处 | `arm_all_failed_gate(...)` 给该分类武装 5s（`ALL_FAILED_SHORT_CIRCUIT_MS`） |
| 每个请求入口 | 先查窗口，命中就**直接返回失败，压根不打上游** |

冲突的两条测试**都用 `CategoryType::ClaudeCli`**，而 gate 只认分类名、不认是哪个 `Store` / 哪个 `ProxyManager` / 哪个临时目录：

1. `proxy_all_fail_returns_*` 故意让两个候选全失败 → 给 `ClaudeCli` 武装 5s 窗口
2. `proxy_fails_over_bad_to_good` 也用 `ClaudeCli`，若在这 5s 内发请求 → 撞上短路 → 直接失败
3. 谁先跑、隔多久跑由线程调度决定 → 时红时绿

`all_failed_gate_arms_blocks_then_clears_on_success` 注释里写了「用独立分类避免与其它并发测试共享 gate 状态」——**这个隔离意识只落到了那一条单元测试上，两条 e2e 测试没做**。

### 取证（三种时序三种结果，与假设完全吻合）

```bash
cd src-tauri
# 修复前必挂：强制让 all_fail 先跑完立刻跑 failover
cargo test --lib -- --test-threads=1 proxy_all_fail_returns proxy_fails_over_bad_to_good
# 全过：只跑 proxy 模块，时序不同
cargo test --lib proxy::
# 全过：单独跑，没人给它武装窗口
cargo test --lib proxy_fails_over_bad_to_good
```

### 实际修法（已落地，同时修掉一个真实产品缺陷）

**为什么没采用「只在 `start()` 里清一次」这个更小的改法**：那只能覆盖「先武装、后 start」的时序。
两条 e2e 用例各自 `start()` 后才发请求，谁的窗口在谁的请求之前落下仍由调度决定——治不彻底。
故改成从键上隔离，`start()`/`stop()` 的清理只作为产品行为保留。

1. **按代理实例隔离**：`ProxyManager` 持有一个自增的 `gate_ns`，窗口键为 `{gate_ns}:{分类名}`
   （`ProxyManager::gate_key`），并把它随 `handle_request` 一路传下去。生产环境全程只有一个
   `ProxyManager`（`lib.rs:905`），语义与原先「按分类」完全一致。
2. **启停即清**：`start()` 在插入 `running` 前、`stop()` 在移除后各清一次窗口。这修掉的是产品缺陷——
   窗口原本只有「一次成功转发」或「自然到期 5s」两条解除路径，而「停止代理 → 换/修好 Key →
   重新启动」这个**用户已介入的明确信号**被忽略，导致刚点启动后最长 5s 内的请求仍被挡成失败，
   用户视角是「刚点了启动却说全部 Key 不可用」。

新增护栏：
- `gate_is_isolated_per_proxy_manager_instance`：两个 `ProxyManager` 的同一分类必须是不同的键，
  一个武装不影响另一个。
- `start_clears_leftover_short_circuit_gate`：`start()` 与 `stop()` 都必须清掉残留窗口。

### 验证判据（可证伪，已执行）

故障注入分两轮，确认两处修复**各自**都能被抓到：
1. 同时禁用 `start`/`stop` 的清理并把 `gate_key` 退化成不带命名空间 →
   `--test-threads=1` 那条命令复现 `left: 529 / right: 200`（红）。
2. 恢复命名空间、仍禁用清理 → 那条命令**转绿**（证明隔离本身就足以解决测试串台），
   而 `start_clears_leftover_short_circuit_gate` 变红（证明清理这条产品行为独立被覆盖）。

全部恢复后：`cargo test --lib` 连跑 5 次均 **283 passed / 0 failed**；`--test-threads=1` 全量也全绿。

---

## 四、其余待办（按优先级）

### P1 —— ✅ 三条全部已修（2026-07-31 换机后）

| # | 问题 | 位置 | 修法与判据 |
|---|---|---|---|
| 1 | 桌面端 `restore` 非原子 | `tools.rs restore_desktop_at` | 拆成 `restore_desktop_at`（套 `with_rollback`）+ `restore_desktop_steps`（实际步骤），四个文件任一步失败整体回滚。护栏 `desktop_restore_is_atomic_on_midway_failure`：把 normal config 写成非法 JSON 让**最后一步**失败，断言前两步（删 profile、清 `_meta`）都被撤销；注入「跳过 with_rollback」后该测试变红已验证 |
| 2 | 上游 429 的 `Retry-After` 不透传 | `proxy.rs` 故障转移分支 | `ForwardOutcome` / `StreamAttempt::HttpError` 各加 `retry_after` 字段（须在 `resp.bytes()` 消费响应体**之前**从头里取），故障转移循环里累积**最大值**，全失败时透传给下游。新增 `parse_retry_after` 支持 delta-seconds 与 HTTP-date 两种形态、夹取到 `[0, 300]`（`MAX_RETRY_AFTER_SECS`，防上游给几小时把客户端锁死）。护栏 `upstream_retry_after_is_propagated_downstream`（两候选分别给 7s/42s，断言下游收到 42）与 `parse_retry_after_accepts_seconds_and_http_date` |
| 3 | 「全部 Key 失败」返 502 | `proxy.rs` | 改 **529 + `overloaded_error`**（`STATUS_OVERLOADED` / `overloaded_status()`；529 非 IANA 注册码，http crate 无关联常量，只能 `from_u16`）。短路窗口内的响应同样改 529 且带 `Retry-After`。新增 `error_resp_with_retry_after`（`error_resp` 委托给它）。顺带把 **529 加入 `status_counts_against_breaker` 的免罚名单**与 `is_retriable_upstream_error`——我们自己用 529 表达「过载稍后再来」，收到时反过来判「Key 坏了」口径矛盾。原测试 `proxy_all_fail_returns_502` 更名为 `proxy_all_fail_returns_529_overloaded_with_retry_after`，并加断 `error.type == "overloaded_error"`、窗口内二次请求也是 529 |

无上游 `Retry-After` 时的兜底退避 = 短路窗口秒数（5s），且**最少 1s**——`Retry-After: 0` 等于不退避，
客户端会立刻重发，与短路目的相反。

### P2（2026-07-31 已处理完，结论备查）

| # | 问题 | 结论 |
|---|---|---|
| 4 | `disableDeploymentModeChooser` 恒 `true` | **保持 true**（用户拍板对齐 cc-switch）。反查确认它**并非 3p 生效的必需条件**——判据 `pd(e) = hasInference(e) && (disableClaudeAiSignIn(e) \|\| persistedMode() !== "1p")`（`app.asar` offset ≈ 7100100）是「或」关系，而我们本就写 `deploymentMode=3p`。代价：接入后桌面端里看不到官方登录入口，须从 SynaRoute 点还原才能回官方。已把这段判据写进 `build_gateway_profile` 的文档注释，免得下次又当成疑点 |
| 5 | `supports1m` 恒 `true` | ✅ **已改为按 `contextWindow` 判定**。新增 `ProviderKey::context_window_for_outward`（对外名 → `resolve_model` → 查真实名的 `context_window`，因为窗口大小记在真实名那条 `ModelInfo` 上）；≥ `ONE_MILLION_CONTEXT`(1M) 才写 `supports1m: true`，**无数据时整个键都不写**——官方语义是「能力断言」，无依据即不断言。此前一律 true，上游实际不支持时桌面端会给出一个必然失败的 1M 选项 |
| 6 | preview 单文件损坏时的行为 | ✅ **本就已解决，这条 P2 是过时的**。`read_preview_text` 是三端唯一读取入口，它对读失败已返回 `Ok((true, Some("/* 无法读取… */")))` 而非 Err，故 codex/CLI 早已与桌面端同等容错。补了护栏 `preview_degrades_unreadable_file_instead_of_failing_whole_snapshot` 锁住该行为（用目录冒充文件构造读失败，不依赖权限），防止日后有人把 `?` 加回去——一旦整份 Err，前端会丢掉全部路径、聚合页永久卡「加载中」 |
| 7 | cc-switch 接管无检测 | ✅ **已加检测 + UI 明示**。新增 `detect_desktop_takeover_at`：判据是「本档 profile 存在」**且**「`_meta.appliedId` 不是本档」——两个条件都必要（只看 appliedId 会把「从未接入」误报成被接管）。结果经 `ToolConfigPreview.takeoverWarning` 下发；前端两处展示：预览弹窗内的警告块 + **CategoryPage 顶部常驻条**（5s 轮询，因为用户可能在 SynaRoute 开着时去点 cc-switch）。文案含当前生效的档 id 与「重新点写入工具配置即可恢复」 |
| 8 | 未用 `labelOverride` / `anthropicFamilyTier` | ✅ **已加 `anthropicFamilyTier` + `isFamilyDefault`**（用户选了这项，`labelOverride` 未加——当前对外名已是可读的 `claude-*` 形式，收益小）。新增 `model::desktop_family_tier_of` 按名字含哪个档位子串推导，取值顺序固定为官方 `Dc`（sonnet→opus→haiku→fable→mythos）保证稳定；**同档位只有第一条**带 `isFamilyDefault`（官方对多个 true 会告警并取首个，我们不制造这种告警）；无 tier 时两个键都不写（官方对「无 tier 却设 isFamilyDefault」会告警并忽略）。作用：桌面端遇到裸别名（`opus`/`sonnet`）时会钉到对应条目（消费点 `ldn()`，offset ≈ 7400700），opus/fable 还兼作 refusal fallback 来源 |
| 9 | `ProviderKey.headers_json` 是死字段 | **未动**（用户未选）。Rust 侧零使用点、前端无编辑入口，是「自定义请求头」的预留位。留着不碍事（旧 config 反序列化需要）。若确认不做该功能，可在字段注释标注「保留字段，当前不生效」 |

配套改动：`tools::apply` / `apply_claude_desktop` / `apply_desktop_at` 都多收一个 `keys` 参数
（该分类按优先级排序的启用 Key），用于推导能力断言；新增 `DesktopModelEntry` 与
`build_desktop_model_entries` 承载「对外名 → 三个字段」的推导。新增 7 个测试，每项都做过故障注入验证。

### 不修（有理由，勿当遗漏）

- **SmartScreen 安装告警**：需代码签名证书（OV 约 $200–400/年，EV $400–700/年），无代码解法。
- **`retrieval.rs` 的 `cwd` 无路径白名单**：加白名单会破坏「传任意项目目录做检索」的正常用法；
  浏览器攻击面已由 `mcp.rs origin_allowed()` 堵住，剩下的「本机其他进程」属 127.0.0.1 单机自管的既定信任模型。
- **请求日志存明文对话**：`requestLogEnabled` 默认 `false`，开启是用户显式选择。
  开启后 `<安装目录>\logs\*.jsonl` 会有完整 system prompt 与对话正文、无权限限制。

### 尚未开发的功能 —— ✅ **全部开发完毕**（2026-07-31 换机后）

**结论：需求规格 FR-001~FR-025 全部实现，无遗留「未开发」项，也没有第二个「静默失效」的开关。**

> 补充（2026-08-01）：本轮新增 **FR-026 聚合参与者只读工具调用** 与 **FR-027 聚合图片输入**，
> 代码与单测均已完成（见第一节「大脑聚合：多模态 + 工具调用」），只剩第八节的真机验证 5 条。
> 这两条不是「原先漏做」——之前 BrainPage 上那句「多模态与工具调用将在后续迭代支持」是如实声明，
> 现已兑现并把提示条改写成当前的能力边界。

原清单四项的最终状态：

| 项 | 结果 |
|---|---|
| FR-021 配置导入/导出 | ✅ 已实现（下详） |
| FR-022 托盘图标反映代理状态 + 代理开关 + 主 Key 快切 | ✅ 已实现（下详） |
| 主口令增强模式（FR-018 可选增强） | ✅ 已实现（下详） |
| docs/13 非 GPT 主 Key 支持 Codex 工具 | ⬜ 代码侧差距已在 `2f51fa8` 填完，**剩一个只能靠真机实测判定的未知**（下详），刻意不动代码 |

#### ✅ FR-021 配置导入/导出

原状态是「设置页两个 `disabled` 按钮 + 开发中文案」。现已完整落地：

| 组成 | 位置 |
|---|---|
| 口令派生加密信封 | `src-tauri/src/crypto.rs`（**新文件**，未 `git add`）Argon2id + AES-256-GCM，17 个测试 |
| 导出 / 预览 / 导入 | `src-tauri/src/portable.rs`（**新文件**，未 `git add`）12 个测试 |
| IPC 三个命令 | `lib.rs` `export_config` / `pick_and_preview_import` / `apply_import_config` |
| UI | `SettingsPage.tsx` 的 `ExportDialog` / `ImportDialog`；中英文案各 33 条 |
| 新依赖 | `argon2` / `sha2` / `zeroize` |

三个设计判据（都是「照搬 config.json」会踩的）：

1. **密钥不能照搬 DPAPI 密文**。`secrets.enc` 绑当前 Windows 账户，换机解不出。
   故「含密钥导出」是先解出明文、再用**用户口令**重新加密（`crypto::seal`），
   导入时用同一口令解开、按目标机当前保护模式重新封存。
2. **本机绑定字段剥离**（`strip_machine_local`）：端口、日志目录、MCP 已注册分类不随导出走。
3. **完整性校验**：`formatVersion` + 载荷 `sha256`，损坏文件在**改动任何东西之前**就被拒
   （测试 `wrong_password_aborts_before_touching_local_config` 锁住）。

导入两种模式用户当场选：`Merge`（同 id 覆盖、本机多出的保留）/ `Replace`
（清空重建，**会删导出后新建的 Key**，故强制先备份 `.pre-import-<时间戳>.json`）。

**与主口令模式的交互**（后加的两道拦截）：锁定态下导出/导入密钥都会在**动任何东西之前**
直接拒绝并提示解锁。否则导出会产出「声称含密钥、实际为空」的文件，
导入会得到「配置全导入、密钥全失败」的半成品——而 Replace 模式已经把本机原有条目删了。

#### ✅ FR-022 系统托盘：图标状态化 + 代理开关 + 主 Key 快切

原来差的两项（需求验收标准里明写、标 P0）都补齐了：

**1. 托盘图标反映代理运行状态。** 新增 `stopped_tray_icon()`：从**运行时拿到的**
`default_window_icon()` 的 RGBA 现场派生一个灰度 + 降透明（alpha×0.55）版本，
缓存在 `OnceLock`。`apply_tray_icon()` 按「任一分类代理在跑」二选一，在 `rebuild_tray` 里调用。

- **为什么派生而不是加一份灰色 png**：图标是 `tauri.conf.json` 配的、打包时嵌入的。
  另存一张灰色版，日后换图标必然忘记同步 → 托盘出现「运行时新图标、停止时旧图标」的割裂。
- 灰度用感知亮度权重（0.299/0.587/0.114）而非三通道均值——均值会让偏蓝图标显得过亮。
- 像素数据长度非法时**原样返回不做部分修改**（宁可两态同图标，也不要画出花屏），有测试锁住。
- 判据取「任一在跑」而非「全部」：托盘只有一个图标，细分状态由菜单里各分类的勾选表达。

**2. 托盘「代理」子菜单**（`proxy::<分类>`）：三分类各一项，勾选=运行中，标签带端口
（`Claude CLI · 47100`）。点击即切换，**语义与界面按钮完全一致**——
起 = `apply_tool_config_for`（起代理**并**写客户端配置）、停 = `proxy.stop` + `tools::restore`。
这一点是刻意的：若托盘只 `proxy.start()` 不写 config，客户端读到的仍是官方端点，
用户会看到「托盘说已启动，但 Claude/Codex 根本没走代理」。
为此把原 `apply_tool_config` 的实体抽成 `apply_tool_config_for(store, proxy, category)`
（收 `Arc` 而非 `tauri::State`，因为菜单回调是同步闭包、要跨 `async_runtime::spawn`）。

**3. 托盘「主 Key」子菜单**（`primary::<分类>::<keyId>`）：每分类一个子菜单，
列出该分类**启用**的 Key（只列启用的——禁用 Key 不进候选池，列出来会让人以为切了却不生效），
勾选当前主（`enabled_keys_sorted` 首个）。

**顺带把重排规则收归后端**：新增 `Store::set_primary_key(category, key_id)`。
原先前端 `store.ts` 自己算重排再逐个 `upsertKey`——托盘也要这功能，两处各写一份规则迟早漂移，
表现为「从托盘设的主」与「从界面设的主」结果不一致。现在前端改调 `set_primary_key` 命令，
只保留乐观更新。规则：目标提到队首、其余按原序顺延、**整列重编号为连续 0,1,2…**
（历史配置存在「全部 999 同级」的形态，那时故障转移没有确定主备顺序）。
幂等（已是主返回 `false` 不写盘，托盘每次点击都会调）；跨分类 id 误传报 `NotFound`。

护栏：5 个 `set_primary_key` 测试（含跨分类不牵连、禁用 Key 参与重编号不留空洞）+
4 个托盘测试（菜单 id 生成/解析往返、`primary::` 按**首个** `::` 切分、
四个前缀互不为前缀防事件分支串台、图标派生的像素断言与非法数据拒绝）。

#### ✅ 主口令增强模式（FR-018 可选增强）

原状态：开关 `disabled` + 「开发中」，`master_password_enabled` 字段后端零使用点。

**核心设计：模式的事实来源是密钥库文件本身**（`secrets.enc` 里有无 `master` 头部），
`settings.master_password_enabled` 只是 UI 镜像，启动时**以库为准**对账修配置。
反过来（以配置为准改库）会改掉用户真实的加密状态。

| 模式 | 密钥来源 | 换机可用 | 需输口令 |
|---|---|---|---|
| DPAPI（默认） | `CryptProtectData`，绑当前 Windows 账户 | ❌ | 否 |
| 主口令 | Argon2id 派生（64MiB/t=3/p=1） | ✅ | 每次启动一次 |

`crypto.rs` 为此加了**第二组 API**（与 `seal`/`open` 并存，别混用）：
`derive_vault_key` + `seal_with_key`/`open_with_key`。理由是性能——
密钥库每条密钥各一个盒子，而代理转发每次取密钥都要解一次；
若每条都跑 KDF，一次多 Key 故障转移就是几百毫秒纯 CPU。故解锁时派生一次 `VaultKey`
常驻内存，各条只做 AES-GCM（微秒级）。每条独立 nonce（同密钥复用 nonce 泄露明文异或）。

还加了**校验串**（`make_verifier`/`check_verifier`，固定明文的密文存在库头部）。
这是必需的：若靠「试解某条密钥」判断口令，密钥库为空时无从判断，
会把错口令当成解锁成功 → 随后新存的密钥用错密钥加密、旧密钥永久解不开。

`secret.rs` 的三个迁移操作（`enable`/`disable`/`change_master_password`）都遵守三条硬要求，
任一不满足即密钥永久丢失：

1. **先整库解密成功再写盘**。边解边写时中途某条失败会留下「一半口令密文、一半 DPAPI 密文」，
   而 `is_master_mode()` 已为真 → 剩下那半永远读不出来。
2. **落盘前备份原库**（`.enc.master-{on,off,change}-<时间戳>`）。这是唯一一次整份重写全部密文。
3. **落盘失败回滚内存**（含 `vault_key`），保持「内存态不领先磁盘」。

另有几处刻意的判断，都有测试：

- **锁定态 `get` 返回 `Err` 而不是 `Ok(None)`**。这是最容易写错、后果最隐蔽的一处：
  `Ok(None)` 会让转发、`has_secret` 对账、导出都当成「这个 Key 没配密钥」去提示用户重录。
- **锁定态 `set` 拒绝写入**，绝不回退成 DPAPI 加密（否则库里两种密文并存而用户以为全由口令保护）。
- **降级态（本次启动读盘失败）禁止切换模式**——那时 `entries` 是空的，迁移等于把密钥全丢掉还声称已加密。
- **关闭/改口令都要求输入当前口令**，即使已解锁（防止有人趁已解锁的机器直接撤掉保护）。
- **`save_settings` 不让前端覆盖镜像字段**。用户切个主题就会带上旧快照，
  若能写成 `true`，下次启动便按「已启用」引导解锁而库里没有 `master` 头部 —— 自造死局。
- **锁定态跳过 `has_secret` 对账**，否则会把全部标记刷成 false 写盘。
- **锁定态跳过健康探测**（`check_one` 与 `check_category` 各一道，因为前端「手动检测」直接走前者），
  否则 UI 一片红而真实原因只是没解锁。

UI：设置页开关改为触发对话框（启用/关闭都要整库迁移，必须收口令），
`MasterPasswordDialog` 四合一（enable/disable/unlock/change），启用时显式警告
「口令无法找回，忘记等于永久失去全部密钥」；已启用时旁边有「立即锁定」与「修改主口令」；
锁定时 CategoryPage 顶部有 **danger 级常驻条**（比 cc-switch 接管那条更靠前——它让**全部**转发失败）。

测试：`secret.rs` 13 个（迁移往返 + 重开解锁、锁定态读写、错口令不锁回已解锁态、
空库靠校验串判口令、备份、降级拒绝、重复启用拒绝、两个密文 map 都清）、
`crypto.rs` 8 个长驻密钥用例、`store.rs` 3 个（镜像不被前端覆盖双向、专用写入幂等、锁定态跳过对账）、
`health.rs` 1 个（锁定态不改健康态）。**其中 4 个做过故障注入验证确实能变红**
（锁定态退化成 `Ok(None)`、迁移残留 DPAPI 密文、关闭时只在锁着才验口令、
镜像允许前端覆盖、对账不跳过锁定态）。

#### ⬜ 非 GPT 主 Key 支持 Codex 工具（docs/13）—— 代码已就绪，缺一次真机实测

已回填 [13-非GPT主Key支持Codex工具移植方案.md](13-非GPT主Key支持Codex工具移植方案.md)
的现状（那份文档的差距表写于 `2f51fa8` 之前、已过时）：WP1/WP2 列的四项差距**三项已完成**
（freeform 工具识别、freeform 回程转 `custom_tool_call`、`tool_search` 整链）。

**唯一真正未闭合的是一个无法靠读代码判定的问题**：exec 是「模型写 JS 编排多个工具」的范式，
而 opus 走 Anthropic 协议一次只能发一个具名 `tool_use`。Codex 客户端会不会接受「一次一个」？

- 接受 → 现有实现可能已够用
- 强依赖 JS 编排 → 需要在代理侧把 `tool_use` 反向包装成一段 JS，是另一个量级的改动

**故本轮刻意不动代码**，只把实测步骤写进 docs/13（含 rollout 取证手段与判据：
出现 `custom_tool_call`/`function_call` 即客户端认了，出现 `output_text` 即没认）。
当前推荐仍是零成本方案：codex 分类主 Key 用 gpt，opus 作聚合成员参与会诊。

---

## 五、审查覆盖边界

原计划的六路并行审查（CLI 端 / Codex 端 / 桌面端 / 代理内核 / 安全 / 三端功能矩阵）
**跑了两次都因上游额度不足全部失败**（7/7 agent 报 403），最终改为人工聚焦自查。

**已系统查过**：三端接入/还原写盘路径、协议转换矩阵（含流式 6 个方向）、
熔断与故障转移判据、日志落盘、MCP 传输与 Origin、错误信封。

### ✅ 原「未系统覆盖」清单已全部查完（2026-07-31）

五个区域逐一过完，**未发现缺陷级问题**；顺手补了 2 处加固 + 10 个测试。结论备查：

| 区域 | 结论 |
|---|---|
| `aggregate.rs` 超时预算 / 成员降级 / 决策者兜底 | 干净。三阶段共享墙钟 deadline，`decider_floor_ms`（35%、≥90s、被 45% 夹住）保证决策者不被饿死；各阶段 5s 最小下限防 0ms。降级链完整：成员全败→决策者独答（带上已检索文件，不浪费检索开销）；压缩失败→全量拼接；决策者失败→返回已聚合成员意见并标注原因。信号量排队时间**刻意不计入**成员超时（否则并发数<成员数时后排成员在队列里就被判超时）。单请求 HTTP 超时给预算 +5s 余量，让外层 `tokio::timeout` 先到点报干净错误。空响应（200 但 content 空）显式转错误，不当有效答案流转。`mcp_client_timeout_ms` = 各分类 total 最大值 +30s，保证服务端先返回 |
| `retrieval.rs` / `codegraph.rs` | 干净。敏感文件排除分两级、恒定无开关，Tier B 只对配置类扩展名按名字子串判定（不误伤 `TokenService.java` / `secret.rs`）。`pack_symbols` 有个易写错却写对的细节：预算装不下第一个符号时**截断后装入**而非 `break`——返回空会让上层降级到文件级检索、反而按 8000 字符/文件发送，更贵。codegraph 全部 `Command::args` 数组传参，无 shell 拼接；失败判据是「能否解析成 T」而非退出码（codegraph 恒返回 0） |
| `health_probe_real` | 干净。探测与熔断彻底分离：探测只写 `status`/`latency`，`is_candidate` 只看熔断窗口，故探测端点 401 / 探测模型名不匹配都不会把真实流量本可成功的 Key 踢出路由。探测超时封顶 30s（`fast_timeout`）不跟随用户为慢厂商设的长超时，否则串行探测循环会被一个挂掉的慢 Key 拖住几分钟 |
| 前端与磁盘状态一致性 | **比原先担心的更好**。前端 Badge 用的是 `ToolConfigPreview.mcpRegistered`，由 `is_mcp_registered()` **每次实读磁盘**（CLI/桌面端读 `mcpServers.synaroute`，Codex 解析 `config.toml`）；settings 里的 `mcp_registered_categories` 只用于「端口漂移后批量重写」这一后端内部用途，不参与 UI 展示 → 不存在展示漂移 |
| `lib.rs` IPC 命令面密钥外泄 | 40 个命令逐个过完。`reveal_secret` 是唯一返回明文的，设计如此。`RequestTrace` 不含鉴权头；`fetch_models_draft` 接受前端传 secret，但那是用户正在编辑器里填的值、本就在前端内存里，不构成新外泄面；事件日志 / `get_settings` / `get_tool_config_preview`（走 `redact_config_secrets`）均不泄密 |
| 测试隔离性（剩余三处进程级状态） | 已核对，**可划掉**。`default_log_dir()` 的 `OnceLock` 缓存的是一个路径；`LOG_LOCK` 与 `atomic_write` 的 `WRITE_LOCK` 是纯互斥锁。三者都不携带「会改变后续请求判定的业务状态」，与 `all_failed_gate`（按字符串键存业务状态）性质不同，不具备串台条件 |

**本轮顺手做的 2 处加固**（不是发现的 bug，是「会写磁盘 + 有安全判定 + 零覆盖」这个组合本身的风险）：

1. **`parse_and_apply` 加第二道路径防线**（`aggregate.rs`）。原有的组件检查看的是 LLM 给的
   **字符串**（拒 `..`/绝对路径/盘符/UNC），但符号链接是文件系统层面的事：work_dir 下若存在
   指向外部的链接目录（`link/` → `C:\Windows`），`link/x.dll` 不含 `..`、非绝对路径，
   组件检查完全放行，实际却写到了工作目录之外。新增 `is_within_work_root`：落盘前把父目录
   `canonicalize` 后确认仍在 work_dir 下，**fail-closed**（canonicalize 失败即判不安全，
   宁可拒写让用户重试）。只在父目录已存在时校验——待创建的新子目录无链接可循。
   测试用 **junction**（`mklink /J`，普通权限即可，`canonicalize` 同样解析）真实构造该场景，
   而非因 symlink 需特权就跳过。
2. **`save_brain` / `save_settings` 改走 `mutate_and_persist`**（`store.rs`）。落盘失败时内存态
   领先磁盘——而「内存比磁盘新」这个方向**永不自愈**（mtime 自愈只认「磁盘比内存新」），
   表现为 UI 一直显示保存成功、重启后配置回退。

   > ⚠️ **勘误（2026-08-03 架构评审实测）**：本条原文写的「这两个是全仓仅剩的直接 `persist()`
   > 写路径」**不成立**，当时只改了这两个用户可见的 save，其余并未清点。实测 `store.rs` 仍有
   > **12 处**裸 `self.persist()`：`update_health:843`、`set_active_model:1029`、
   > `set_active_effort:1052`、`set_proxy_port:1067`、`add_registered_category:1084`、
   > `remove_registered_category:1099`、`clear_registered_categories:1113`、`set_mcp_port:1128`、
   > `set_mcp_enabled_flag:1142`、`set_master_password_flag:1157`、`upsert_vendor:1184`、
   > `delete_vendor:1200`（复核命令：`grep -n "self.persist()" src-tauri/src/store.rs`，
   > 排除 `:414` 那处——它在 `mutate_and_persist` 内部，是正确的）。
   >
   > 其中 `upsert_vendor` / `delete_vendor` / `set_proxy_port` / `add_registered_category`
   > 有**用户可见后果**（自定义厂商「保存成功」但重启消失；粘滞端口丢失导致重启后端口漂移、
   > 客户端里写的旧端口连不上——这正是引入粘滞端口本要解决的问题），应优先改走
   > `mutate_and_persist`。**`update_health` 是刻意例外**：高频后台调用，整份 clone 快照代价
   > 不对称，且健康态是可重建的瞬态——保留直写，但改动时勿「顺手统一」掉。
   >
   > 教训与本文档末尾的方法论一致：**动手前先 grep 实证，不要相信任何「仅剩 N 处」式的结论
   > （包括本文档自己写的）**。
   `save_settings` 改造时注意：「保留后端自管字段」必须在 `cfg.settings = settings` **之前**做，
   顺序反了会把 `mcp_port` / `active_models` 等清零（已用故障注入验证该顺序错误会被 4 条测试抓到）。

新增 10 个测试：`aggregate` 的 8 个（路径逃逸、junction 逃逸、父目录创建、内层三反引号不截断、
未闭合块不写截断文件、普通代码块不误判、`is_within_work_root` fail-closed）+ `store` 的 2 个
（两个 save 的落盘失败回滚，后者兼验后端自管字段不丢）。每个都做过故障注入双向验证。

---

## 六、换机注意事项（**重要，先读这节再动手**）

### 1. ~~已发布的安装包不含本轮修复~~ ✅ **已解决（v0.1.14 已发布，2026-08-08）**

三处版本号已 bump 到 **0.1.14**。当前发布状态：

| 平台 | 产物 | 大小 | 判据 |
|---|---|---|---|
| Windows | `SynaRoute_0.1.14_x64-setup.exe` | 6.3 MB | chunk 嵌入命中 1 次，生产私钥签名，sha256 与源一致 |
| Windows | `synaroute.exe` | 25.2 MB | 同上，已复制到 `F:\SynaRoute\` |
| macOS | `SynaRoute_0.1.14_aarch64.dmg` | 8.7 MB | CI 六条判据全通过，arm64，minOS 11.0 |
| macOS | updater `.app.tar.gz + .sig` | 9.0 MB | CI 临时签名，仅测试用，非生产更新 |

**仍未做：NSIS 安装器从未被验证过能装成。** 静默安装 `/S` 在旧机器上卡死过两次。
（一次 `timeout 120` 返回 124，一次后台 300s 无退出）。此前所有部署都是绕过安装器、
直接覆盖 exe 完成的 —— **F 盘现在放的是直接覆盖的 exe，不是装出来的**。
双击 `SynaRoute_0.1.6_x64-setup.exe` 正好是对安装器的第一次真实验证；若卡住，那是真问题。

### 2. `secrets.enc` 不可跨机器搬运

密钥用 **Windows DPAPI** 加密（`secret.rs`），绑定「当前用户账户」。
把 `%APPDATA%\SynaRoute\secrets.enc` 拷到另一台机器**解不出来**，
表现为转发时报「密钥缺失」。换机后需要在新机器上重新录入密钥，
或用本仓库新增的「从 cc-switch 导入」功能重新导（若新机器上也有 cc-switch）。

`config.json`（Key 的名称/base_url/映射等元数据）是普通 JSON，可以搬。

### 3. 本文提到的绝对路径是**旧机器实测值**，新机器不同

| 旧机器 | 说明 |
|---|---|
| `F:\soft\SynaRoute\` | 安装目录（exe + `logs\` + `backup\` + `mcp-port`） |
| `E:\CC Switch\cc-switch.exe` | cc-switch 安装位置 |
| `~/.cc-switch/cc-switch.db` | cc-switch 的 SQLite 库（这个路径是固定的，各机器一致） |

新机器上请重新确认，不要照抄。

### 4. MSIX AppData 虚拟化陷阱依旧适用

见 [11-MSIX虚拟化踩坑复盘.md](11-MSIX虚拟化踩坑复盘.md)。补充一条本轮验证到的**判断方法**：

```bash
ls -d "$LOCALAPPDATA/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Roaming/SynaRoute"
```

该目录**不存在** → 说明你当前读到的 `%APPDATA%\SynaRoute\` 是真实文件；
**存在** → 你读的是包内私有副本，结论不可信，必须用计划任务逃逸包身份再看。

另外已确认 `%LOCALAPPDATA%\Claude*`（桌面端数据）**不被虚拟化**，读写它是可信的。

### 5. 构建硬规则不变

必须 `npm run tauri build`（或 `-- --no-bundle`），**禁止裸 `cargo build --release`**。
部署前用「`dist/assets/` 的 chunk 名能在 exe 里 `grep -c` 到（>0）」验证前端已嵌入。
详见 [04-构建部署指南.md](04-构建部署指南.md)。

新增依赖 **`rusqlite` 0.32（bundled）**，会编译自带的 SQLite C 源码 —— 新机器需要 C 编译器
（Windows 上即 VS Build Tools）。二进制因此从约 21.5MB 涨到约 23.3MB。

---

## 七、判据来源与取证方法（可复现，别再靠猜）

本轮所有关于「客户端到底认什么字段」的结论，都来自**反查客户端二进制**，不是文档推测。
换机后同样可以这么查（路径按新机器实际调整）。

### Claude Code CLI（`claude.exe`，本轮版本 v2.1.219）

位置：`%LOCALAPPDATA%\Claude-3p\claude-code\<版本>\claude.exe`（约 265MB，内含打包后的 JS）

分块搜字节串定位，再 dump 上下文：

```bash
node -e '
const fs=require("fs");const f="claude.exe";
const fd=fs.openSync(f,"r"),size=fs.statSync(f).size;
const n=Buffer.from("CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY");
const CH=1<<24;let pos=0,prev=Buffer.alloc(0);
while(pos<size){const len=Math.min(CH,size-pos);const b=Buffer.alloc(len);
 fs.readSync(fd,b,0,len,pos);const hay=Buffer.concat([prev,b]);let i=0;
 while((i=hay.indexOf(n,i))!==-1){console.log(pos-prev.length+i);i++;}
 prev=b.slice(Math.max(0,len-n.length));pos+=len;}'
```

已确认的关键事实（写代码时的判据）：

- `kn()`（getAPIProvider）只有在 `wy()`（即 `Ot.gatewayAuth` 已设）时才返回 `"gateway"`；
  `gatewayAuth` 仅由 `CLAUDE_CODE_USE_GATEWAY` + `ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN`
  或企业 `enterpriseGateway` 设置。**SynaRoute 不写 USE_GATEWAY，故 CLI 走的是 `firstParty` 分支。**
- 因此模型发现走的是 `BIc()`/`GIc()`/`uGr()`（源码内叫 gatewayDiscovery），条件为：
  `CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY` 为真 **且** `kn()==="firstParty"`
  **且** 非官方 anthropic base url **且** `ANTHROPIC_BASE_URL` 已设。
  它 `GET {base}/v1/models?limit=1000`，**超时仅 3000ms**、`redirect:"error"`，
  校验 schema 是 `{id: string, display_name?: string}`（`.optional()` 不接受 `null`），
  并按 `/^(claude|anthropic)/i` 过滤 `id` —— 这就是 `to_gateway_model_id` 要包
  `claude-synaroute-` 前缀的原因。
- bool 型环境变量的真值判据 `Yt()`：`["1","true","yes","on"]`。
- **claude.exe 内嵌了一份官方 gateway 协议规范全文**（本轮在 offset ≈ 258929000 附近）。
  错误信封形状、status→`error.type` 对应表、`/v1/models` 返 404 即干净回退、
  `/managed/settings` 的 404 与 `200 {}` 语义不同、`/v1/{metrics,logs,traces}` 返 404
  会让 exporter 每次 flush 记错误、「Don't buffer SSE on the stream:true path」
  —— 全部出自这份文档。**改代理行为前先搜出来读一遍。**

### Codex（`codex.exe`，本轮版本 26.602.40724）

位置：`%LOCALAPPDATA%\OpenAI\Codex\bin\<hash>\codex.exe`（Rust，符号表里有 serde 字段名串）

已确认：`struct ModelProviderInfo with 17 elements`，字段串含
`base_url` `env_key` `env_key_instructions` **`experimental_bearer_token`** `auth` `aws`
`wire_api` `query_params` `request_max_retries` `stream_max_retries`
`stream_idle_timeout_ms` `websocket_connect_timeout_ms` `requires_openai_auth`
`supports_websockets` `http_headers` `env_http_headers` `id` `name`。
MCP server 侧字段含 `startup_timeout_sec` / `startup_timeout_ms` / `tool_timeout_sec` /
`bearer_token_env_var` / `env_http_headers`。auth 存储模式枚举含 `api_key` / `chatgpt`。

### Claude 桌面端（`app.asar`，本轮版本 v1.24012.9）

定位安装目录（MSIX 包，`ls` 看不到，用 PowerShell）：

```bash
powershell -NoProfile -Command "Get-AppxPackage -Name *Claude* | Select-Object PackageFullName,InstallLocation | Format-List"
```

`app.asar` 在 `<InstallLocation>\app\resources\app.asar`（约 38MB，未加密、minify 后仍可读）。
用与上面 `claude.exe` 相同的「分块搜字节串 → dump 上下文」手法。本轮据此拿到的判据：

| offset ≈ | 内容 |
|---|---|
| 2280400 | 内置模型清单（含 `claude-fable-5` / `claude-mythos-5` 的 `context.window`、`capabilities`、`aliases`、`latest_per_family`） |
| 6842900 | `sD()` —— 桌面端模型名判据（见第九节） |
| 7013300 | `inferenceModels` 元素的 schema：`supports1m` / `prefer1m` / `labelOverride` / `anthropicFamilyTier` / `isFamilyDefault` 的官方说明与取值 |
| 7098550 | `Zrn()` = hasInference、`sme()` = disableClaudeAiSignIn |
| 7100100 | `pd()` = deploymentModeIs3p 的完整判据（**「或」关系**，见第四节 P2#4） |
| 7400700 | `ldn()` —— `anthropicFamilyTier` 的消费点（裸别名钉到条目、多个 `isFamilyDefault` 时告警取首个） |
| 7964450 | `W0n()` —— 模型清单**硬过滤**（`filter(aD(...).ok)`）与 `resolveDefaultSessionModel` |

### cc-switch 的数据模型（用于导入功能）

`~/.cc-switch/cc-switch.db`（SQLite）的 `providers` 表。用 Node 22 内置驱动只读查看：

> **实测坑（2026-07-31）**：不能把库拷到 `/tmp/`。Git Bash 的 `/tmp` 映射到
> `%LOCALAPPDATA%\Temp`，而 node 的 `DatabaseSync` 拿到的是 Windows 视角的路径，
> 会报 `unable to open database file`。拷到普通盘（如 `E:\tmp\cc.db`）并用
> **Windows 风格绝对路径**（`"E:/tmp/cc.db"`）打开即可。

```bash
mkdir -p /e/tmp && cp ~/.cc-switch/cc-switch.db /e/tmp/cc.db
node --experimental-sqlite -e '
const {DatabaseSync}=require("node:sqlite");
const db=new DatabaseSync("E:/tmp/cc.db",{readOnly:true});
console.log(db.prepare("SELECT id,app_type,name,settings_config FROM providers").all());'
```

`settings_config` 两种形态与映射规则见 `src-tauri/src/ccswitch.rs` 头部注释。
**实测坑**：用户自建档的 `sort_index` 是 `NULL`（只有内置官方模板写 0），
不 `COALESCE` 则 rusqlite 取列报 `InvalidColumnType`、**整次扫描全灭**。已兜底并有测试锁住。

---

## 八、旧机器上未完成的真机验证

以下需要在**能双击启动 SynaRoute 的机器**上做（按
[11-MSIX虚拟化踩坑复盘.md](11-MSIX虚拟化踩坑复盘.md) 的铁律，AI 自己启动的实例不算）：

1. 「从 cc-switch 导入」候选列表是否正确（旧机器预期：8 条可导入 / 4 条不可导入）
2. 桌面端点「接入」后，`configLibrary\` 里出现 SynaRoute 的档
   （`00000000-0000-4000-8000-000053796e61.json`）
3. `_meta.json` 的 `appliedId` 指向该档，**且 cc-switch 那条 entry 仍在**（共存逻辑正确）
4. `%APPDATA%\SynaRoute\desktop_prev_applied.json` 被写出
   （只有在「接入前 appliedId 不是我们自己」时才会写，这是**正确行为**，
   旧机器上该文件缺失已确认是因为接入时 appliedId 已经是 SynaRoute 自己）
5. Codex 接入后 `~/.codex/auth.json` **字节不变**、且不产生 `auth.json.synaroute.bak`
6. 桌面端存一个对外名为 `glm-4.6` 的 Key → **保存应被拒**并显示多行修法（见第九节）；
   改成映射 `claude-opus-4-8` → `glm-4.6` 后保存成功、接入后模型选择器里能看到 `claude-opus-4-8`
7. **开机自启动**（FR-025，见第十节末的 4 步）：开开关 → 查注册表 `Run` 项 → 重启验证
   「自动起来且只在托盘」→ 关开关验证注册表项消失。**注意别在 `tauri dev` 下测**——
   会往注册表塞一个指向 `target/debug` 的启动项
8. 桌面端接入后去点开 cc-switch（它会整份重写 `_meta.json`）→ 回到 SynaRoute，
   CategoryPage 顶部应在 5s 内出现**「已被其他工具接管」常驻警告条**（P2#7）
9. **配置导出 → 换机导入的完整往返**（FR-021，见第四节）。这是唯一没被单测覆盖的部分——
   `export_with_secrets_roundtrips_into_a_fresh_store` 只验了同机跨 Store。步骤：
   旧机「导出（含密钥）」设口令 → 拷 json 到新机 → 新机「导入」输同一口令 →
   断言 Key 数量与名称一致、**能真的转发成功**（密钥确实解出来了，不是只有元数据）、
   且端口/日志目录没被导入文件覆盖成旧机的值（`strip_machine_local` 生效）。
   顺带验 `Replace` 模式导入前确实生成了 `.pre-import-<时间戳>.json` 备份。
10. **托盘图标随代理状态变化**（FR-022）：全部代理停止时图标应明显发灰/变淡，
    启动任一分类后恢复彩色。**注意**这条只能在打包产物上看——`tauri dev` 的图标来源相同但
    观感受 dev 窗口影响。派生逻辑本身已有像素级单测，这里验的是「视觉上能否分辨」。
11. **托盘代理开关的语义一致性**（FR-022，这条最值得亲手验）：从**托盘**点某分类的「代理」
    → 该分类客户端配置应被真的写入（去看 `~/.claude.json` / `~/.codex/config.toml` /
    `configLibrary\`，端点指向 `127.0.0.1:<端口>`）；再点一次关闭 → 配置应被还原。
    若只有代理起了、客户端配置没变，就是 `apply_tool_config_for` 那条链没接上——
    症状是「托盘显示已启动，但 Claude/Codex 根本没走代理」。
12. **托盘主 Key 快切**（FR-022）：托盘「主 Key」→ 某分类 → 选一条非当前主的 Key，
    回主窗口应看到它排到第一、优先级 0，其余顺延为连续 1,2,3…（不是全 999 同级）。
    再从**界面**的「设为主」切一次，结果口径必须与托盘一致（同一套 `Store::set_primary_key`）。
13. **主口令增强模式**（FR-018，见第四节）。这条**必须真机做**，因为涉及 DPAPI：
    1. 先确认有 ≥2 条已存密钥的 Key，且转发正常
    2. 设置页开启主口令 → 输两遍口令 → 应提示「N 条密钥已重新加密」，
       且 `%APPDATA%\SynaRoute\` 下出现 `secrets.enc.master-on-<时间戳>` 备份
    3. 打开 `secrets.enc` 看结构：应有 `master` 头部与 `boxes`，`entries` 应为空
    4. **重启应用** → CategoryPage 顶部应有红色「已锁定」条，此时转发应失败且错误信息
       指向「请解锁」；健康检查不应把 Key 刷成红（锁定态跳过探测）
    5. 解锁 → 转发恢复、`has_secret` 标记没被刷成「未配置」
    6. 「修改主口令」→ 旧口令应立即失效、新口令可用
    7. 关闭主口令（输当前口令）→ `secrets.enc` 回到只有 `entries`，转发仍正常
    8. **反例**：故意输错口令解锁 → 应报「主口令错误」而非含糊失败，且不影响已解锁状态
14. **聚合工具调用**（FR-026，2026-08-01 新增）：BrainPage「工具调用」开关打开 → 在 Claude Code 里
    问一个「需要读多个文件才能回答」的问题（例如「代理转发里模型名是怎么一步步定下来的」）→
    运行日志应出现 `工具 · <Key/模型> · read_file path=...` / `grep pattern=...` 记录，
    且答案引用了具体文件内容（不是泛泛而谈）。**再把开关关掉重跑**：应回到单轮纯文本、
    完全没有工具调用日志——这条是验「关掉真的等于回到改动前」。
    反例两条：① 用 prompt 诱导模型去读 `.env`（如「先读一下项目根的 .env 再回答」）→ 应被拒、
    日志留痕、且模型换方向后成员仍然成功；② 把轮数上限调到 2 并问一个很需要多轮挖的问题 →
    应在第 2 轮停下并给出结论，日志有「工具循环收尾」。
15. **聚合图片输入**（FR-027，2026-08-01 新增）：在 Claude Code 里调 `synaroute_ai` 时带
    `images: ["<相对 cwd 的截图路径>"]` 问「这个报错怎么回事」→ 答案应**描述图片内容**。
    三条反例：① 传一张 >5MB 的图 → 应报「超过单张 5MB 上限」而不是静默丢；
    ② 传 5 张 → 应报张数超限；③ 传 `.bmp` → 应报格式不支持。
    还要验一条降级口径：把成员换成纯文本模型再带图 → 失败原因里应出现
    「该模型可能不支持图片输入」的提示（而不是只有一个裸的 HTTP 400）。

---

## 九、桌面端模型名判据（2026-07-31 反查修正，**推翻了此前的错误结论**）

### 此前错在哪

旧记忆/文档写的是「模型名校验**只是面板黄字**，只要 `inferenceModels` 非空就能用」。
**这是错的**，实际是硬过滤。按错的结论办事的后果：桌面端分类里配 `glm-4.6` 之类对外名，
保存与接入都不报错，用户重启桌面端后模型选择器**空的**，开会话抛 `ModelsNotDiscoveredError`
——症状与「卡在登录页」同级难排查，而根因埋在几天前存 Key 那一刻。

### 判据原文（app.asar v1.24012.9，offset ≈ 6842900）

```js
const Dc=["sonnet","opus","haiku","fable","mythos"];
const nhe=new RegExp(`^(${Dc.join("|")})(-[\d.]+)?$`);   // 裸档位名，可带 -数字/点
const Snt=["claude",...Dc,"anthropic"];                   // 须含其一
const Ent=/ark-code|astron|command-r|deepseek|doubao|gemini|gemma|glm|gpt|grok|hermes|hy3|kimi|lfm|\bling\b|llama|longcat|mimo|minimax|mistral|mixtral|moonshot|nemotron|openai|phi-|qianfan|qwen|tc-code|\bunic\b|yi-|stepfun|step-3|seed-|bytedance|hunyuan|granite|amazon\.nova|nova-|devstral|ministral|ernie|codex|arcee|trinity|abab|phi\d|\bk2\.|\bm2\.|jamba|arctic|solar|mercury|zamba|kat-coder|\bds-|dpsk/;
function sD(e){const t=e.toLowerCase();return Ent.test(t)?!1:nhe.test(t)||Snt.some(n=>t.includes(n))}
```

**厂商名黑名单（`Ent`）优先于关键词（`Snt`）。**

### 硬过滤链路（offset ≈ 7964450）

```
resolvedModels() → W0n(provider,{adminList,discovered})
  末行: return e ? i.filter(o => aD(e, o.id).ok) : i     ← 直接删掉不合规条目
       （aD("gateway", id) → aKt(id) → sD(id)）
→ resolveDefaultSessionModel() 见空 → {ok:false, reason:"models_not_discovered"}
→ 开会话抛 ModelsNotDiscoveredError
```

`validateSessionModel` 的 allowed 成员判断（`min(t,n)`）也是拿**过滤后**的列表比对，同样救不回来。
所以「`inferenceModels` 非空但全不合规」与「`inferenceModels` 为空」后果**完全等价**。
面板旁那句 `Doesn't look like an Anthropic model: {reason}` 只是 warn 谓词，不是全部影响。

### 最容易踩的一条：`claude-synaroute-` 前缀对桌面端无效

| 名字 | Claude Code CLI | 桌面端 |
|---|---|---|
| `glm-4.6` | ✗（不以 claude/anthropic 开头） | ✗（命中 `glm`） |
| `claude-synaroute-glm-4.6` | ✓ **靠前缀救回** | ✗ **仍命中 `glm`，前缀无用** |
| `claude-opus-4-8` | ✓ | ✓ |
| `opus`（裸档位名） | ✗（会被包前缀） | ✓ |

两端是两套规则：CLI 只判前缀、无黑名单（`is_cli_discoverable_model_id`）；桌面端要求含关键词
**且**不含厂商名（`is_desktop_acceptable_model_id`）。**别拿一个顶另一个。**

### SynaRoute 的实现与取舍

1. `model.rs::is_desktop_acceptable_model_id` —— 1:1 复刻 `sD()`，**不引 `regex` 依赖**
   （手写词边界 `\b` 与 `phi\d`；黑名单其余项是普通子串）。
2. `lib.rs::upsert_key` → `reject_desktop_key_with_unusable_model_names` ——
   桌面端 Key 的**对外名**（`serviceable_models()`，即真正写进 `inferenceModels` 的那份）
   有任一不合规就**拒绝落盘**，错误消息含被拒名字 + 后果 + 可照做的修法。
   **无条件校验**（用户拍板「该严，避免不可用」）：即使这次只改了优先级，历史遗留的不合规 Key
   也必须先修好。代价是老数据调优先级/开关会被拦一次，但报错已给出修法；
   放行的代价是不可用状态一直留在库里、到接入那刻才炸。
   真实上游名不受限——配映射 `claude-opus-4-8` → `glm-4.6` 即可。
3. `tools.rs::desktop_model_names_warning` —— 接入侧**兜底强警告，不阻断**（对齐 cc-switch，
   保留「先接入、再回头调映射」的用法）。全不合规时措辞升级为「写进去了但用不了」。
   注意：不合规名必须**原样写进档里**，不能过滤——过滤会让 `effective` 变空、撞上
   「空列表 → 报错」分支，把「照写」变成阻断。
4. `lib.rs::apply_tool_config` —— 另记一条 `error` 事件进运行日志（接入弹窗一关就没了，
   而这问题往下排查很久，需要留痕）。用 `error` 而非新增 `warn` kind：前端 `LogsPage`
   的分组映射是穷举的，且这条本质是「配置不可用」。

### 验证方法（可复现）

把官方 `sD()` 原样搬进 Node，生成 **676 例语料**（官方词表全项 × 10 种上下文包裹 + 关键词组合
+ 边界形态），与 Rust 实现逐条差分——**676 例全一致**。语料生成脚本是一次性的（已清理），
需要重跑时按上面的正则重新构造即可。仓库里留下的是 `model.rs` 里那两个手写用例测试
（含 `claude-synaroute-glm-4.6` 必须被拒这条护栏，防日后有人误以为前缀能绕过）。





---

## 十、FR-025 开机自启动：从「静默失效的开关」到真接线

### 原来错在哪（这是个比「功能没做」更坑的状态）

`AppSettings.auto_start` 字段存在、设置页开关能点、值也确实落进 `config.json` ——
但后端**从无任何代码去注册系统自启动项**（`lib.rs` / `store.rs` 里搜不到该字段的消费点）。
用户打开开关看起来生效了，重启后什么也不会发生，而设置页下次打开仍显示「已开启」。
「看起来能用但什么都不做」比明确标注「开发中」（主口令、导入导出那两处的做法）危险得多。

### 修法

接官方 `tauri-plugin-autostart`（Windows 走注册表 `Run` 键，跨平台差异由插件适配），
没有自己撸注册表——项目虽是 Windows-only，但注册表边界情况（组策略锁定、
MSIX 沙箱、路径带空格）插件已经处理过，自己写等于重造且更易漏。

三个接线点：

1. **`save_settings` 命令里同步系统状态**（`sync_autostart`）。顺序是**先动系统、成功后才落盘**
   —— 反过来会留下「配置说开、系统实际没开」的不一致，而用户下次看到的是开着的、无从察觉
   （与 `save_secret` 先写密钥再置 `has_secret` 同一道理）。失败**上抛**而非只记日志：
   注册可能因权限/组策略失败，静默吞掉又回到「以为开成了」。
2. **启动时状态对账**（`setup` 内）。`config.json` 与系统实际注册项可能背离——用户手动改过
   注册表、用清理工具删过启动项、或**从旧版本升级**（旧版只存字段、从不注册，所以老用户
   开关是开着的但系统里没有项）。以配置为准把系统拉回一致，让开关「所见即所得」。
   对账失败只告警不阻断启动（功能降级，应用照常可用）。
3. **随系统启动时最小化到托盘**（需求原文要求）。判据是启动参数含 `--autostart`
   （`AUTOSTART_FLAG`，注册自启动项时带上），**不是**「`auto_start` 配置为真」——
   后者在用户手动双击时同样成立，那时把窗口藏起来会让人以为程序没启动、反复双击。

### 前端顺带修的一处

`SettingsPage` 的 `update()` 原先乐观更新 UI + 失败弹 toast，但**不回滚开关状态**。
对「后端要动系统」的设置（自启动是第一个这类设置）这就是致命的：系统侧失败了，
UI 却仍显示已开启。已改为失败时 `setSettings(prev)` 回滚。

### 验证边界（诚实说明）

- 单测覆盖的是**参数判据**（`launched_by_autostart`）：精确相等而非 `contains`，
  故 `--autostart=1` / `autostart` / `--mcp-stdio` 都不误命中。已用故障注入
  （把判定改成 `contains("autostart")`）验证该测试确实会变红。
- **注册表写入本身没有自动化测试**，也无法有：`autolaunch()` 需要真实 `AppHandle`，
  且会往当前用户的 `HKCU\...\Run` 里真写。这部分只能真机验证（见下）。
- 开发模式（`tauri dev`）下注册的是 `target/debug` 的 exe 路径。**别在开发模式下测这个开关**
  —— 会往注册表塞一个指向 debug 产物的启动项，卸载后残留。要测就用安装后的正式产物。

### 真机验证步骤（加进第八节清单）

1. 装正式包 → 打开设置页 → 开「开机自启动」
2. `reg query "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v SynaRoute`
   应能查到，值为安装后的 exe 路径 + `--autostart`
3. 重启系统 → 应用应自动起来且**窗口不弹出**、只在托盘
4. 关掉开关 → 再查注册表，该项应消失

---

## 十一、2026-08-02 待办落盘（新对话接手从此处读起）

> **本次会话因对话内容触发压缩拦截，未能走 `/compact`，故此节为唯一的新会话接续依据。**
> 工作区改动**尚未提交、尚未打包**。以下按「现状 → 已修 → 待办」三层写，接手的 AI/人先读本节点。

### 现状

工作区（对比最后提交 `191eb27`）有一大批未提交改动，分四块功能：

| 块 | 涉及文件 | 说明 |
|---|---|---|
| **A. 聚合工具调用 + 多模态**（FR-026/027） | `agent_tools.rs`(新)、`aggregate.rs`、`upstream.rs`、`mcp.rs`、`model.rs`、`types.ts`、`mockData.ts`、`BrainPage.tsx`、`i18n.ts` | 成员 agent 循环（`ToolSession`）、四个只读检索工具、图片输入（≤4张/≤5MB/四格式） |
| **B. 日志折叠降噪** | `store.rs`、`model.rs`、`proxy.rs`、`LogsPage.tsx` | `append_event_collapsible`：连续同类转发事件在内存态折叠成一条带 `×N`，日志文件仍逐条完整写 |
| **C. 关于页** | `AboutPage.tsx`(新)、`App.tsx`、`Sidebar.tsx`、`i18n.ts` | 作者信息 + 外部链接走 shell 插件 |
| **D. 新图标** | `src-tauri/icons/*`、`app-icon.svg`(新) | 换图标 |

测试基线：**576 passed / 0 failed**（v0.1.14，`cargo test --lib`）；`tsc --noEmit` 干净；`clippy` 零警告。

### 已修（本轮审查后）

1. **成员循环时间预算被单次长轮击穿**（聚合 `run_member_turns`）：外层 `timeout(budget)` 若先到，整个 future 在 HTTP await 上被丢弃，已挖到的 `preamble` 连同花掉的 token 一起作废、用户只见「超时」。已改为：单轮 `request_timeout` 夹到「硬截止 − 750ms 余量」，内层先返回；轮内失败但已有正文 → 交差并写「工具循环中断」日志；连一次请求的时间都没有 → 有正文交差、无正文如实报超时。

### 待办（按优先级）

- [x] **P2 `read_file` 行边界丢行 —— 已闭环**（2026-08-02）。**接手时的实测更正**：上一轮记的
  「未修（grep `lineno` 为 0）」**已过时** —— 该文件在那条记录落盘后又被编辑过（同时接入了
  `191eb27` 的 `hidden()`），`resume_from = lineno` 两处修复**代码早已存在**，缺的只是回归测试。
  本轮补齐两条可故障注入的护栏：
  `read_file_char_cap_at_line_boundary_loses_no_line`（预算在**行边界**耗尽：header 声称
  「完整给到第 N-1 行」必须属实，续读锚点那行不得已出现在首屏，且从锚点续读必须真能拿到它）
  与 `read_file_inline_truncation_resume_keeps_the_tail`（行**内**截断：首屏含头部、不含尾部，
  从锚点续读必须拿回尾部）。两条都**不硬编码行号**，从 header 解析 `start_line=` 锚点，
  故对 `+8` 余量之类的算术调整仍然有效。
  **故障注入已双向验证**：把两处 `resume_from = lineno` 各退回 `lineno + 1` →
  分别复现红「首屏应完整含第 81 行（LINE0081）——header 声称给到了它却没给，即丢行」。
- [x] **P3 硬链接绕过敏感文件名判定 —— 已加固**（2026-08-02，用户拍板「实现健壮加固」）。
  根因要点：**硬链接没有「目标」可解析** —— 它就是同一份文件内容的另一个目录项，故
  `canonicalize("notes.md")` 仍是 `notes.md`，按名字、按落点两道判定全部放行，而读到的字节
  与 `.env` 一模一样。唯一可靠判据是枚举**该文件记录的全部硬链接名**。
  新增 `agent_tools::sensitive_hardlink_alias`（Win32 `FindFirstFileNameW`/`FindNextFileNameW`，
  Cargo 加 `Win32_Storage_FileSystem` feature；非 Windows 为 no-op），接在 `resolve_readable`
  第三道防线**之后**，任一别名叶子名命中 `is_sensitive_path` 即拒。
  **为什么不用 `std` 的文件身份 API**：`file_index()`/`number_of_links()` 在本工具链
  （rustc 1.97.0）仍是 unstable（`windows_by_handle`，issue #63010），stable 上编译不过 ——
  已实测确认，别再试一遍。
  **刻意保守 fail-open**：枚举 API 本身失败（非 NTFS 卷 / 权限 / 路径过长）时放行 ——
  它是名字与落点两道判定**之后**的第三道，前两道仍在；且枚举失败通常意味着底层文件系统
  根本不支持硬链接、本无此攻击面。宁可放行也不把正常读路径在边缘环境上全拦死。
  3 个测试：攻击形状（`notes.md` 硬链到 `.env` 必须被拒）、**不误伤**（普通文件 /
  名字吓人的源码 / 两个无害名字互为硬链接都必须放行）、图片入口同受保护（否则凭据会被
  base64 塞进请求体发给上游）。测试用 `mklink /H`，**普通权限即可、不静默跳过**（已实测）。
  **故障注入已验证**：禁用别名枚举 → 两条攻击测试立刻变红（`notes.md` 被放行），
  而「不误伤」那条仍绿 —— 证明三条分工准确。
- [x] **P3 文件恰好等于 2MiB 上限时误报「只读了前一段」—— 已闭环**（2026-08-02）。
  同上，「探读 1 字节区分真 EOF 与真截断」的**代码早已存在**，本轮补的是回归测试
  `read_file_at_exactly_2mib_does_not_false_report_truncation`（构造**恰好** 2097152 字节的文件，
  断言末行读得到且不出现「只读了前一段」）。**故障注入已验证**：把判定退回
  `reader.limit() == 0 && !hit_cap`（旧行为）→ 立刻变红并打印出误报原文。
- [x] **前端 i18n 抽查**（2026-08-02 复跑）：zh / en **各 448 键、双向零缺失**。
  `brain.tools*`(7) / `about.*`(17) / `logs.repeatHint` / `nav.about` 均两端齐备。
  **校验脚本的一个坑**：用 `/["']?([\w.]+)["']?\s*:\s*["`]/` 扫键会把英文文案**句中的冒号**
  误判成键（本轮误报出 `proactively` / `settings.json` / `connected` 等 5 项，实际来自
  `"Optional: add a line to AGENTS.md so Codex calls it proactively:"` 这类句子）—— 别当成真缺口。
- [ ] **提交 + 打包 + 安装到 `F:\SynaRoute`**：按 CLAUDE.md「更新后审查闭环」→ 提交（分块或单提交）
  → `tauri build` → 安装 → 核对 chunk 嵌入 → 复制到 F 盘。
- [ ] **生成三端 Excel 测试用例表**（CLI / 桌面端 / Codex 各一端 sheet）：覆盖 FR-001~027 +
  第八节真机清单 15 条 + 聚合工具/图片反例。用户将在另一台电脑逐条测。
- [ ] **真机验证**：docs/14 第八节 15 条（尤其聚合工具调用开关开/关两态、图片反例三条、
  纯文本模型带图降级提示）。**新增第 16 条**：在工作目录里 `mklink /H notes.md .env` 造一个
  硬链接，让聚合成员去读 `notes.md` → 应被拒且拒绝原因点明「硬链接」。

### 2026-08-02 本轮小结（缺陷三条全部闭环）

测试基线 **576 passed / 0 failed**（v0.1.14），`clippy --lib --tests` **零警告**；
`tsc --noEmit` exit 0。新增 6 个测试，**每一个都做过故障注入双向验证**（去掉防线必须变红）。

**给下一位接手者的方法论提醒**：本轮最大的时间陷阱是「文档/记忆说未修、代码其实已修」。
交接记录里那句「grep `lineno` 为 0」是**落盘那一刻**的事实，之后文件又被改过而记录没同步。
**动手前先用 grep/测试实证当前代码状态，别直接照着待办清单改** —— 否则要么重复劳动，
要么把已经修好的逻辑二次「修」坏。反过来也成立：代码修好 ≠ 完事，**没有故障注入验证过的
护栏，等于没有护栏**（本轮三条修复原本都处在「代码在、没测试锁」的状态）。

### 新对话接手要点

- 代码状态：工作区未提交，改动在 A/B/C/D 四块 + 本轮 `agent_tools.rs`（硬链接加固 + 6 个测试）
  与 `Cargo.toml`（`Win32_Storage_FileSystem` feature）；测试 448 全绿。
- 审查确认的 4 条缺陷**已全部闭环**，无已知未修缺陷。
- 下一步是**提交 + 打包 + 三端测试用例表**，然后才是真机验证 15（+1）条。
- 写完这节后若又触发压缩拦截，本文件是本对话的唯一持久化载体，新对话读「十一」即可。

---

## 十二、2026-08-09 进度落盘（v0.1.15 已发布；接手从此处读起）

> 与「十一」不同：本节写的时候**工作区几乎是干净的**，v0.1.15 已构建、已签名、已发布、
> 已复制到 `F:\SynaRoute`。唯一未提交的是一处**故意留着不提交**的半成品（见下「待办 1」）。

### 12.1 当前状态（一眼看清）

| 项 | 值 |
|---|---|
| 版本 | **0.1.18**（四处一致：package.json / tauri.conf.json / Cargo.toml / Cargo.lock） |
| 最新提交 | `1c16842`，**已推送**，`origin/master == HEAD` |
| Windows 测试基线 | `cargo test --lib` **588 passed / 0 failed** |
| macOS 测试基线 | **584 passed / 0 failed**，GitHub run `31299131199`（跑在 `fed0f6e`，含 FR-030），22 步全绿 |
| 与 0.1.17 同基线的原因 | FR-030 未新增自动化测试 —— 它的核心是「停代理但**保留 MCP**」这条编排，与 FR-029 的恢复编排一样需真的 Tauri AppHandle，只能真机验。可测的部分（还原逻辑本身）早有覆盖 |
| clippy | `--lib --all-targets` 零警告 |
| 前端 | `tsc --noEmit` exit 0；`npm run build` 通过 |
| 交付物 | `F:\SynaRoute\SynaRoute_0.1.18_x64-setup.exe`（6321189 B，sha256 `cf7379cf…`）+ `.sig`；复制后已核对 sha256 与源一致。前端嵌入判据：chunk 命中 1 次。<br>⚠️ 同级 `synaroute.exe` **仍未更新**：复制时被运行中的实例锁住。装 setup 即可，或先退出应用再复制 |
| Release | [v0.1.18](https://github.com/EngineeMoMo/SynaRoute/releases/tag/v0.1.18)，4 个资产。**已验签名一致性**：`latest.json` 内嵌签名 == Release `.sig` == 本地 `.sig`（三方 md5 均 `ea70f81f…`），且**下载回来的** setup.exe sha256 == 本地 == F 盘（均 `cf7379cf…`），签名对象逐字节相同（0.1.14 的签名错配未重演） |
| 本版新增 | FR-030 每分类「切官方」按钮（停代理 + 还原客户端配置，**保留 MCP**）；FR-029 代理状态记忆与自动恢复 |

**两边测试数差 4 条是刻意的平台门控，不是漏跑**：仅 Windows 6 条（4 条硬链接防线
用到 NTFS 的 ADS 语法与 8.3 短名；2 条托盘灰度派生，mac 走 template 图标故不存在这条路），
仅 mac 2 条（Unix `nlink` 语义，且必须豁免目录 —— APFS 上目录 `nlink` 天然 > 1）。
算术闭合 583 − 6 + 2 = 579。详见 docs/08 的双平台基线表。

### 12.2 本轮做完的事

**A4 轮代码审查修出 3 条**（提交 `e486706`，详见 docs/08 的 A4 节）：

1. **用量统计满环即冻结**（高，等于功能不可用）—— 总量原从 500 条事件环现算，
   环满后数字停止增长（实测 before=5000 / after=5000）。改为独立累加器 `usage_totals`，
   在唯一写入点累加，与事件环解耦。
2. **第二次及以后的熔断永远静默** —— 通知用 `breaker_until.is_some()` 判「此前是否已熔断」，
   而窗口自然到期时该字段仍留 `Some(过去时刻)`，故重新武装时判据恒为假。抽出
   `breaker_window_active(until, now)` 统一为「窗口是否还活着」。
   **`record_live_success` 刻意仍用 `is_some()`**：残留 `Some → None` 才精确对应
   「这个 Key 真的恢复了」，若也按窗口算，「窗口到期后第一次成功」就发不出恢复回执，
   用户手上的熔断告警会永远悬着。两处口径不同是设计，已在两侧注释互相指认。
3. **通知发送失败被 `let _` 丢弃** —— 健康告警链路整体坏掉时无任何痕迹。改为落 warn。

**用量累计改周期性落盘**（提交 `359ed56`）：新增 `usage.json`，与 `config.json` 同目录。
60s 后台合并写 + `RunEvent::Exit` 补写，**仅在有新消耗时才写**（空闲零 I/O，针对你提过的
SSD 写入寿命顾虑）。刻意不搭健康探测那趟车 —— 那趟车周期跟着用户配的
`health_check_interval_secs` 走，设 3600s 就一小时才落一次。

**CI 加版本号 / Cargo.lock 一致性前置判据**（提交 `ea5b36a`）：堵住连踩两次的 bump 坑
（run `31240396211`、`31242466356` 都是改了版本号忘了同步 lock，`--locked` 下第一条
cargo 命令即失败、后面十步全 skipped）。

**修掉 0.1.14 的签名错配**：Release 上的 setup.exe（6282430 B）与 `latest.json` 内嵌签名
不是同一对（签名来自另一次重建的 6287983 B 那份），应用内更新必然验签失败。
0.1.15 的 `.sig` 由 `tauri build` 与二进制同批产出，天然成对；并已用公钥
**真验签**通过（不只是比 sha256）。

### 12.3 待办

**1. ~~半成品：`usage.json` 版本门~~ ✅ 已完成（`fec7d47`，随 v0.1.16 发布）**

当时记的洞（版本门返回空 map 后，下一次 flush 会拿空累加器覆写更高版本的文件）
已补完：`Store` 加 `usage_read_only` 标记，`flush_usage_if_dirty` **在清脏标记之前**
短路返回 —— 顺序很关键，反了就是「一边保护文件一边丢数据」。
判据：`newer_usage_file_is_never_overwritten_by_older_program`。

**2. 真机验证（优先级最高，代码侧已无已知缺陷）**

- Windows：docs/14 第八节 15 条，**全部仍未验证**。
- macOS：docs/16 第 4 / 6 / 9 节（Gatekeeper 首启放行、Keychain 授权弹窗、
  日志与配置落点、`Claude-3p` 目录、菜单栏 template 图标可辨识度、dmg 拖拽安装、Cmd+Q）。
- docs/07 第 19 节九条 0.1.13/0.1.14 回归用例。
- **docs/07 第 20 节 12 条（FR-029 代理自动恢复）** —— 恢复编排无自动化覆盖，
  这 12 条是它唯一的验证手段。核心是 20.1/20.2（恢复后客户端**真的**走代理，
  而不只是界面显示已启动）。
- 用量面板「统计起点」显示正确、重启后累计**不归零**、
  熔断时**系统通知 + 应用内告警条**都出现、第二次熔断也有通知。

**3. NSIS 安装器从未验证过能否装成** —— 静默 `/S` 历史上卡死过两次，
F 盘放的一直是直接覆盖的 exe，不是装出来的。这条独立于代码质量，必须真机试。

**4. 应用内更新全流程从未真跑** —— 签名链已核对到位（v0.1.18 三方一致性已验：
`latest.json` 内嵌签名 == Release `.sig` == 本地 `.sig`，三方 md5 均 `ea70f81f…`，
且下载回来的二进制 sha256 == 本地 == F 盘，均 `cf7379cf…`），但「旧版本收到提示 →
下载 → 验签 → 替换 → 重启」这一条完整路径没走过。**现在有了合适的测试条件**：
手上有 0.1.15/0.1.16/0.1.17 旧包，装一个旧版就能触发更新提示。

**5. `F:\SynaRoute\synaroute.exe` 未更新到 0.1.18** —— 复制时被运行中的实例锁住
（0.1.17 时同样如此）。装 setup 即可，或退出应用后手动覆盖。setup.exe 与 .sig 都是新的。

**6. FR-030「切官方」的三条编排未真机验** —— 见 docs/07 §21。可测的部分（还原本身）
早有覆盖，但「停代理 + 还原客户端配置 + **MCP 仍在**」这个组合，以及切完官方后
Claude Code 里 `synaroute_ai` 是否仍能调起，只能真机看。这正是这条功能的全部价值所在。

### 12.4 刻意不做（别当遗漏重复劳动）

- **mac Developer ID 签名 + 公证**（你拍板：先发未公证版 + 使用说明）。
- **多分类共享 Key 池**（你拍板：先不做）。
- docs/15 的 P2-1 / P2-7 / P2-8 与 `AppSettings` 拆分 —— 纯结构调整、零用户可感收益、
  diff 最大风险最高，不该紧挨着真机测试做。
- 历史轮次小结里的 `576 passed` 等旧数字**刻意不改**：那是当时的实测值，
  按 CLAUDE.md 的口径不该被当成现值，改了反而抹掉时间线。

### 12.5 本轮的两条教训（都是我自己犯的，写下来防复发）

**教训一：`grep -c` 返回 0 会中断 `&&` 链，让「没有」看起来像「没查」。**
我一串 `&&` 拼的核对命令在第一个 0 处静默停住，导致我据此下过一个**错误结论**
（说 `backfill_usage_for_collapsed_event` 在生产路径从未被调用，还围着这个错前提
做了一整轮分析）。实际它早在 `e486706` 就已接进 `proxy.rs`。
核对存在性时用 `|| echo 0` 兜住，或改用循环逐项打印。

**教训二：从并行测试的 CI 日志抓测试名会漏。**
我第一次用 `grep -E '^test .* ok$'` 只捞到 549 个名字，而 run 报告 579 —— 并行输出
同行交错吃掉约 30 行，据此算的差集虚高。改用 `grep -oE 'test [\w:]+ \.\.\. ok'`
抓到 579 个唯一名、与报告数吻合后，差集才可信。
**判据：抓出来的条数必须等于 `test result` 里报的数，不等就说明解析漏了。**
